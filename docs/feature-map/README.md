# Feature Map

One table per area, mapping every user-facing dashboard feature to **how a user
reaches it** and **where its code lives**. It exists so that "which page and
which handler own this?" is a lookup rather than a search, for a human or an
agent landing cold on a feature.

It is a **navigation index, not a contract**. Behavior contracts live in
[`../system-specs/`](../system-specs/README.md); this file says where to go and
deliberately says nothing about how a feature works.

## Maintenance contract

**A pull request that ADDS or REMOVES a feature updates this map in the same
PR. A pull request that only changes how an existing feature behaves does
not.** That line is drawn where it is because a map re-reviewed on every edit
stops being read: the cost has to land on the change that actually invalidates
a row.

`scripts/check_feature_map.py` enforces the mechanical half. It reads the
`base..HEAD` diff and fails only when a file is **added or deleted** under
`website/src/pages/` or `src/kiro_crew/dashboard/handlers/`, or a `<Route>`
entry is added or removed in `website/src/App.tsx`, while this file is
untouched. Editing existing files never trips it. The job row is in
[../ci/ci-and-reviews.md](../ci/ci-and-reviews.md).

The judgment half is reviewer-owned: the blocking `feature-map-correctness`
rule in root `AUTOSDE.yaml` verifies each changed row against the code diff and
rejects unrelated or cosmetic map churn. The mechanical check cannot tell
whether a row's *content* is still true, only that a structural change went by
without anyone looking at the map. When the check fires and the honest answer
is "no feature changed", update the row the new file belongs to so its columns
remain truthful; a whitespace or cosmetic edit does not count.

## How to read this

- **Reach it** is the user's path, written as the URL plus the tab or control
  that gets there. `?tab=` and `?view=` are real query parameters the page
  parses, not shorthand.
- **Page** is relative to `website/src/`.
- **Handler** paths resolve under `src/kiro_crew/`: `handlers/` is under
  `dashboard/`, other directory prefixes are literal, and `src/` paths are
  repo-relative. Bare module names resolve by unique basename; a short name after
  a full path inherits that path's directory. Per-app paths are relative to the
  app named in the row.
- **Endpoints** are representative routes, not the complete set. The full table is
  `src/kiro_crew/dashboard/routes/` plus the direct registrations in
  `dashboard/server.py`; `test/test_dashboard_route_table.py` pins its order.
- `TODO(verify)` marks a cell nobody has confirmed against code. Fix it or
  leave it; never replace it with a guess.

Sidebar entries come from `website/src/surfaces/builtins.tsx`, which is the
authoritative nav table (label, group, badge, preview gate). A surface marked
`hiddenFromNav` there has a working route but no rail row — it is reached from
somewhere else, noted per row below.

---

## Chat and sessions

The default destination. `/chat` is the rail's **Sessions** row; everything in
this area is reached from inside it unless stated otherwise.

| Feature | What it is | Reach it | Page | Handler | Endpoints |
|---|---|---|---|---|---|
| Sessions | Multi-slot agent chat, one slot per conversation; pinned rows form a manually ordered section above automatic sorting. The sidebar's session list has three button-cycled layouts: the folder tree, the **conductor lane**, and the flat lane. The tag board is NOT in that cycle. A configured board renders instead of all three, and the header button then switches flattening INSIDE its columns. The button cycles tree -> conductor -> flat -> tree, skipping any layout that cannot render. The conductor lane nests LOCAL sessions under the session that OPENED them via `session_create`, or under whichever session has since TAKEN THEM OVER: `session_adopt` re-parents a session and `session_release` returns one to the top level, so the nesting is mutable and no longer opener-only. Collapsed by default, with a chevron and child count on a card that has children and that subtree's aggregated needs-you / running counts while it is collapsed; indentation is capped so a deep chain cannot walk the card off a narrow sidebar. Because an adoption moves a session that is already on screen, the lane auto-expands the row a session moved UNDER — a row whose citation CHANGED between two slots frames, and its whole ancestor chain — so an agent-initiated takeover cannot unmount the rows the person was watching; a row that is merely NEW keeps the collapsed default, which is what `session_create` still gets. Sessions from a federated PEER gateway always render as roots, never nested: the peer-slot allowlist at that trust boundary (`_clean_peer_slot`) does not carry `parent`, so no lineage crosses it and a peer's own parent/child pairs are not reconstructed here. The conductor lane is offered only when some row carries a creator and no board is configured. The edge itself rides the slots broadcast as `parent: {slot, key} | null` per row, resolved from an in-memory session-tree projection the crew-log writer advances at commit time, so nesting costs no scan per poll; the System page's Sessions table nests on the same field through one shared `nestsUnder` | `/chat/:slug?` — rail **Sessions** | `pages/ChatPage.tsx`, `pages/ChatSidebar.tsx`, `pages/chat/SessionFlyout.tsx`, `pages/chat/TranscriptScrollShell.tsx` (internal split of ChatPage — the transcript scroller skeleton, no new user-facing feature), `pages/chat/ChatPageMessageContent.tsx` (internal split of ChatPage — the header menu, row-key helpers and user-bubble content renderers, no new user-facing feature), `pages/chat/useChatPageTranscriptController.tsx` (internal split of ChatPage — scroll-to-bottom, auto-follow gating, nav scrolling and the pinned-prompt banner, no new user-facing feature), `pages/chat/useChatPageSessionController.ts` (internal split of ChatPage — session identity, `?sid` URL sync, session tabs and slot auto-create, no new user-facing feature), `pages/chat/useChatPageResourcesController.tsx` (internal split of ChatPage — side-panel tabs, source/issue selection, file/folder/artifact/diff opening, uploads, screen snips and drops, no new user-facing feature), `pages/chat/hoverHold.ts` (internal split of ChatSidebar — seat arithmetic for the hovered-row hold, no new user-facing feature), `pages/chat/CompactionCard.tsx` (transcript row for the gateway's compaction / session-reload status notices — folds the compaction summary behind a one-line card instead of an assistant bubble; no new user-facing entry point), `pages/chat/transientNotice.ts` (helper for the existing error row — resolves the gateway's transient-retry `meta.notice` token into localized NoticeCard / ErrorCard copy; no new user-facing entry point), `utils/pinnedSessionOrder.ts`, `lib/sessionLineage.ts` (the one lineage reader both the sidebar's conductor lane and the System page's Sessions table nest with) | `chat_handlers.py`, `ws.py`, `src/kiro_crew/crew_log/session_tree_projection.py` (the in-memory tree the writer advances at commit; `src/kiro_crew/crew_log/session_tree.py` remains its cold rebuild path) | `POST /api/chat`, `GET,POST /api/chat/slots` (each slot row carries `parent`), `GET /api/ws` (the `slots` frame carries the same `parent`) |
| Agent picker | The composer's agent pop-up lists every execution choice for the chat — configured crewmates (each wearing its roster avatar) and installed shared templates — grouped under **Crewmates** / **Agent templates**, so a member and a template of one name are two rows and picking one binds that namespace to the session (a template pick runs the shared template without enrolling it as a member or pinning member memory). The same read-only catalog feeds the schedule form, channel and project bindings and the agent-cycle shortcuts, folded to one row per name; opening any of them no longer enrols discovered templates as crew members | Chat composer → agent chip (also split-view panes); `Alt+Shift+A` / `Alt+Shift+Z` cycle | `components/AgentDropdownList.tsx`, `hooks/useAgents.ts`, `components/ChatPane.tsx`, `pages/ChatPage.tsx` | `handlers/agent_catalog.py`, `chat_handlers.py` (`agent_kind` on create and switch) | `GET /api/agents/catalog`, `POST /api/chat/slots` (`agent`, `agent_kind`), `POST /api/chat/slots/{slot}/agent` (`agent`, `agent_kind`) |
| Sessions page | A neutral, bookmarkable session chooser at a stable URL — lists open sessions with search, filter chips (All / Unread / tags) and recency groups; never auto-selects or auto-creates a session, unlike bare `/chat` | `/sessions` — typed or bookmarked URL only (deliberately no nav-rail entry; pending a maintainer call) | `pages/SessionsPage.tsx`, `pages/chat/sessionOrder.ts` (shared ordering with the sidebar) | none new — reads the same SSE slots frames and existing handlers | `GET /api/ws` (`slots`), `GET /api/chat/tags`, `POST /api/chat/slots` (New session) |
| Default memory mode | Picks Persistent, Incognito, or Temporary for new dashboard-created chats; explicit per-chat choices win, while app-owned and direct API sessions keep their own mode | Settings → Chat → **Default Memory Mode**; the Welcome view can override one chat | `pages/settings/ChatPanel.tsx`, `store/chatSlice.ts`, `components/WelcomeView.tsx` | `handlers/files.py` (`api_dashboard_config`), `chat_handlers.py`, `remote_relay.py` | `GET,PUT /api/dashboard/config`, `POST /api/chat/slots` |
| Remote-bound session | A local session whose turns execute on a connected peer crew and stream back over its tunnel — local sidebar row, transcript and history, remote execution | New-chat menu → **New chat on crew** → pick a connected crew | `pages/ChatPage.tsx`, `pages/ChatSidebar.tsx`, `components/RemoteCrewChip.tsx` | `chat_handlers.py`, `handlers_instances.py`, `handlers/core.py`, `remote_relay.py`, `remote_mirror.py` | `POST /api/chat/slots` (`instance_id`), `POST /api/chat?relay=1`, `GET /api/instances/{id}/capabilities`, `GET /api/version` |
| Peer-owned live session | The OPPOSITE direction of travel from the row above: a session a connected peer crew owns, merged read-only into this machine's live Sessions list and ordered with local rows by recency, badged with the owning crew. Clicking one opens it HERE: the hub binds a fresh local slot to that peer session and backfills the peer's transcript into it, so the row opens a real local conversation whose turns keep running on the peer. The bound row keeps the identity the peer row had (`<instance_id>:<peer_key>`), so it re-renders in place instead of appearing as a second row. The row still omits rename, close, pin, folders, drag and the ⋯ group, because the peer — not this machine — owns the session itself. An adopted session drops out of the peer rows, so it is never listed twice. Board view stays local and reports the count it filtered. The peer's CLOSED sessions are not here; they are reachable only through federated history search | Settings → Developer → Feature Previews → **Remote crew sessions** (default off), then rail **Sessions** | `pages/ChatSidebar.tsx`, `hooks/useInstanceSessions.ts`, `components/RemoteCrewChip.tsx`, `pages/settings/FeaturePreviewsSection.tsx` | `handlers_instances.py`, `chat_handlers.py`, `remote_adopt.py`, `slot_projection.py` | `GET /api/instances/{id}/chat-slots` (owner-only, GET-only; reads the peer's `GET /api/chat/slots`, drops the slots this hub itself drives, then allowlists each row to the fields the sidebar reads and re-redacts its text — a peer's session title is model-authored on another machine, and the local rows beside it are scrubbed by `slot_projection.py`), `POST /api/chat/slots` (`adopt_remote_slot` — binds a fresh local slot to the peer session named by a row of the listing above, and backfills its transcript; refuses with `adopt_peer_mode_unknown` when the crew reports no `memory_mode`, rather than guessing the session's privacy boundary) |
| Session folders | User-defined folders grouping session rows; a folder holding no sessions can optionally drop its body so it costs one row instead of two. A folder can also declare extra **steering directories**: the always-inclusion `*.md` files under them (`.kiro/steering`-style) are delivered to every session filed in the folder or its subfolders (folder membership belongs to a dashboard session slot, including a channel conversation surfaced as one -- a Slack, Discord, Telegram or Teams session the reconciler or channel auto-filing placed in a folder, or a Slack thread linked with `/kirocrew link-to-dashboard`; a turn typed in that slot's dashboard tab runs on the linked channel session through the dashboard chat runner and receives the folder's steering, while a turn arriving from the channel side is dispatched with no slot and does not), on every model provider (kiro-cli, Claude Code, Codex, KAS or a config-authored harness) -- with one provider-aware difference: a document under the project's or the operator's own `.kiro/steering` tree is skipped only where the provider already delivers that tree (kiro-cli natively, Claude Code through the explicit steering load, KAS via `native_steering`) and is delivered through the folder on the harnesses that have no such path (Codex, OpenCode, Pi, Goose, DeepSeek) -- accumulating root-first up the parent chain, bounded to 64 documents and 4096 directory entries per root with any excess reported in-band as a `[FOLDER STEERING OMISSION: …]` line; only the person may **declare** steering directories -- `POST`/`PATCH /api/chat/folders` from an app or crew-member principal refuses a non-empty `steering_dirs` with 403 (`steering_dirs_forbidden`, audited) before any path is touched, because folder permission is not host-file permission and the gateway reads these files unsandboxed; clearing to `[]` is still allowed -- and a folder a non-person principal owns steers only chats running as that principal (an app's folder → that app's slots; a crew member's folder, stamped `member:<store>`, → that member's own private-memory chats); a private-member chat in **Temporary** memory mode receives none of it, the same way that mode withholds the member's project steering (the essentials envelope is built with reads blocked), while a non-member Temporary chat still receives it. Not available on native Windows: the tree is read only through descriptor-pinned directory handles (never by name), and Windows cannot open a directory relative to a descriptor, so there `PATCH /api/chat/folders/{id}` refuses a non-empty `steering_dirs` before touching the filesystem (clearing to `[]` still works) and the collector skips a stored value with a warning rather than attempting a by-name walk | Sidebar folder header → drag a row; folder ⋯ menu → New ephemeral chat (incognito / temporary) to start a mode-pinned session in the folder; folder ⋯ menu → **Folder settings** → **Additional steering** → Add directory (a child folder lists what it inherits read-only, filtered the way delivery is: an ancestor owned by another principal is not listed because its directories never reach this folder's chats); Settings → Chat → **Compact Empty Folders** (off by default) | `pages/chat/FolderPanel.tsx`, `pages/ChatSidebar.tsx`, `components/FolderConfigModal.tsx`, `utils/folderAgent.ts`, `pages/settings/ChatPanel.tsx`, `pages/chat/ChatSettings.tsx` | `chat_folders.py`, `chat_handlers.py`, `chat_runner.py`, `folder_steering.py`, `context.py` | `GET,POST /api/chat/folders`, `PATCH /api/chat/folders/{id}` (`steering_dirs`), `PATCH /api/chat/slots/{slot}/folder`, `POST /api/chat/slots` |
| Folder search | Find a session folder by name and jump to it. The sidebar's search box matches folder names as well as session titles, keeping a matched folder with its whole subtree; the command launcher reaches folders the way it reaches sessions and artifacts — one **Search Folders** row that opens the folder list, which lands on every folder and narrows as you type. The launcher owns that corpus (`apps/command-bar/foldersProvider.ts`): the host's quick-search palette carries no Folders tab, so there is one implementation rather than two that drift. Choosing a folder makes it visible for the visit, expands its collapsed ancestors, scrolls it into view and flashes it. Lane-dependent: the flat lane renders no folder rows at all, so a match there is visible only through the sessions it keeps and a reveal leaves that lane for the tree; the board lane draws a folder as a column, so a reveal flashes the column rather than a header row | Sidebar search box → type a folder name; ⌘K / Ctrl+K → **Search Folders** → type a folder name (a folder name typed in the launcher root offers the same view as a row) | `pages/ChatSidebar.tsx`, `apps/command-bar/CommandBarOverlay.tsx`, `apps/command-bar/foldersProvider.ts`, `apps/command-bar/rootIndex.ts`, `store/chatSlice.ts`, `utils/folderTree.ts` | none new — reads the folder tree the sidebar already loads | `GET /api/chat/folders` |
| Session tags | Colored labels on sessions, filterable | Sidebar row context menu → Tags | `pages/chat/SessionFlyout.tsx` | `chat_tags.py` | `GET,POST /api/chat/tags`, `PUT /api/chat/slots/{slot}/tags` |
| Embedded MCP Apps | An MCP tool result that declares a `ui://` resource renders as an interactive app inline in the transcript (gateway-stubbed servers only, behind `mcp_gateway.apps_enabled`). The app frame supports inline/wide/overlay presentation, app-initiated tool callbacks scoped to app-visible tools, and the `ui/message` return channel: an app-sent user-role message (an Acknowledge click, a form submit) lands in the owning session as a provenance-bannered app-authored turn — queued behind a live turn, never mirrored to a linked channel as human speech | Transcript — call any MCP-App tool from a gateway-stubbed server | `components/McpAppFrame.tsx` (not under `pages/` — it is a transcript row component), `lib/mcpAppSrcdoc.ts`, `lib/mcpAppTheme.ts` | `handlers/mcp_apps.py`, `src/kiro_crew/mcp_apps_render.py`, `src/kiro_crew/mcp_gateway/apps.py` + `app_call.py` (gateway side) | `POST /api/mcp-apps/call`, `POST /api/mcp-apps/message` |
| Older sessions | The sidebar's history pane, searchable, with per-session delete and a bulk **Delete all** | Sidebar → **Older Sessions** disclosure | `pages/ChatSidebar.tsx` | `handlers/sessions.py` | `GET /api/sessions` (`exclude_open`, `user_only`), `GET /api/sessions/search`, `GET /api/sessions/{key}`, `DELETE /api/sessions/{key}`, `DELETE /api/sessions`, `GET /api/sessions/clearable/count` |
| Pinned messages | Pin a message; pins panel per session | Message hover → pin; header pin count | `pages/chat/PinnedMessagesPanel.tsx` | `chat_pins.py` | `GET,POST /api/chat/pins`, `DELETE /api/chat/pins/{id}` |
| Pinned prompt banner | Keeps the user's latest typed prompt — a steer included — visible in a single-state banner at the top of a long reply while it scrolls; distinct from **Pinned messages** above. Clicking it glides back to that prompt, smooth at any distance and landing exactly — to the head of its run when several typed prompts sit back to back, so the previous turn's banner survives the landing — and each further click walks one turn further back. Worn by the main chat AND by every `ChatPane` host — split-view panes and the Crew Members DM thread — through the same hook | Settings → Chat → **Pin the latest turn** | `pages/chat/PinnedPrompt.tsx`, `pages/chat/usePinnedPrompt.ts` (internal split of `useChatPageTranscriptController` — the host-agnostic pin geometry, scroll recompute and in-place jump shared with `components/ChatPane.tsx`; no new user-facing entry point), `utils/pinnedPrompt.ts` (which rows pin, push and anchor the jump), `utils/convergingGlide.ts` (the self-driven travel-then-converge scroll both jump hosts run; the virtualized host steers an unmounted target by `useVirtualChat.estimateRowTop`), `app-sdk/ChatMessageList.tsx` (`onDisplayItems` / `hiddenRow` row-indexing seam) | client-only (no handler) | none |
| Turn minimap | A proportional, non-scrolling scrub rail of turn markers in the transcript's gutter, capped at 45 markers — longer loaded histories are evenly bucketed so the rail's footprint stays constant, and the newest turn always keeps its own dedicated final marker (an image-only send counts, since it persists as image markdown; earlier, not-yet-loaded history has no markers — the transcript's own paging affordance loads it); the markers for on-screen turns read in the text color, hovering grows markers with a fisheye falloff and floats a preview card beside the rail (the turn in its conversational neighbourhood — one faint indexed neighbour title above and below — the final assistant reply before the next turn opener, and a meta row naming the covered turn span plus relative time), click / arrow keys jump to that turn (Escape closes the preview), and press-and-drag along the rail scrubs the chat live with instant scrolls. Settings → Chat → **Minimap** picks the hosting edge: left (default), or right, where the rail replaces the native scrollbar while shown. Desktop-only by design | Session transcript → gutter; appears when the viewport is at least the `md` breakpoint (768px), the pane is ≥ 560px wide with ≥ 40px of free gutter beside the content column, there are two or more turns, and the primary pointer is not coarse (`(pointer: coarse)` — touch — hides it; a hover-capable stylus counts as fine) | `pages/chat/TurnNavigationMinimap.tsx` (mounted by `pages/ChatPage.tsx`; items from `hooks/useChatNavigation.ts`; edge picked in `pages/settings/ChatPanel.tsx`, stored by `pages/chat/ChatSettings.tsx` under `mc-chat-config`) | client-only (no handler) | none |
| Message font size | Resizes the conversation surface independently of the rest of the dashboard: message text, inline and block code, tables, follow-up chips and the composer, each at its default ratio to body text; sidebar, session list and other chrome are unaffected. On the chat page the Compact content width scales alongside so the column keeps its characters per line; embedded/popout chat sessions scale the same text and composer but have no Content width setting and keep their own column | Settings → Chat → **Message font size** | `pages/ChatPage.tsx`, `components/ChatPane.tsx`, `pages/chat/ChatSettings.tsx`, `pages/chat/UserMessage.tsx`, `pages/chat/AssistantMessage.tsx`, `pages/settings/ChatPanel.tsx`, `styles/message-font-size.css`, `components/MarkdownRenderer.tsx`, `components/FollowUpBar.tsx`, `components/PasteHighlightLayer.tsx`, `components/LexicalComposerInput.tsx`, `app-sdk/ChatEmbed.tsx`, `app-sdk/messageRenderers.tsx` | client-only (no handler) | none |
| Collapse the message input | Puts the composer away while you read a long reply and hands the room to the transcript (measured 89px at 1500x950); a labelled bar stands in its place, reports the unsent draft’s first line, and restores it. Off by default, and the choice persists. Collapses the opposite end of the same reply from the **Pinned prompt banner** above | Composer **+** menu → **Collapse the message input**; on touch, the **⋯** overflow beside the attach button | `components/ChatInput.tsx` (opted in by `pages/ChatPage.tsx`; focus intents resolve through `pages/chat/composerFocus.ts`) | client-only (no handler) | none |
| Show pasted text in full | Keeps a long paste as editable text in the composer instead of folding it into a `[ Paste #1 · 23 lines ]` chip. Off by default — the chip is what keeps a 100k-line paste off the main thread, so the full-text shape is opt-in rather than inherited by every client with no stored config — and the choice persists. `Cmd/Ctrl+Shift+V` remains the one-off raw paste for anyone who leaves it off. Both composers read the one flag at their single existing collapse branch, so the plain textarea and the Lexical editor cannot drift apart | Settings → Chat → Composer → **Show Pasted Text in Full** | `pages/settings/ChatPanel.tsx` (stored by `pages/chat/ChatSettings.tsx` under `mc-chat-config`; consumed by `components/ChatInput.tsx` and `components/LexicalComposerInput.tsx`, opted in by `pages/ChatPage.tsx`) | client-only (no handler) | none |
| Share message as card | Turn an assistant reply into a branded PNG card + prefilled caption for X/LinkedIn; governed by `capabilities.social_share` (a policy pin withdraws the entry) | Message hover → More actions → Share as image | `pages/chat/share/ShareMessageModal.tsx`, `pages/chat/share/ShareCard.tsx` (helpers: `pages/chat/share/shareSupport.ts`) | `dashboard/social_share.py` (governance probe; card itself is client-side) | `GET /api/dashboard/config` (`social_share_enabled`) |
| Fork a session | Branch a new slot from an existing transcript; an incognito or temporary session forks into a child of the same memory mode | Session row menu → Fork | `pages/ChatSidebar.tsx` | `chat_fork.py` | `POST /api/chat/slots/{slot}/fork` |
| Rewind | Drop the transcript back to an earlier turn | Message action → Rewind | `pages/chat/AssistantMessage.tsx` | `chat_rewind.py` | `POST /api/chat/slots/{slot}/rewind` |
| Regenerate / variants | Re-run a turn, keep and switch between answers | Message action → Regenerate | `pages/chat/AssistantMessage.tsx` | `chat_regenerate.py` | `POST /api/chat/slots/{slot}/regenerate`, `.../switch-variant`, `.../edit-resend` |
| Session title | Auto-generated and hand-editable slot titles; a generated title can be undone from the header for a few seconds | Header title → click to rename / hover → **Auto-title**, in the single-session header and in every split-view pane header | `pages/chat/SessionTitleControl.tsx` (the shared inline-rename + Auto-title + Undo control), `pages/ChatPage.tsx` and `components/ChatPane.tsx` (its two hosts), `pages/ChatSidebar.tsx` (the sidebar row's own rename) | `chat_title.py` | `POST /api/chat/slots/{slot}/generate-title`, `PATCH .../title` |
| Session summary | Right-panel rolling summary of the conversation | Chat right panel → **Summary** tab | `pages/chat/SessionSummaryTab.tsx` | `chat_handlers.py` | `GET,POST /api/chat/slots/{slot}/summary` |
| Crew log | Right-panel read of the session's append-only record, as the five folds the backend serves: lifecycle and turn status, credits / tokens / compactions, a timeline window, per-tool totals with the calls still open, and approvals with what is waiting on the user. Each fold is a section whose collapsed header carries its own figures; the footer states the record seq the values were folded through. A slot-key read resolves to the session the slot's turns run on, so a channel-linked slot folds the channel session's record rather than an empty one. Re-reads when the session's own turn stops and when its spawned work goes idle, plus a manual control; a read waits briefly for the writer to drain and says so when it could not, and an id with no addressable unit is reported as that rather than as nothing recorded. Diagnostics, so it sits behind Developer Mode with Logs and Context | Chat right panel → **+** menu → **Crew log** (Developer Mode on); also under `/members`, where the Crew Members side panel is the same `SidePanel` and offers the row once the member's thread confirms the slot | `pages/chat/CrewLogPanel.tsx`, `pages/chat/ActivityViewer.tsx`, `pages/chat/SidePanel.tsx` | `handlers/crew_log.py` | `GET /api/sessions/{id}/crew-log/projections` (every advertised panel fold from one read), `GET /api/sessions/{id}/crew-log/projection/{status,usage,timeline,tools,approvals}` |
| Side chat | A scratch sub-conversation beside the main turn; read-only lookups (file reads, searches, fetches, read-only shell) run there without asking on the kiro-cli backend, changes are refused | Chat right panel → **Side**; `/side` or `/btw` in the composer; **Ask about this** on selected assistant text (main chat, split-view panes, Members threads — the seam is `chat-core/composer/selectionActions.ts`) | `pages/chat/SideChat.tsx`, `pages/chat/SidePanel.tsx`, `pages/members/MembersPage.tsx` (detail drawer's Side Chat view) | `handlers/side.py` | `POST /api/chat/slots/{slot}/side/open`, `.../side/turn`, `.../side/close` |
| Channel mirroring | Mirror a session into Slack / Discord / other channel | Header channel menu → Link channel | `pages/chat/ChatSettings.tsx` | `chat_mirror.py`, `chat_slack.py` | `POST /api/chat/slots/{slot}/mirror-link`, `.../slack-link`, `GET /api/chat/channel-targets` |
| Voice reply and dictation | TTS on replies; streaming mic transcription | Composer mic; Reply → More actions → Read aloud; Settings → Voice; Settings → Voice → Tidy up transcripts with AI | `components/ChatInput.tsx`, `components/VoicePlaybackNotice.tsx`, `pages/chat/AssistantMessage.tsx`, `pages/settings/SttSettings.tsx` | `chat_voice.py`, `stt_stream.py`, `handlers/core.py` | `POST /api/voice/synthesize`, `POST /api/voice/cancel`, `GET /api/voice/system-voices`, `GET /api/ws/stt`, `POST /api/stt/transcribe`, `POST /api/stt/polish`, `POST /api/stt/prewarm` |
| Decision strip | One-line receipts under an assistant reply for the decisions Jev made on that turn — one row per decision, so a turn decided twice shows two. The SKILL row: which skills the shipped word-matching rule would have loaded, which ones Jev answered, how sure it was, and the prompt tokens the narrower set saved. Both sides picking the same skills reads as one muted check over one list; a divergence names both, because that is the only thing on the line a reader can act on. Expanding adds the question's own shape — candidates offered, in how many batches, characters of history carried, what was left out and which answers the gate refused with their scores — plus a second thumbs pair, so a reader can say the word-matching rule was the right one instead of only rating Jev. Present wherever the row carries the record, and the **Decisions (Jev)** switch is deliberately NOT consulted: a stamped record is history that already sits on this machine, so drawing it sends nothing, and the switch governs whether a future turn may ask Jev. Gating the receipt on the current switch would mean an owner who tried the preview and turned it off loses the record of which past replies were Jev-picked. The MODEL row (`model.route`): the difficulty level Jev answered, the model that level routed the turn to, how sure it was, how long the decision took, and the model the turn would otherwise have used — that last one is what makes the line actionable, since "complex" alone is not a claim a reader can disagree with. Expanding adds the level, both models, the history the question carried and what was clipped. ONE thumbs pair on the Jev side, because the alternative to a routed model is the absence of a judgement rather than a second one. Which row is drawn comes from the record's own `point`, never from which fields it happens to carry, and a point the reader does not know draws nothing. Rendered by every transcript host, the main chat and the panes alike. Drawn only for a turn the seam decided, so an ordinary reply looks exactly as it does without a record | Under an assistant reply, when the seam answered for that turn (Settings → Developer → Feature Previews → **Decisions (Jev)**) | `pages/chat/DecisionStrip.tsx`, `pages/chat/decisionRecord.ts` (the pure reader — validates each record, dispatches on its `point`, accepts the list the gateway stamps as well as the bare object an earlier release stamped, and remembers a thumb across the virtualised row; no feature of its own), `pages/chat/AssistantMessage.tsx`, `pages/ChatPage.tsx` and `app-sdk/messageRenderers.tsx` (its two hosts) | `handlers/decisions.py` (consent and the verdict route; the record itself is stamped on the assistant row by `chat_runner.py`) | `GET /api/decisions/consent`. The thumbs post to `POST /api/decisions/feedback`, and a refused write surfaces on the strip through the shared error surface |
| Compaction keep line | One line under the compaction notice in the transcript — **Jev would keep 23 of 61 tool calls · 41% of the characters** — with a thumbs pair beside it. A MEASUREMENT and nothing else: when context fills and Kiro Crew ITSELF compacts a session automatically, Jev is asked, per tool call, whether the call and its result were worth keeping, and NOTHING is kept — the compaction runs exactly as it does without the seam whatever the answer is. Scored on the compactions Crew dispatches from the threshold path (`_compact_session`: the in-place `/compact` on kiro-cli, codex and opencode, and the recycle that follows a failed one or serves a backend with no compaction at all). NOT scored where the harness compacts on its own initiative and the gate therefore declines before that funnel — Claude-Code sessions (`cc_managed`) and KAS (`ACP_BACKENDS_HARNESS_MANAGED_COMPACTION`), which summarize and report it with no Crew-side dispatch to hang a measurement on — nor on a manual `/compact`, nor on any other gate decline (below threshold, unconfirmed reading, already compacting, cooldown). The copy says "would" for that reason: what today's compaction keeps is the conversation text alone (every tool call and every tool result is already dropped by `context.RECALL_ROLES`), and a reader who took this line for a description of what happened would believe their tool history survived. It exists so a later, live keep-list can be argued about with numbers: the row behind it carries what the transcript held, what today's replay keeps and what Jev's answer would have kept. Manual `/compact` is never scored — the dashboard dispatches that command as an ordinary turn and never reaches the coordinator, so the exclusion is structural. Drawn under EVERY shape the compaction card takes (the success notice, the recycle notice and the failure that renders through the shared error surface), because the scoring starts before the compaction picks an arm. A PARTIAL answer draws nothing at all, since "23 of 61" over calls nobody was asked about is a wrong number rather than an incomplete one; so does a scoring slower than the compaction it observed. Present wherever the row carries the record, and the **Decisions (Jev)** switch is deliberately NOT consulted, for the reason the decision strip states | Under the compaction notice in the transcript, when the seam scored that compaction. Needs Settings → Developer → Feature Previews → **Decisions (Jev)**, and the third switch under it, **Also send the conversation and tool-call inputs so Jev can score compaction**, which starts off even for an owner who already granted the other two | `pages/chat/CompactionKeepLine.tsx`, `pages/chat/decisionRecord.ts` (`readCompactionKeepRecord` — the pure reader; it sums the log's own tallies and refuses a record whose parts disagree with the total, no feature of its own), `pages/chat/CompactionCard.tsx` (its host), `pages/chat/DecisionVerdictThumbs.tsx` (the verdict control, shared with the decision strip) | `decisions/points/compaction_keep.py` (the point and its record store), `session_compaction.py` (`_shadow_score_compaction`, the unawaited hook at the top of `_compact_session`), `dashboard/state.py` (`_compaction_keep_record`, which stamps the record on the notice row), `handlers/decisions.py` (the verdict route) | No route of its own. The thumbs post to `POST /api/decisions/feedback` |
| Memory decision strip | A one-line receipt under an assistant reply for a turn in which the agent called `memory_recall` and Jev narrowed what came back: how many entries the recall found, how many Jev kept, its average confidence that one was worth the prompt, how long it took, and the characters of recalled memory TEXT the smaller answer left out — `memory · recalled: 6 · Jev kept: 3 (confidence 0.81, 210 ms) · saved 2.1K prompt chars`. The figure is that text alone, clipped the way the store clips it (`EPISODIC_BLOCK_TEXT_CHARS`) and counting none of the `[memory:id]` framing around each line: it is what the dropped entries would have contributed, not a measurement of the whole prompt, which the seam never sees. COUNTS collapsed, ids expanded: a memory id is a store handle a reader cannot read anything off at a glance, so six of them on one line would be six opaque words where the only actionable fact is that three were dropped. Both counts are printed even when they are equal, because "similarity 6 · Jev kept 6" already says the two sides agreed. Expanding adds both id lists, how many were offered, the characters of message the question carried, the latency, and a second thumbs pair for the similarity ranker, so a reader can say the plain closest-match list was the better one. Each id in the expanded lists is a BUTTON: clicking one opens a small dialog (`MemoryPopover`) that resolves the id as an episode by `{kind,id}` identity in the SAME store the recall ran against — global (`default`) or a member's (V2), threaded onto the record — never a free-text search, which cannot find a key-less episode and could fuzzy-match a different one — and shows that memory's full text, so a reader who wants the words behind a handle can read them without leaving the transcript. Recall ids are episode ids, and the store they belong to travels on the record, so the dialog looks each one up in its own store — a member (V2) recall's ids resolve in that member's store rather than reading as "not found". A record no longer in the store reads as "not found" rather than as a mismatched one. The decision itself only ever REMOVES from the shortlist — it cannot add a memory the search did not rank or one its own size budget excluded, cannot reorder them, and every refusal returns the shortlist unchanged — and it is taken only for a session with an open dashboard tab, never for a cron, a sub-agent, an app or a session nobody has open, because the question carries snippets of the owner's own remembered notes and the receipt needs a reply they are looking at. A channel-born conversation counts while its tab is open, which is the same "is anyone watching this" reading every other dashboard-surface gate uses. Drawn wherever the row carries the record, with the **Decisions (Jev)** switch deliberately NOT consulted, for the reason the Decision strip row above gives | Under an assistant reply, when the seam decided a `memory_recall` call in that turn (Settings → Developer → Feature Previews → **Decisions (Jev)**, plus the recalled-memory switch under it) | `pages/chat/MemoryRecallStrip.tsx`, `pages/chat/MemoryPopover.tsx` (the clicked-id dialog — a `Dialog` over `api.memoryRecordsRefresh`, keyed by the id so a second click cannot paint a stale record; no reader of its own), `pages/chat/decisionRecord.ts` (`readMemoryRecallRecord` — the pure reader; it requires `point` and reads the two lists as `baseline_keys`/`jev_keys`, which is what keeps it and the skill reader from rendering each other's records), `pages/chat/AssistantMessage.tsx` | `decisions/points/memory_recall.py` (the point), `handlers/memory_member.py` (`_memory_recall_keep`, the owner-dashboard gate and the `keep=` wiring on the recall route), `vector_memory.py` (`recall(keep=...)` / `_recall_once`, which owns every fallback), `decisions/outcomes.py` and `chat_runner.py` (the record on the finalized row) | The popover resolves a clicked id through `POST /api/memory/records/refresh`; the thumbs post to `POST /api/decisions/feedback` |
| Recalled-memory egress consent | A switch of its own under **Decisions (Jev)** — *Also send snippets of recalled memories so Jev can drop the ones that do not help* — and the only thing that lets `memory.recall` run. A SEPARATE consent rather than a wider reading of the main switch, because the category is genuinely new: the main switch is agreed against a message excerpt the owner just typed and skill descriptions this build shipped, while a recalled memory is text the AGENT wrote down turns or days ago about work the owner was not reviewing when they flipped it. So it starts OFF even for an owner who already had the main switch on, an install consented before it existed is inert for the point rather than retroactively signed up, and only a literal `true` on the keystone counts. Drawn only while the main switch is on, since nothing is sent at all below that and a second egress control would describe a state that cannot happen. Independent of the two switches beside it in every direction — granting one grants nothing else — because an owner may want risky tool calls flagged without the contents of their memory store leaving the machine. A click writes the scope field ALONE: no `enabled` travels with it, not even the value the card holds, because that value comes from a read a concurrent revoking PUT makes stale and writing it back would re-commit a consent the owner had just withdrawn — the gateway preserves the recorded flag and endpoint for an absent one. Every switch on the card freezes while any scope write is in flight, and a refused write says so in its own notice beside the switch it belongs to rather than only snapping the switch back | Settings → Developer → Feature Previews → **Decisions (Jev)** → the recalled-memory switch | `pages/settings/FeaturePreviewsSection.tsx`, `pages/settings/decisionsPreview.ts` (`memoryText` — the pure reader; only a literal `true` counts, so an older gateway and an unrecorded scope both read off) | `decisions/consent.py` (`STATE_KEY_MEMORY_TEXT`, `consented_memory_text`, `KEEP_MEMORY_TEXT`, `KEEP_ENABLED`), `decisions/gate.py` (`POINTS_NEEDING_MEMORY_TEXT`, `_POINT_SCOPES`, `_scope_consented`), `handlers/decisions.py` (the consent route) | `GET,PUT /api/decisions/consent` (`memory_text`); a scope-only PUT omits `enabled` |
| Tool risk badge | A small note under a tool card — **Jev: risky (0.88)** — in a session that approves its own tool calls, saying the Decisions (Jev) seam looked at that call and thought it was worth a second look. An ANNOTATION and nothing else: the permission DECISION is taken without consulting it, so the note changes nothing about what is allowed. It is not a verdict on whether the call RAN either -- a host gate or a PreToolUse hook can still refuse a call that carries a note, and the SEL tool-invocation audit is where that outcome lives. The tooltip says so in words — it states that flagging changed nothing about whether the call was allowed and that the call may still have been refused, never that it ran — and `risky` is drawn in the warning colour rather than the danger colour this transcript uses for a call that WAS blocked, because a coloured word on a tool card otherwise reads as "this was refused". Only the two flagged tiers are drawn, and each of them has to be BELIEVED: a `safe` answer is recorded in the log and leaves the card exactly as it looks without the seam, and so does a `caution` or `risky` answer the seam was under that tier's OWN bar confident about (two separately movable constants, both 0.80 today), so the note's presence is the flag. Both bars are measured rather than picked -- the calls a `risky` answer BELOW its bar suppresses were a queued monitor edit and a comment posted on a pull request, and at 0.90 eleven real force-pushes went unflagged; the `caution` bar was added after the log showed the tier with no bar drawing most of every note and burying the alarming word in the mild one (measured distribution: see `CAUTION_CONFIDENCE_THRESHOLD` in `decisions/points/tool_risk.py`). A session that PROMPTS is never annotated — the person reading the call is the annotation, and a second opinion beside a control they are operating is noise. Carries the same thumbs pair as the decision strip (side `jev`, one shared component), so a reader can say the risk was read wrong. At most twenty notes per turn, and a slow or failed answer draws nothing. The one cost is timing: the note is awaited before this turn's next event is read, so a sampled call's approval can be answered up to the seam's own wait budget later -- bounded, and paid only by a session the switch is on for | Under a tool card in the transcript, when the seam answered for that call and the session auto-approves. Needs BOTH consents: Settings → Developer → Feature Previews → **Decisions (Jev)**, and the second switch under it, **Also send tool-call arguments so Jev can flag risky calls**, which starts off even for an owner who already had the first one on | `pages/chat/ToolRiskBadge.tsx`, `pages/chat/toolRiskRecord.ts` (the pure reader — validates the record and refuses the `safe` tier, no feature of its own), `pages/chat/DecisionVerdictThumbs.tsx` (the verdict control, shared with the decision strip), `pages/chat/ToolCallLine.tsx` (its host) | `decisions/points/tool_risk.py` (the point), `chat_runner.py` (`_tool_risk_meta`, which stamps the record on the tool row), `handlers/decisions.py` (the verdict route) | No route of its own. The thumbs post to `POST /api/decisions/feedback` |
| Auto (Jev) send while busy | A third mode on the composer's split send button, beside Steer and Queue: Jev decides per message whether text sent into a RUNNING turn interrupts that turn or waits for the next one (`message.steer`). Both paths are the shipped ones and neither changes — the mode only chooses between them, and EVERY refusal (the seam off, this session unsampled, a timeout, an answer outside the two options) takes the steer path the button has always defaulted to, so the feature can only ever pick the other one. Picking Steer or Queue by hand asks nothing and sends nothing: only `steer: "auto"` is decided, and only for a send the dashboard owner made while a turn is running — an app token, an integration and a cron are never decided for and keep falling through to the fail-closed queue. The mode is drawn only while the gateway reports BOTH the fleet ceiling (`decisions_enabled`) and the owner's consent (`permits`), fail-closed on each, AND a turn is actually running — not merely a busy composer, which also covers a slot whose sub-agents are still working and has no turn to decide about — and a stored `auto` resolves back to Steer while any of those is missing, so a withdrawn consent cannot leave a mode on screen whose send would not be decided. The request carries the new message and, only inside the consented `history_budget_chars` ceiling (default `0`, so nothing by default), a redacted extract of the turn in progress: its own request and the newest assistant text and tool activity, never its private reasoning. The receipt rides the USER row it is about as `meta.decisions_strip` and renders as one line — what Jev CHOSE (the choice, never the delivery, because a chosen steer whose client is gone falls through to the queue), how sure it was, how long it took, and the same `jev` thumbs the decision strip uses. The key is reserved to the gateway: a request's own `meta` cannot carry one, so no caller can stamp a decision nobody made. ⌘↩ still performs the OTHER action for one send, which from this mode is a manual queue that asks nothing. A send nobody decided for carries no record and draws nothing | Composer split send button while a turn runs → **Auto (Jev)** (needs Settings → Developer → Feature Previews → **Decisions (Jev)**) | `components/BusySendButton.tsx`, `components/ChatInput.tsx`, `pages/chat/useJevAutoSend.ts` (the availability read), `pages/chat/SteerDecisionLine.tsx`, `pages/chat/decisionRecord.ts` (`readSteerRecord`), `pages/chat/UserMessage.tsx`, `pages/ChatPage.tsx` and `components/ChatPane.tsx` (its two hosts) | `decisions/points/message_steer.py`, `chat_handlers.py` (the busy branch that asks), `chat_delivery.py` (the stamp on each path) | `POST /api/chat` (`steer: "auto"`), `GET /api/dashboard/config` (`decisions_enabled`), `GET /api/decisions/consent` (`permits`). The thumbs post to `POST /api/decisions/feedback` |
| Tool approvals | Approve or deny a tool call the agent proposes | Inline card in the transcript | `components/ApprovalCard.tsx` | `handlers/sessions.py` | `GET /api/approvals`, `POST /api/approvals/{id}/{action}` |
| Question cards | Agent asks a multiple-choice question in chat | Inline card in the transcript | `components/PendingQuestionCard.tsx` | `handlers/ask_question.py` | `POST /api/ask-question`, `GET /api/ask-question/pending`, `POST /api/ask-question/{ask_id}/answer` |
| Files in chat | Browse, attach and upload workspace files; search the rail by file NAME or by file CONTENTS, including the text inside Word/PowerPoint/Excel documents; preview one in the side panel — a .docx renders its headings, lists, tables and bold/italic runs, falling back to extracted plaintext; right-click a file row → Download saves it to the machine running the browser through the credential-gated download endpoint; complete a relative path in the composer by typing `./` or `../`, which lists one directory level of the project (never above its root); the header carries the per-project actions — reveal the project directory, and open a terminal already in it | Chat right panel → Files tab, Name/Content toggle below the search field; right-click a file row → **Download**; composer attach; composer `./` / `../` path token; Files tab header → reveal button and **Project actions** → **Open terminal in the project directory**, whose row carries a one-line note saying what a terminal is | `pages/chat/FileBrowserRail.tsx`, `pages/chat/FilesHomePanel.tsx`, `api/fileGrep.ts`, `components/FileRenderers.tsx`, `pierre/PierreWorkspaceTreeImpl.tsx`, `components/ChatInput.tsx`, `components/FilePickerMenu.tsx`, `components/composerTokens.ts` | `handlers/files.py` | `GET /api/file-read`, `GET /api/file-office-preview`, `GET /api/file-download`, `POST /api/file-grep`, `GET /api/file-search`, `GET /api/path-complete`, `POST /api/upload`, `POST /api/upload/file` |
| Terminal panel | Two shells on the gateway host: an app-wide docked PTY, and a per-chat terminal whose tab lives in that chat's panel state (opens on the chat's working dir; switches with the session) | Header terminal toggle (docked); chat right panel → **+** menu → **Terminal**, or Files tab header → **Project actions** → **Open terminal in the project directory** — both go through one `openProjectTerminal` callback, so they cannot disagree about the cwd (per-chat) | `components/BottomTerminalPanel.tsx`, `pages/chat/SidePanel.tsx`, `pages/chat/FilesHomePanel.tsx` | `handlers/terminal.py` | `POST /api/terminal/sessions`, `GET /api/ws/terminal/{session_id}` |
| Browser panel | Live in-panel browser the agent drives; the non-native address bar opens a real website in it; the framed CLI view is served through the dashboard-origin relay (`/browser-view/`), so it works remotely over the same forwarded port as the dashboard; **Annotate** (desktop) picks page elements, takes a note per element in the panel, and adds them to chat with the agent's `eN` refs (`browser:annotate` IPC, `electron/browser-annotate.js`) | Right panel → **Browser** | `components/WebPreviewPanel.tsx` | `handlers/messaging.py`, `handlers/browser_view_relay.py` | `GET /api/browser/view`, `POST /api/browser/view/start`, `POST /api/browser/open`, `GET /browser-view/{tail}`, `POST /api/browser/command`, `POST /api/browser/command-result` |
| App-contributed panel tabs | Side-panel tabs an installed app declares in `contributes.panelTabs`; each mounts the app's own ESM bundle as the tab body through the in-process app host. Only enabled apps contribute, and no app means no rows | Chat right panel → **+** menu → the app's row; on an empty panel, its launcher card. Also under `/members` — the Crew Members side panel is the same `SidePanel`, so an app's row appears in its **+** menu once the member's thread is confirmed | `pages/chat/SidePanel.tsx`, `hooks/panelTabRegistry.ts`, `components/AppHost.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps`, `GET /apps/{name}/ui/{path}` |
| Route-history Back / Forward | Browser-style Back and Forward over the dashboard's own route history (desktop layout; on desktop a session switch pushes an entry, so it retraces sessions as well as pages). Each arrow disables when there is nowhere to go (the Forward watermark persists per tab in `sessionStorage` across a reload; it is discarded on a fresh navigation, and a cross-document Back/Forward return (or a BFCache restore) collapses the watermark to the current entry — persisted, so a reload cannot resurrect it — until an in-app navigation re-establishes the frontier — so Forward never leaves the dashboard) and every step asks the page's draft guard before an unarmed pop can unmount typed work | Topbar left cluster → **Back** / **Forward** arrows; `⌘←` / `⌘→` on macOS, `Ctrl+←` / `Ctrl+→` elsewhere (registry ids `history-back` / `history-forward`; left to text fields and terminals, claimed as a no-op on narrow viewports where the arrows are hidden) | `components/NavHistoryArrows.tsx`, `lib/routeHistoryPosition.ts`, `components/NavigationLeaveGuard.tsx` (`useGuardedHistoryStep`), `hooks/useKeyboardShortcuts.ts`, `lib/shortcutRegistry.ts` | client-only (no handler) | none |
| Notifications | Bell feed of agent-pushed notifications; a live `notification` frame also shows a Notification Center-style banner under the top bar that tucks into the bell (approvals keep their own row) (see `docs/system-specs/modules/app-notifications.md` § In-app banner) | Topbar bell → `/notifications` | `pages/NotificationsPage.tsx`, `components/notifications/NotificationBanner.tsx` | `handlers/messaging.py`, `handlers/notifications_push.py` | `GET /api/notifications`, `POST /api/notifications/ack`, `POST /api/notifications/push` |
| Crew Members | One durable pinned DM thread per crew member, plus a configurable webview each crew fills in, with the chat page's tabbed SidePanel docked beside it (permanent, no close control on wide windows; an overlay on narrow ones) whose first three tabs are the crewmate's Notes (its self-maintained briefing, read-only), Work log (which also embeds the DM thread's own Crew Log session record -- the same `CrewLogTab` the chat page's Developer-Mode Crew log tab shows -- once the thread endpoint has confirmed the slot) and Dashboard, followed by the chat panel's pinned Artifacts and Files tabs, with the + menu offering its remaining views (Terminal / Browser / Side chat / Subagents / Workflows / Git, plus Logs / Context / Crew log in Developer Mode) and any app-contributed panel tabs against the DM slot — all withheld until the thread endpoint confirms the slot, and Changes (pinned on the chat page) / Issues / Links / Pins / session Summary withheld outright (no transcript index feeds them here); the Work log tab folds the crewmate's recent activity by calendar day (counts per day, project, a time strip behind each row, floors when the log is capped), lists the worker sessions the member is driving (live `slots` frames filtered on `created_by`) and the member's auto-patrol status — the nudge loop on its own `member-<slug>` slot (active / stopped with reason / none), with a roster avatar badge only while a loop is ACTIVE (a stopped loop and a never-armed member both show no badge — the stop reason lives in the Work log tab); the roster carries a per-crew star (persisted on the crew record) and, in the sidebar's search row's sort/filter menu, persistent filters — starred-only, live state (working / needs you / unread / patrolling), origin (mine / built-in / from packages) — and a sort (recent activity / name), so the package-installed crews the agent sync writes can be collapsed. The panel's Side tab is the thread's **Side Chat** surface (selection toolbar → Ask about this lands there; its draft persists per slot in the chat-core store) | `/members` — rail row when the crew preview is on; also the sidebar create menu → **Crew Members** (always shown, its own separator group): with the preview on it opens `/members`, with it off it deep-links to Settings → Developer → Feature Previews with the Crew Members card ringed (`settingsPath` + `useSettingHighlight`, id `developer.crew-members`). The open member rides `?member=<name>` (a bare `/members` reopens the remembered member, else lands on the roster with the "Pick a member" empty pane; a `?member=` naming someone gone falls back the same way, with a notice; below md it stays the roster); select text in a member reply → **Quote** / **Ask about this**; the panel's **Dashboard** tab (the crewmate's published webview, expandable to full window) and its **Notes** tab (read-only: the crewmate's own briefing rendered as markdown, with a visible line above the text when part of it is hidden; no editor, the file is agent-written) | `pages/members/MembersPage.tsx`, `pages/members/CrewWebview.tsx`, `pages/members/rosterFilter.ts` (internal split of MembersPage — the pure filter/sort model over the roster array, no new user-facing feature), `components/listShell.ts` + `components/SearchFilterBar.tsx` (the Sessions sidebar's card / row / search-row recipes, shared so the two lists read as one surface), `pages/members/activityDays.ts` (pure helpers: fold the activity log by local day, label days, cap floors — no feature of its own), `pages/chat/SidePanel.tsx` (`leadingTabs`), `pages/members/CrewNotesTab.tsx`, `pages/chat/CrewLogPanel.tsx` (`CrewLogTab`, embedded in Work log), `hooks/usePanelDocumentActions.ts`, `components/autoNudgeLoop.ts` (shared cycle/countdown readouts) | `handlers/members.py`, `handlers/crew_log.py` (`api_session_crew_log_projections`), `handlers/agent_panel.py`, `handlers/agents.py` (`starred`), `slot_projection.py` (`created_by`), `handlers/autonudge.py` (registry read), `handlers/side.py` (Side Chat) | `GET /api/members`, `POST /api/members/{slug}/thread`, `GET /api/members/{slug}/activity`, `GET /api/members/{slug}/briefing`, `GET /api/sessions/{id}/crew-log/projections` (the Work log's session record), `GET /api/members/{slug}/panel`, `POST /api/agent-panel/publish`, `GET /api/agent-panel/templates`, `GET,PUT /api/members/{slug}/rules`, `PUT /api/agents/{name}` (`starred`), `GET /api/autonudge`, `GET /api/ws` (`slots`, `autonudge_state`), `POST /api/chat/slots/{slot}/side/*` |
| Reply threads | A Slack-style reply thread on any one message of a crewmate's chat that carries a durable id (every message written since ids were minted; an older row without one has no Reply action) — yours or the crewmate's. Under a bubble that has replies, a footer shows the faces of who took part, "N replies" and "Last reply 2h ago"; every user bubble's hover row and every finished crewmate reply's footer row carry **Reply in thread** (a mid-turn crewmate row that runs on into a tool call has no footer, so no Reply until the turn's last reply). Either opens the thread in the right side panel over its tabs (the main chat stays visible): the parent quoted as one bubble, a hairline reply count, the replies as small bubbles on the grouped-corner rule, a typing row while the crewmate replies, and a one-line **Reply…** composer. The crewmate's reply runs in its own read-only session and lands in the thread, never in the main chat. Only a crewmate's chat offers threads, and only while the `dashboard.crewmate_threads` flag is on (default off: no Reply control, no footer, the routes answer not-found) | Settings → Developer → **Crewmates** → **Reply threads on crewmate chat messages** (default off); then `/members` → a crewmate's chat → hover a bubble → **Reply in thread**, or click a bubble's replies footer | `pages/settings/CrewmatesSection.tsx` (the switch), `hooks/useCrewmateThreadsFlag.ts`, `pages/members/ThreadPanel.tsx` (reads the run and corner rule from `components/chat/crewmateBubbles.ts`), `pages/chat/ThreadFooter.tsx`, `pages/chat/AssistantMessage.tsx` + `pages/chat/UserMessage.tsx` (the row action), `app-sdk/messageRenderers.tsx` (`ThreadHooks`), `components/ChatPane.tsx` (`threads`), `state/threadLiveStore.ts`, `api/threads.ts` | `chat_threads.py`, `history.py` (`read_threads` / `append_thread_reply`), `ws.py` (`broadcast_thread_reply`) | `GET /api/chat/threads?slot=`, `GET /api/chat/threads/{mid}?slot=`, `POST /api/chat/threads/{mid}/reply`, the `chat.thread_reply` WS frame |
| Member event log | A per-member append-only ledger drives the Crew Members roster and drawer: roster / activity / wake projections push whole values to the page over the WebSocket so a change (a star toggled elsewhere, a patrol that stopped, a model edited) re-renders in place with no roster refetch and survives a restart with its reason. The gateway is not the only writer: `kirocrew-core` runs as its own subprocess and records member activity through the same service, so the log has two ordinary writers and each one re-reads the file it holds before serving a read. The log lives under the fenced `crew-log/` root like every other crew log | No new page: it feeds the existing `/members` roster, drawer and card (see **Crew Members**) | `pages/members/MembersPage.tsx`, `state/memberProjectionStore.ts`, `state/useMemberProjection.ts`, `hooks/useWebSocket.ts` (`member_projection` frame) | `handlers/members.py`, `eventlog/service.py`, `eventlog/log.py`, `eventlog_hooks.py`, `crew_log/store.py` | the `member_projection` and `members_subscribed` WS frames |
| Crewmate teams | A team is a name plus an ordered list of crewmates, the manager's grouping of the roster; a crewmate is on at most one team, and the list is one document in the masked `crew-teams/` directory, written only by the gateway and by the operator's own `kirocrew agent create` / `delete` (which purge or drop the crew's membership), so a crewmate's own file tools cannot re-team itself. Storage and API only in this row: the Crewmates page's grouped roster and team view build on it | No page yet: the `/members` roster grouping and team view (a follow-up) are its only consumer | none yet (`pages/members/MembersPage.tsx` in the follow-up) | `crew_teams.py`, `handlers/teams.py`, `handlers/agents.py` (crew delete drops the crew from its team) | `GET,POST /api/teams`, `PUT,DELETE /api/teams/{id}` |
| Crewmate chat | How a crew member's DM thread is drawn: what the member says to the user, the user's own messages, and the rows the user must see or act on (errors, notices, files, OAuth prompts, a pending approval, the stop card, the live stream) — the auto-nudge patrol turns, cron / workflow / sub-agent envelopes, tool rows, resolved approvals and empty (say-nothing) replies stay in the slot's history but are not rendered — and the member's messages read as Slack-style runs: avatar + name + time on the first message of a run, one bordered bubble per message, grouped corners along the run, a run being one turn's bubbles — it breaks on a user message, on any other visible row (error, pending approval, stop card) and at a turn boundary (a patrol wake or envelope between two replies). Ordinary chats are untouched. A never-spoken crewmate's empty chat says so and links to its Work log; the composer says "Message <name>…" | The thread under `/members` (see **Crew Members**); the empty hint's "Work log" link (`onOpenCrewWorkLog`) | `pages/chat/CrewmateMessage.tsx` (the author line + bubble placement of one member message), `components/chat/crewmateBubbles.ts` (what is drawn and the run / corner rule), `components/ChatPane.tsx` (`crewmate` prop), `pages/chat/transcriptRenderers.tsx` (the member assistant row), `pages/members/MembersPage.tsx` (passes the identity) | `handlers/members.py` (roster preview is speech-only, `last_speech_info`), `dashboard/state.py` (`member/message` event carries a preview only for speech), `dashboard/system_notices.py` (`is_speech_row`, the one backend spelling of what the chat draws) | `GET /api/members` (`last_message`), the `member_projection` WS frame |
| Your crew in the cloud | Whether the crew is deployed in the cloud and what to do next, in one plain sentence: for an EC2 deploy, the Session Manager target of the machine to copy; for a Fargate deploy, the container task's cluster and id with its ECS console page; read-only (creating, cancelling and deleting a launch stay in Settings > Remote Crew) | `/members` — the **Cloud** button in the page header | `pages/members/DeployMyCrew.tsx` (EC2), `pages/members/FargateCrewView.tsx` (Fargate), `pages/members/MembersPage.tsx` (the trigger) | `handlers_cloud.py` (launch list, task read), `handlers_instances.py` (registry) | `GET /api/cloud/launch`, `GET /api/cloud/launch/{id}/task`, `GET /api/instances` |
| Crewmate prune (startup) | Once, when the dashboard gateway starts (`start_dashboard`; the headless `--slack-only` entrypoint has no dashboard and does not run it), removes the crewmates an older agent sync generated from the custom agents under `~/.kiro/agents` that were never chatted with; the ones that were chatted with stay exactly as they are (shared store, no `member_id` — a memory binding is chosen at creation only). No UI: the roster is simply shorter on the next visit. Evidence: the DM binding file, the member activity log, and each session's metadata `agent`. Removal needs a complete history: a session file that cannot be read (torn, not UTF-8, not JSON, unreadable) keeps every candidate, while one that reads but names nobody is evidence and voids nothing; a crewmate whose own activity log or binding cannot be read is kept; the kept rows are named in the marker and the pass still finishes (never re-run on a bad file). One process runs the pass (cross-process lock beside the marker; the marker is created exclusively). A row `config.local.json` touches is never removed. The agent files are never touched; removed rows do not come back because nothing has called `POST /api/agents/sync` since #12224 | `/members` — the roster after the first start on the new build | — | `crewmate_prune_migration.py` (`prune_synced_crewmates`, called from `dashboard/server.py` `start_dashboard`) | — (marker `<config dir>/crewmate_prune_migrated.json`, lock `<config dir>/crewmate_prune.lock`) |
| Channels | Group rooms with several agents in one thread | `/channels` (builtin app surface) | `pages/ChannelPage.tsx` | `handlers_channel.py` | `GET,POST /api/channels`, `POST /api/channels/{id}/messages`, `POST /api/channels/{id}/agents` |

The Notifications surface is registered `hiddenFromNav`: its route and badge
stay wired, but it is entered from the topbar bell rather than a rail row.

## Agent Capabilities

One destination, pinned to the bottom of the rail, hosting nine tabs. Every tab
is a `?tab=` value on `/capabilities` (`pages/CapabilitiesPage.tsx`).

Any one of those tabs can also be **promoted to its own top-level rail row**, so
a sub-item someone uses daily is one click away instead of two. Each tab is
registered as a `pinnable` surface (`surfaces/builtins.tsx`,
`surfaces/registry.ts`), which keeps it OFF the rail until the user promotes it;
the promoted set is a per-browser preference held under `mc-nav-pinned` and
owned by `lib/navPinned.ts` (the sibling of `lib/appNavHidden.ts`, which does
the same job for app rows in the Apps group). Reach it from the pin control in
the Agent Capabilities page header (`components/PinSurfaceButton.tsx`), which
resolves its subject from the current `?tab=` value. Promotions are capped at
`NAV_PINNED_LIMIT`, and the rail applies the filter in `App.tsx`. No handler and
no endpoint: the preference never leaves the browser.

| Tab | What it is | Reach it | Page | Handler | Endpoints |
|---|---|---|---|---|---|
| Crews | Named agent bindings — which agent, model and workspace; per-crew custom avatar (hand-picked ghost traits, or an uploaded picture; a crew can also wear an appearance pack, picked on the builder's Library tab — see Crew appearance library). A ghost also has a REACTION layer, authored on the builder's **Reactions** tab: a built-in motion when a turn finishes (`none`/`bounce`/`nod`/`sparkle`, default bounce) and when one fails (`none`/`shake`/`cross-eyes`/`droop`, default shake), plus an optional preset sound for working, done and error. The tab is ghost-only — a picture is static and silent, and a pack ships its own per-state art and audio. The per-state eyes/mouth `expressions` layer is retired — no picker writes it and nothing draws it — but retirement is a RENDERING decision, not a deletion: the editor rewrites the whole `avatar` record on any save, so `retiredCarryFrom` re-attaches a stored record's dead reaction keys (`expressions` on a ghost; `expressions`/`motions`/`sounds` on a picture or pack, where both readers are ghost-gated) to an outgoing record still on the same tier. Editing a crew's model therefore cannot be what deletes a preset cue a previous release let the user save, and switching tier still drops the old tier's keys because that is a deliberate act; the definition panel forks a private template copy on first edit (`components/crew/AgentTemplateDetail.tsx`) | `/capabilities?tab=crews` | `pages/KiroCrewAgentsPage.tsx`, `components/CrewAvatarBuilder.tsx`, `components/CrewAvatarLibraryTab.tsx`, `components/CrewAvatar.tsx` (renders the state it is told), `components/CrewStateAvatar.tsx` (observes the crew's slot and plays its cue), `hooks/useCrewAvatarState.ts` (the working/done/error transitions, the cue edge and its debounce), `lib/kiroGhostAvatar.ts` (the ghost and its built-in motions), `lib/crewAvatarState.ts` (reads `motions`/`sounds` off the record, ghost-only; `retiredCueFrom` for the builder's lost-cue notice — the states AND the tier they were saved on, so each pane shows only its own tier's notice — and `retiredCarryFrom` for the save-time carry), `hooks/useNotificationSound.ts` (`playPreset` for the ghost's cue, `playSoundFile` for a pack's), `lib/appearancePacks/` | `handlers/agents.py` | `GET,PUT /api/config/default-agent`, `POST /api/agents`, `PUT,DELETE /api/agents/{name}`, `GET,POST /api/agents/{name}/avatar`, `POST /api/agents/detail/{name}/fork`, `POST /api/agents/detail/{name}/publish`, `POST /api/agents/detail/{name}/reset` |
| Crew capabilities | Member-level MCP connections, tool exposure, automatic approval and Skill mappings with explicit Parent inheritance, reviewed saves and runtime status | `/capabilities?tab=crews` → member → Capabilities | `pages/KiroCrewAgentsPage.tsx`, `components/crew/CrewCapabilitiesPane.tsx` | `handlers/agent_capabilities.py` | `GET,PUT /api/agents/{name}/capabilities`, `POST /api/agents/{name}/capabilities/preview` |
| Crew appearance library | The dashboard's own library of appearance packs — art AND per-state sound cues — a crew can wear, rooted at `<data home>/appearance-library/`. Separate from Crew Companion's library: that app is independent and keeps its own packs; the two share only the pack format, so a pack exported from the Companion gallery imports here unchanged. A crew record names a pack as `avatar: {"kind":"pack","id":...}`, and the API can list, read, import, PetDex-fetch and delete packs; deleting a pack a crew wears is refused by name unless forced. The crew editor's avatar builder carries the picker on a **Library** tab: a grid of cards (thumbnail read through the per-slot route, name, author, built-in/custom badge, format), an "Import pack (.json)…" control that posts an exported bundle, and a two-click delete on a custom pack whose 409 names the crews still wearing it (`?force=1` is deliberately not offered). **Crews wear any format the library holds** — `PackAvatar` dispatches on the format of the slot the state resolves to, drawing an `<img>` for `svg`, `LottieRenderer` for `lottie` and `SpriteRenderer` for a `sprite` sheet, all three in core so a crew's face renders while Crew Companion is absent. A pack may also carry a per-state sound (`sounds` in its manifest, `.mp3`/`.ogg`/`.wav` under 512 KB, typed by sniffing the bytes), served one state at a time from `GET /api/appearances/{id}/sound/{state}` and travelling in the bundle with the art. **A worn pack's cue is what a pack-wearing crew plays**: on a reaction edge into a state the pack declares a sound for — `working`, `done` or `error`, the vocabulary `appearance_packs/sounds.py` accepts — `CrewStateAvatar` reads the states the pack DECLARES (`sounds` presence on the detail payload) and plays that state's bytes through the same global sound toggle and volume the ghost's presets use, with silence — never a preset — for a state the pack declares none for. While the pack's manifest is still being read, the edge is HELD rather than consumed, and when the manifest lands the cue plays for the state the crew is in at that moment: a state it already passed through during the read (a `working` that became `done` before the manifest arrived) is not replayed, and a reaction whose flash has already ended gets no late cue. The picker does not author a pack's cue; the pack's own bundle carries it | `/capabilities?tab=crews` → a crew → Edit avatar → Library | `components/CrewAvatarLibraryTab.tsx`, `components/CrewAvatarBuilder.tsx`, `components/CrewAvatar.tsx` (the pack tier), `components/appearancePacks/PackAvatar.tsx` (draws a slot; `LottieRenderer`/`SpriteRenderer` beside it), `hooks/usePackDetail.ts` (the one read per worn pack), `lib/appearancePacks/detail.ts` (`packDetailFrom`, incl. `sounds` presence), `components/CrewStateAvatar.tsx` (reads which states the pack declares a cue for and plays it on the edge), `hooks/useCrewAvatarState.ts` (`cueFor`: a pack's cue short-circuits the presets), `hooks/useNotificationSound.ts` (`playSoundFile`), `lib/appearancePacks/library.ts` (`packSlotUrl`, `packSoundUrl`), `lib/appearancePacks/types.ts` | `handlers/appearances.py`, `dashboard/appearances.py` (crew store), `appearance_packs/store.py` (shared store class), `appearance_packs/transfer.py` (bundle import and PetDex fetch), `appearance_packs/sounds.py` (cue vocabulary and audio sniffing) | `GET /api/appearances`, `GET /api/appearances/{id}`, `GET /api/appearances/{id}/slot/{slot}`, `GET /api/appearances/{id}/sound/{state}`, `POST /api/appearances/import`, `POST /api/appearances/petdex/fetch`, `DELETE /api/appearances/{id}` |
| Agent Templates | The harness-level agent definitions crews bind to. The **Agent templates** tab lists every installed template by origin (Mine / Crewmate overrides / From packages / Built-in) with what still runs it; an owned template's description, model, prompt and tools (with auto-approval marks) are edited as one draft saved through the detail PATCH, while skills save on their own through the shared skills editor (they are a computed view over `resources`, not part of the draft); a package, runtime, markdown or private-copy template is read-only with **Duplicate to edit**; **New template** starts blank or copies an installed one; **Delete** is refused with a reference list while a crewmate, the default agent, a schedule, a chat folder's default agent, a webhook token or a private copy still names it (an open chat that picked the template is deliberately not a holder); **Chat with this template** opens a chat in the template namespace and **Enroll as crewmate** binds a member to it. The per-crew Template pane (`components/crew/AgentTemplateDetail.tsx`) still edits a crew's private copy | `/capabilities?tab=templates`; the crew's Template pane under `/capabilities?tab=crews` | `pages/overview/AgentTemplatesTab.tsx`, `pages/KiroCrewAgentsPage.tsx`, `components/crew/AgentTemplateDetail.tsx` | `handlers/agent_templates.py`, `handlers/agents.py` | `GET,POST /api/agents/templates`, `GET,PATCH,DELETE /api/agents/detail/{name}`, `GET /api/agents/installed`, `POST /api/agents/detail/{name}/fork`, `POST /api/agents/detail/{name}/publish`, `POST /api/agents/detail/{name}/reset` |
| Connections | MCP servers: install, sign in, enable, scope tools | `?tab=mcp` | `pages/connections/ConnectionsPage.tsx` | `handlers/mcp.py`, `handlers/connections.py`, `handlers/mcp_discover.py` | `GET /api/mcp`, `GET /api/mcp/discover`, `POST /api/connections/mint`, `POST /api/mcp/custom` |
| Skills | Installed skills, the public registry, pending candidates | `?tab=skills` | `pages/overview/SkillsTab.tsx` | `handlers/prompts.py`, `handlers/discover.py`, `handlers/skill_budget.py` | `GET,POST /api/skills`, `GET /api/skills/-/discover`, `GET /api/skills/-/pending` |
| Knowledge | The document library: sources, sync, entities, graph | `?tab=knowledge` | `pages/knowledge/index.tsx` | `handlers/knowledge.py` | `GET /api/knowledge/items`, `POST /api/knowledge/sources`, `GET /api/knowledge/graph` |
| Prompts | Reusable prompt entries from the registry | `?tab=prompts` | `pages/overview/PromptsTab.tsx` | `handlers/prompts.py` | `GET /api/prompts`, `GET /api/prompts/{name}` |
| Steering | Always-injected steering documents | `?tab=steering` | `pages/overview/SteeringTab.tsx` | `handlers/steering.py` | `GET,POST /api/steering`, `PUT,DELETE /api/steering/{key}` |
| Hooks | Event-triggered agent runs | `?tab=hooks` | `pages/HooksPage.tsx` | `handlers/hooks.py` | `GET,POST /api/hooks`, `PUT,DELETE /api/hooks/{hook_id}`, `GET /api/kiro-hooks` |
| Workflows | Saved dynamic-workflow definitions and their runs. A run's detail reads in two modes: the phase **Tree** (what happened, newest work last) and a **Graph** that draws the run's PLANNED phase graph — read off the run's own captured script source — and colours each node from the live run state (planned / running / ran ok / ran failed), marks a phase the plan could not promise runs at all, marks work that ran without being predicted, and says when the plan is partial because the preview hit its ceiling. The plan rides the run-detail snapshot and is requested only while Graph mode is open | `?tab=workflows` | `pages/overview/WorkflowLibraryTab.tsx`, `apps/workflows/WorkflowsRuns.tsx` (Tree / Graph switch), `apps/workflows/WorkflowRunGraph.tsx` (the drawing), `apps/workflows/planModel.ts` (pure model: pair the plan with what ran) | `handlers/workflows.py`, `workflows/preview.py` (read the planned graph off a script's source) | `POST /api/workflows/run`, `GET,POST /api/workflows/definitions`, `POST /api/workflows/author`, `GET /api/workflows/runs/{id}` (carries `plan` when `plan=1`) |

`/agents`, `/mc-agents` and `/connections` are legacy paths that redirect here;
`/knowledge` redirects to `?tab=knowledge`. `/hooks` also stands alone as a
full page.

## Memory, lessons and usage

Not a rail destination. The user-facing memory browser is a drill-in under
Settings → Overview; the graph visualizer is a Developer internals view.

The picker separates Global Memory V1 from each member's private V2. Member
creation allocates its empty memory automatically; the editor shows an immutable
binding and links directly to `/settings/overview?view=memory&store=<name>`. Legacy members
offer explicit empty initialization. The member panel scopes facts, directives,
experiences, recall evidence, preferences, projects, carve, retirement and backups
to its store. Copying selected starting knowledge is an explicit owner action
with provenance and no overwrite. Global settings and embedding controls appear
only on the global surface. Global V1's shared editor also keeps the Key/Value/Set
action for creating facts through `PUT /api/memory/semantic`; the form never
appears on a member's private store. An absent `?store=` continues to serve V1.

Private memory cards show the remembered content; their internal keys are in
Memory details. Selection controls occupy a separate row from Find and replace
and Forget. Legacy schedule rows state that the run uses Global Memory V1.

| Feature | What it is | Reach it | Page | Handler | Endpoints |
|---|---|---|---|---|---|
| Memory browser | Paged facts, rules and experiences; text search, memory-type filters, single correction and bulk preview/apply; preferences, history and diagnostics | `/settings/overview?view=memory` | `pages/overview/MemoryTab.tsx`, `pages/overview/MemoryRecordsEditor.tsx` | `handlers/memory.py`, `handlers/memory_edit.py` | `GET /api/memory/records`, `POST /api/memory/records/refresh`, `POST /api/memory/bulk/preview`, `POST /api/memory/bulk/apply` |
| Store picker | Choose Global Memory V1 or one member's private store | Memory browser → store card | `pages/overview/MemoryStoreCard.tsx` | `handlers/memory_admin.py` | `GET /api/memory/stores` |
| Member memory V2 | Browse, correct and bulk-edit private facts, rules and experiences; inspect recall and explicitly copy starting knowledge | Member drawer/editor → Manage memory; `/settings/overview?view=memory&store=<name>` | `pages/overview/MemberMemoryPanel.tsx`, `pages/overview/MemoryRecordsEditor.tsx`, `pages/overview/MemoryDocCard.tsx` | `handlers/memory_member.py`, `handlers/memory_edit.py` | `GET /api/memory/records`, `POST /api/memory/bulk/preview`, `POST /api/memory/bulk/apply`, `GET /api/memory/recall`, `POST /api/memory/seed` |
| Explore memory | Group a store's memories by facet and drill into one group | Global memory → Explore memory; member memory → Recovery → Advanced | `pages/overview/MemoryCarveCard.tsx` | `handlers/memory.py` | `GET /api/memory/carve` |
| Replaced experiences | List experiences excluded from recall after replacement, and restore one | Global memory → Replaced experiences; member memory → Recovery | `pages/overview/MemoryRetiredCard.tsx` | `handlers/memory_admin.py` | `GET /api/memory/retired`, `POST /api/memory/retired/restore` |
| Backups | Snapshot a store; stage or cancel a member restore for gateway restart | Memory browser → backups card | `pages/overview/MemoryBackupsCard.tsx` | `handlers/memory_admin.py` | `GET /api/memory/backups`, `POST /api/memory/backup`, `POST /api/memory/restore`, `POST /api/memory/restore/cancel` |
| Episodic search | Search past episodic memories | Memory browser → search | `pages/overview/MemoryTab.tsx` | `handlers/memory.py` | `GET /api/memory/episodic/search`, `GET /api/memory/episodic`, `DELETE /api/memory/episodic/{id}` |
| Embeddings | Enable the vector store, pick its model, and view localized setup errors, warnings, and deferred repair status | Memory browser → vector card | `pages/overview/VectorMemoryCard.tsx`, `pages/overview/EmbeddingModelCard.tsx`, `pages/overview/embeddingStatusText.ts` (shared status-text helper; no new entry point) | `handlers/memory.py` | `GET /api/memory/embedding-status`, `POST /api/memory/enable-embeddings`, `POST /api/memory/embedding-model` |
| Memory graph | Entity/relation visualizer over the memory store | `/developer?tab=memory` | `pages/overview/MemoryGraphTab.tsx` | `handlers/memory.py` | `GET /api/memory/graph`, `GET /api/memory/observability` |
| Usage | Token and turn usage over time | `/settings/overview?view=usage` | `pages/overview/UsageTab.tsx` | `handlers/usage.py`, `handlers/telemetry.py` | `GET /api/usage`, `GET /api/usage/kiro`, `GET /api/usage/turns` |
| Kiro credit balance | The top-bar credit pill and its account modal. The gateway refreshes the reading on a timer: the free `GetUsageLimits` API first, then the `kiro-cli /usage` scrape (a local slash command over the same free call) whenever the API returns no plan, parked for 6 h after three consecutive scrape attempts that failed (timed out, errored) or produced no parseable plan. The modal's **Refresh** button runs that same refresh now — under the state's notice when there is no reading ("Could not read your balance." for a failed read, "No balance reading is available for this account yet." for `none` — a dash the pill shows only while the selected harness is kiro-cli (`agent.acp_backend` names the kiro id, read positively by `api/acpBackend.ts`); on any other harness that payload hides the pill, as it always did), compact beside the meter when there is one — and writes the result into the pill's query. Single-flight (409 while a refresh runs, shown as an `ErrorNotice` with the hand-off like every other refresh outcome, Refresh held until the modal's ~3 s re-read lands and the notice clears); one notice at a time (a failed refresh replaces the standing failure notice with the same one sentence, the stale line yields to the stale-refresh notice); Refresh is held while the parked notice says when to try again; a parked scrape is reported after the API attempt (`skipped: scrape_parked`): no new reading is fetched, and the cache then holds a same-identity prior reading dimmed `stale` or an unavailable marker. A refresh that comes back `stale` is reported as such — an `ErrorNotice` with the hand-off — rather than stamped as checked now. Every credential read — the API call and the `/usage` scrape alike — is bracketed by a whoami immediately before and one immediately after, and its reading is published only when the pair PROVES one account (`_whoami_agreement` = `same`: at least one matching identity field, none disagreeing); `different` and `unproven` publish nothing (two identity-less whoamis around the API fall through to the scrape; around the scrape they end the refresh with no reading), and every failure path keeps a prior reading only for the same account (`_same_identity`), so an account switch never shows the previous account's balance. No config key, no Settings control, no cost copy | Top bar → credit pill → **Kiro Account** | `App.tsx`, `components/KiroAccountModal.tsx`, `api/kiroUsage.ts` | `handlers/sessions.py` | `GET /api/sessions/usage`, `POST /api/sessions/usage/refresh` |
| WakaTime coding activity and export | Coding stats for a named range plus project-grouped hours over a date range as a CSV or JSON download, for the productivity view and billable-hours invoicing | `/settings/overview?view=wakatime` | `pages/overview/WakaTimeTab.tsx` | `handlers/wakatime.py` | `GET /api/wakatime/stats`, `GET /api/wakatime/export` |
| Portability | Export and import the whole memory/config bundle | `/settings/imports` | `pages/overview/PortabilityTab.tsx` | `handlers/portability.py` | `GET /api/portability/export`, `POST /api/portability/import`, `POST /api/portability/preview` |

Private-memory boundary coverage lives in `test/test_member_memory_ownership.py`,
`test/test_member_memory_runtime.py`, `test/test_member_memory_algorithm.py`,
`test/test_member_memory_api.py` and `test/test_member_memory_backup.py`.
Pooled member authority is covered by `test/test_mcp_caller.py`,
`test/test_mcp_gateway_recaller.py` and `test/test_mcp_gateway_backend_coverage.py`:
kernel-bound per-call proof issuance, forged caller removal and concurrent
header isolation.
`website/src/test/MemberMemoryIdentity.test.tsx` covers automatic identity and
scheduled-member binding; `MemberMemoryPanel.test.tsx` covers scoped lifecycle
requests, server search, explicit copy limits and partial outcomes, structured
correction (including stable-identity experiences), full detail expansion from
three-line card previews, retired-memory pagination and live refresh, persisted
recovery staging/cancellation, unavailable-store retry, exact member avatars,
empty-member conversation links, and profile drafts across tab/store/browser
navigation and saves in flight.
`MemoryStorePicker.test.tsx` retains the global-versus-member read assertions.
`MemoryRecordsEditor.test.tsx` covers cross-page selection, stale-preview draft
refresh, proposal acceptance/retention, JSON scalar editing and recall query
changes. `website/playwright/member-memory.spec.ts` exercises the real isolated
gateway: member lifecycle, recovery staging, persisted conversations and both
V1/V2 65-record Email batches with concurrent edits and mobile previews.
It also verifies accepting and rejecting conflict proposals in each lineage.

## Schedules and loops

| Feature | What it is | Reach it | Page | Handler | Endpoints |
|---|---|---|---|---|---|
| Schedule | Cron jobs: recurring agent turns, scripts, commands | `/schedule` — rail **Schedule**; also created inline from the crew editor's "What wakes this crew" section (`/capabilities?tab=crews`) (`member_id` is set for a named member so the job runs on that member's private memory, and omitted for the DEFAULT crew, whose schedules stay on Global V1) | `pages/SchedulePage.tsx`, `components/CrewWakeSection.tsx`, `components/JobForm.tsx` (the form both hosts share) | `handlers/cron.py` | `GET,POST /api/crons`, `DELETE /api/crons/{job_id}`, `GET /api/crons/history` |
| Cron secret grants | Owner-approved vault-secret env grants for script crons: agent requests via `cron_secret_request`, the owner approves/denies/revokes on the job's Secrets panel | `/schedule` → job → **Secrets** | `pages/SchedulePage.tsx` (`JobSecretsPanel`) | `handlers/cron.py` | `PUT /api/crons/{job_id}/secrets` |
| Job tab filing into a chat folder | Per-job target folder in the chat sidebar: a persistent job's single chat tab is filed there, so the folder holds the job's timeline (delivery is unchanged; offered only for persistent agent jobs, and not while **Hide in chat** is on) | `/schedule` → job → **Chat folder** picker (create and edit) | `components/JobForm.tsx`, `utils/folderTree.ts` | `handlers/cron.py`, `cron_inject.py` | `POST /api/crons`, `PATCH /api/crons/{job_id}`, `GET /api/chat/folders` |
| Template update signal | A job created from a Schedule template records the template's id and prompt snapshot; the job detail panel shows a dismissible hint (naming the template) when that template's prompt has since changed | `/schedule` → job detail | `pages/SchedulePage.tsx` (`TemplateUpdatedNotice`), `utils/schedulePresets.tsx` (`templateUpdate`) | `handlers/cron.py` | `POST /api/crons` (accepts `source_preset`, `source_template_prompt`), `GET,POST /api/crons` |
| Monitor loops | Same-session bounded monitors and legacy nudge loops watching an external thing; a member's loop is also surfaced read-only in the Crew Members side panel's Work log tab. A prompt loop may additionally carry a **wake judge** (`judge` on `monitor_start` / `monitor_update`), and once the `nudge_evidence` scope is granted every GATED loop is screened on every tick whether or not its owner wrote one: a loop naming no criteria of its own runs under the default brief in `autonudge_judge.default_spec` (wake when the subject needs its owner -- a blocker, a question or ruling addressed to it, a terminal state, or the loop message's own exit condition; quiet when nothing new for the owner since the last tick), with the loop's instruction passed as the tick's context so that default reads the loop's own exit condition without restating it. An explicit brief overrides the default, and one naming only `targets` keeps them while taking the default's sentences. The default is never written back onto the loop, so the stored brief stays a record of what the owner asked for. Two bypasses that mean different things: a `gate=false` loop is never screened, because its duty is to act WHILE its subject is quiet, and `judge: false` (normalised to the reserved `validation.JUDGE_OFF_KEY` marker) is an owner who wants observation gating and no judge -- an empty object is neither, it only drops the owner's criteria. Ahead of each tick the `nudge.wake` decision point reads what the loop's targets produced since the last tick -- a watched session's new assistant rows through `session_control.read_messages`, which authorizes with `authorize_target` first, so a target the loop's owner could not read by hand is dropped and counted rather than fetched; and a watched pull request contributes NOTHING on any loop this judge screens, because the canonical observation its reader wants has one writer -- the structured controller's provider -- and a judged loop is by definition a GATED one, which observes through the raise-based kernel in `irq.poll`, whose verdict carries an outcome and prose and never canonical facts. So an owner's pull-request criterion is not evaluated on any build today, and the reader finds an empty canonical every time. What that absence MEANS is the part that decides the loop's cost, and it turns on whether anything read the subject: on the gated tick the judge is asked only AFTER the typed probe's own verdict came back quiet, so the subject WAS read, the target is not a drop, and the probe's free re-arm stands exactly as it does without a judge -- the tick stays quiet on the PROBE's verdict, which the judge neither reaches nor overrides. A factless reading with no probe behind it -- a loop carrying no monitor, a spent watch, a brief naming some other pull request -- is an unread target and fires. The reader and its renderer are wired and tested against the canonical shape and produce the summary below once a gated tick publishes one: `autonudge_judge.render_pr_summary` turns the twelve keys such a record carries (state, draft, mergeability, review decision, blocking review, unresolved thread count, per-bucket check tallies with the failed and pending lanes named, completeness flags, head revision) plus the PR-level comment-body FINGERPRINT when one is present into the prose a judge reads -- never comment body text, because the reader that sees those bodies reduces each to a fixed width and retains none; the rendering lives in the judge's collector because that is its only consumer, and the monitor's canonical shape is pinned by full-dict equality tests and hashed into the wake fingerprint. The collector is wired, rendered and tested against the canonical shape, and gains a producer when a gated tick publishes one -- then answers QUIET or fire, so a patrol whose every target was READ with nothing new on any of them answers QUIET and costs no turn, while a tick that could not read one of them, or that found evidence the scrub and the budget shed entirely, fires rather than reading as calm. What the judge WILL add over the probe on a pull request, once a gated tick publishes a canonical observation, is the OWNER'S up-to-500-character criterion evaluated against those typed facts, which no typed check can do. Targets default to the `chat-*` keys and pull request the loop's own message names, so a brief needs no second spelling of its subject. The judge never ENDS a loop: `finished` and `broken` wake with the verdict attached to the fired turn, and only a typed probe may terminate one, because a prose-minted terminal would let one hostile comment buy permanent silence. A QUIET verdict writes one scrubbed notice row on the owning session carrying probabilities and counts but never evidence text, and naming which brief the verdict was reached under -- `default` or `custom` -- because a quiet under the shipped default is not evidence that the owner's own sentence works; notice rows are themselves excluded from evidence, so a verdict can never feed itself. Consecutive QUIET verdicts are floored by `decisions.nudge_wake.quiet_streak_floor` (0, the default, means the probe path's own floor; the engine owns the ceiling and clamps on read, because `config.json` is agent-writable) so a judge wrong about a subject costs a late turn rather than silence. The lane is `decisions.nudge_wake.provider` -- `auto`, `jev`, or `llm` -- and the Jev lane additionally needs the Decisions consent switch plus the `nudge_evidence` scope, granted on the Decisions card; with the scope off an explicitly selected `jev` is what leaves a stored brief unread and the loop running as a plain timer, while `auto` falls to the LLM lane, which the provider key alone authorizes, and still judges | Chat composer → monitor popover; Crew Members → side panel → Work log → Auto patrol; agent-armed | `components/SessionAutomationPopover.tsx`, `components/AutoNudgePopover.tsx`, `components/autoNudgeLoop.ts`, `pages/members/MembersPage.tsx` (read-only), `pages/settings/DecisionsCard.tsx` + `pages/settings/decisionsPreview.ts` (the `nudge.wake` row and its scope switch) | `handlers/autonudge.py`, `autonudge_judge.py` (the collectors), `decisions/points/nudge_wake.py` (the questions, bounded state and mapping) | `GET,POST /api/monitors`, `PATCH /api/monitors/{id}`, `GET /api/monitors/slot/{slot_key}`, `POST /api/monitors/{id}/stop`, `POST /api/monitors/{id}/clear`, `POST /api/monitors/{id}/restart`, `GET,POST /api/autonudge`, `PATCH,DELETE /api/autonudge/{loop_id}`, `POST /api/autonudge/{loop_id}/fire`, `PUT /api/decisions/consent` (the `nudge_evidence` scope) |
| Session ledger | Durable per-session work state surviving compaction | Agent-written; no dashboard page | — | `handlers/session_ledger.py` | `GET /api/session-ledger`, `POST /api/session-ledger/record` |
| Debug reads | Five read-only questions about the running gateway, for an agent whose shell answers are unreliable: the sandbox mounts empty placeholders over the fenced paths, `/proc/<pid>/environ` is unreadable from it, and kiro-cli labels a policy refusal "User denied tool execution". `gateway` reports the pid, start time against HEAD's commit time, the imported package's code fingerprint, whether the MCP gateway daemon runs the same one, the systemd drop-in NAMES (never their contents, which can carry `Environment=`) and the recorder's health. `refusals` classes each refusal from the security event log as `sensitive_path_match`, `unverifiable_path`, `denied_rule` (with its refusal-diagnostic id), `governance` or `tool_policy_timeout` — the first two read identically today and call for opposite actions, so a budget timeout is marked `retryable` and says to retry the identical call. `threads`, `processes` and `snapshots` are backed by `kiro_crew.diag`: they answer from it wherever that module resolves, and answer HTTP 501 verbatim where it does not. The four host-wide views are the OWNER's dashboard tab alone, which is stricter than the crew-log door on purpose: a dispatch tree bounds whose conversation you may read, and the host is not inside anyone's dispatch tree. `refusals` is the one per-session view and takes the crew-log scope. Unattended, app-scoped, incognito, temporary and channel-linked or mirrored callers get no host-wide view, including an owner tab that is mirrored, since a mirrored tab republishes every turn. Environment exposure is four allow-listed keys; no message body is ever read. No write tool exists and the tool set is ratcheted to five | Agent-read via the `kirocrew-debug` MCP tools; no dashboard page | — | `handlers/debug.py` | `GET /api/debug/gateway`, `GET /api/debug/refusals`, `GET /api/debug/threads`, `GET /api/debug/processes`, `GET /api/debug/snapshots` |
| Session crew log | The session's own append-only history, read two ways: a page of entries in a seq range with up to `MAX_PAGE_REFS` citations resolved to a verdict and the rest reported in `refs_unresolved`, and one of five folds (`status`, `usage`, `timeline`, `tools`, `approvals`) with the seq it was folded through. The `ledger` and `work` folds are keyed by SLOT instead and join every session the slot ran under (the `work` fold also its bound workers' sessions), so the singular session route resolves the slot from the session's header before folding, the plural session route serves only the five session folds and omits them, and the unit-keyed route refuses them (`400 slot_projection`); they are kept out of the five a growth pushes, because a slot-wide value under one session's id would report a partial answer as the whole one. Owner-gated, because the log carries the session's redacted message bodies. A growth pushes a `session_projection` frame to owner sockets. Written only when `KIROCREW_CREW_LOG` is on. The folds are what the Crew log panel row above reads; the entry page has no dashboard surface, and a growth pushes a `session_projection` frame whose shape is specified so a client can follow | Owner reads; the folds are read by the Crew log panel, the entry page by API only | — | `handlers/crew_log.py` | `GET /api/sessions/{id}/crew-log`, `GET /api/sessions/{id}/crew-log/projection/{name}`, `GET /api/sessions/{id}/crew-log/projections` |
| Crew log agent reads | The same history a unit at a time, keyed by crew log UNIT rather than by session id, for an agent verifying or auditing the log its file tools are fenced from. Three read-only MCP tools on the assignable `kirocrew-crew-log` server: list the session logs (with the per-entry-type histogram on request), page one unit, read one of the five folds. `unit` accepts a raw unit id, a slot or session key the gateway resolves, or `self`. A session reads its own unit, the unit of any session it dispatched (transitively, keyed on the creator SLOT recorded on `session/opened`, so the tree survives a gateway restart giving the same tab a new session id), and any unit when it is the owner at a dashboard tab whose own caller class allows it; the listing carries the same scope as a filter. The caller classes `session_control.authorize_target` refuses -- unattended, app-scoped, incognito, channel-linked or mirrored -- keep their own unit and are refused another's, INCLUDING an owner's tab mirrored to a channel. A cross-session target is decided by one test over two inputs: its recorded class, folded over the log's whole life from the `class` object on `session/opened` plus every `session/class` move, held at the most restrictive value it ever carried and REFUSED when the log records none or records moves with no beginning, which is what keeps a CLOSED child decidable; and its live slot, which covers only the turn now in flight. A caller off the internal transport, one naming another component, and a session the gateway cannot name are refused outright. No write tool exists and the tool set is ratcheted | Agent-read via the `kirocrew-crew-log` MCP tools; no dashboard page | — | `handlers/crew_log.py` | `GET /api/crew-log/sessions`, `GET /api/crew-log/resolve?key=`, `GET /api/crew-log/units/{unit}/page`, `GET /api/crew-log/units/{unit}/projection/{name}` |
| Work ledger | The shared record between a conductor session and the worker sessions it dispatched: a worker reports schema-bounded status against its own item, and its conductor reads that record plus the acceptance batch it decides from. Used by `kirocrew-conductor` (and its deprecated `kirocrew-ledger-conductor` alias, the same spec under the flow's old name for one release) plus `kirocrew-worker` — `kirocrew-pipeline-conductor` and `kirocrew-security-conductor` deliberately do not mount `kirocrew-work`, because their children report through their own skills' scripts | Agent-written via the `kirocrew-work` MCP tools; no dashboard page yet (Phase 4 adds the Crew page item table) | — | `handlers/work_ledger.py` | `GET /api/work-ledger`, `POST /api/work-ledger/record`, `GET /api/work-ledger/brief`, `POST /api/work-ledger/report`, `POST /api/work-ledger/rebuild` |
| Session control | Create / fork (open a new session already carrying the caller's own transcript, or an authorized peer's, up to an optional message index; attribution lands in the child's birth save) / stop / send-to a session from outside it, plus MOVE one in the session tree: `adopt` records the calling session as a target's parent (a takeover, allowed even when the target already has one) and `release` returns a target to the top level. The caller is resolved from the connection and is never an argument, `adopt` refuses a cycle (`release` has none to refuse -- it only removes an edge), both refuse while the tree is unreadable or incomplete and while that slot's own earlier tree write is still in flight, and neither is auto-granted to an unattended conductor or reachable from a channel agent — they MUTATE where an existing session sits in the person's sidebar. `release` additionally admits the target ITSELF, so a session whose holder has stopped running is not stuck under it | Agent and app callers, not a UI | — | `session_control.py`, `chat_fork.py` (shared fork core) | `POST /api/session-control/create`, `.../fork`, `.../stop`, `.../send`, `.../adopt`, `.../release`, `GET .../read` |
| Agent chat egress | Post, edit and delete the bot's own chat messages from an agent turn. For POST and EDIT, a room target must be in the tracked-channel allowlist and a DM must be the owner's resolved DM, and outbound text and blocks pass the display-form redaction floor before reaching the provider. DELETE carries NO target authorization today: it validates only that `channel` and `ts` are present and forwards to the provider, so it reaches any message the bot token can delete (documented gap, not a described boundary) | Agent-written via the `kirocrew-core` messaging MCP tools; no dashboard page | — | `handlers/messaging.py` | `POST /api/send-message`, `POST /api/update-message`, `POST /api/delete-message` |

## Artifacts

| Feature | What it is | Reach it | Page | Handler | Endpoints |
|---|---|---|---|---|---|
| Artifact library | Saved widgets, HTML and documents, versioned; unsaved chat documents listed under "From your chats" open in a read-only preview, with the star to promote one into the library | `/artifacts` — rail **Artifacts** | `pages/ArtifactsPage.tsx`, `components/library/SessionDocPreview.tsx` | `handlers/artifacts.py`, `handlers/files.py` (preview read) | `GET,POST /api/artifacts`, `GET /api/artifact-folders`, `PATCH /api/artifacts/{slug}/folder`, `GET /api/file-read` (preview) |
| Artifact detail | View, edit, version history, companion chat | `/artifacts/:slug` | `pages/ArtifactDetailPage.tsx` | `handlers/artifacts.py` | `GET,PATCH /api/artifacts/{slug}`, `GET /api/artifacts/{slug}/versions`, `GET /api/artifacts/{slug}/events` |
| Artifact comments | Threaded, anchored comments on an artifact | Artifact detail → select text → comment | `pages/ArtifactDetailPage.tsx` | `handlers/artifacts.py` | `GET,POST /api/artifacts/{slug}/comments`, `PATCH,DELETE .../comments/{comment_id}` |
| Remote artifacts | Provider-hosted docs browsed and commented in place | `/artifacts/remote/:provider/:externalId` | `pages/RemoteArtifactDetailPage.tsx` | `handlers/artifacts.py` | `GET /api/remote-artifacts/{provider}/browse`, `GET .../{external_id}`, `GET .../comments` |
| Publishing | Push an artifact out to a configured provider, then re-check a recorded notice | Artifact detail → share menu, plus the publication banner's "Check again" | `pages/ArtifactDetailPage.tsx`, `components/PublishHub.tsx` | `handlers/artifacts.py` | `GET /api/artifacts/publish-providers`, `POST /api/artifacts/{slug}/publish`, `DELETE .../publish`, `POST .../publish/refresh`, `POST .../publish/reprobe-notice`, `PATCH /api/artifacts/{slug}/sharing` |
| Deploy | Ship a webapp artifact to a public URL on the user's AWS | `/deploy` (`/artifacts/deploy` redirects) | `pages/ArtifactDeployPage.tsx` | `src/kiro_crew/deploy/handlers.py` | `GET,PUT /api/deploy/config`, `GET,POST /api/deploy/profiles`, `POST /api/deploy/deploy` |

## Apps

Third-party and builtin apps that add their own pages, crons and MCP tools.

| Feature | What it is | Reach it | Page | Handler | Endpoints |
|---|---|---|---|---|---|
| Discover | Browse the app registries and install | `/apps` — **Apps** section header link | `pages/apps/DiscoverPage.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps/registry`, `POST /api/apps/registry/install`, `GET /api/apps/registries` |
| Updates | Apps with a newer version available | `/apps/-/updates` | `pages/apps/DiscoverPage.tsx`, `pages/apps/UpdatesList.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps`, `POST /api/apps/{name}/update` |
| Library | Installed apps as launchpad tiles, with rail pinning. Lists enabled apps by default; a labelled toggle ("Show N disabled") reveals the disabled ones too, and the choice is remembered per browser in `localStorage` (not synced across devices or origins) | `/apps/library` | `pages/apps/LibraryPage.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps`, `POST /api/apps/{name}/enable`, `POST /api/apps/{name}/disable` |
| App detail | One app's manifest, config, permissions, uninstall | `/apps/detail/:name` | `pages/AppDetailPage.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps/{name}`, `GET /api/apps/{name}/manifest`, `GET /api/apps/{name}/config` |
| Installed app page | An app's own UI, served by the app | `/apps/:name` | `pages/AppPage.tsx` | app-owned | `POST /api/apps/{name}/token`, `POST /api/apps/{name}/open` |
| App migration | Move an app's data after a packaging change | `/apps/migrate/:name` | `pages/MigrationPage.tsx` | `src/kiro_crew/apps/routes.py` | `DELETE /api/apps/{name}/migrate-cleanup` |
| Builtin app surfaces | Top-level routes builtin apps claim | `/<app>` via the `/:builtinApp/*` catch-all | `apps/builtinRegistry.ts` → per-app page | per-app `backend/routes.py` | `POST /api/apps/<app>/...` per app |
| Dev Fleet Make Live | Repoint the live gateway at a worktree (stage the cutover, cancel a staged one, restart into it) — runs in the gateway process because the pointer is bind-masked from the sandboxed backend; owner-only, every app token refused | `/dev-fleet` → row → **Make live** / **Cancel staged cutover** / **Restart** | `pages/DevFleetPage.tsx` (`devFleetApi.ts` `postGateway`) | `src/kiro_crew/apps/builtins/dev_fleet/gateway_routes.py`, `live.py`; `src/kiro_crew/service/live_target.py`; the backend reads pointer state back through `pointer_broker.py` | `POST /api/apps/dev-fleet/make-live`, `POST /api/apps/dev-fleet/restart-gateway`; backend-token-only `GET /api/apps/dev-fleet/live-target`, `POST/PUT/DELETE /api/apps/dev-fleet/live-target/removal-lease` |
| Dev Fleet Switch back | One safe return to the previous checkout after a completed Make Live cutover; the banner is absent while a cutover is only staged | `/dev-fleet` → completed-cutover banner → **Switch back** | `pages/DevFleetPage.tsx` | `src/kiro_crew/apps/builtins/dev_fleet/fleet_state.py`, `gateway_routes.py`, `live.py`; `src/kiro_crew/service/live_target.py` | `GET /apps/dev-fleet/api/fleet`, `POST /api/apps/dev-fleet/make-live` (`undo: true`) |
| App session controls | A compact per-chat control an app contributes to the composer, handed the active session's identity | Chip in the composer bar of any chat, when an enabled app declares `contributes.sessionControls` — no route of its own | `hooks/useSessionControls.ts`, `components/SessionControlHost.tsx`, `components/ChatInput.tsx` (chips), `pages/ChatPage.tsx` (wiring) | `src/kiro_crew/apps/manifest.py` (declaration + validation); the status route is app-owned | `GET /api/apps`, `GET /api/apps/{name}/{statusPath}` (in-gateway) or `GET /apps/{name}/api/{statusPath}` (process-backed) |
| App rail run state | The state of an app's own scheduled jobs on its rail icon — running, failed, or succeeded within the last 90s. Derived from the app's crons rather than pushed: the host already owns the in-flight marker, `last_status`, and the `created_by` owner stamp, so an app declares nothing new and cannot set its own mark. A user-paused job contributes nothing, which is the opt-out that works with the app's page closed | Mark on the row of an enabled installed app that OWNS a cron job — whether declared in `contributes.crons` or created at run time through `ctx.cron.add_job()`; no route of its own. Covers builtin and AppHost-routed apps alike: the row carries the app's resolved `name`, so a builtin's bare nav id is not parsed back | `appRunState.ts`, `App.tsx` (`RunStateIndicator` in `NavBadge`) | `src/kiro_crew/dashboard/handlers/cron.py` (`api_crons` serializes the owning `app` and `user_paused`); `src/kiro_crew/apps/cron_sdk.py` (`owner_tag` / `app_owner_name`) | `GET /api/crons` |

`apps/builtinRegistry.ts` is the path→component table for builtin surfaces
(23 entries: Worlds, Channels, Auto Improvement, Auto Research, AWS Control,
File Explorer, Code Review Sage, Workflows, Dev Fleet, Issue Radar, Meetings,
Papyrus, PPTX Maker, Ops Mission Control, Design Critique, Crew Companion,
Task Runner, MD Notebook, Mochi, Spec Builder, Personal Shopper, Design Tweak,
Project Scaffolder).
Adding a builtin surface means an entry there plus `ui.pages` in the manifest —
`App.tsx` needs no change, which is why the router-delta half of the freshness
check cannot see it and the pages-dir half can.

## Task runner and subagents

| Feature | What it is | Reach it | Page | Handler | Endpoints |
|---|---|---|---|---|---|
| Task Runner | Autonomous multi-step runs from a spec | `/projects` — **Apps** group rail row | `pages/ProjectsPage.tsx` | `handlers/taskrunner.py`, `handlers_project.py` | `GET,POST /api/projects`, `POST /api/taskrunner/plan`, `POST /api/taskrunner/from-chat` |
| Task detail | One run's steps, gates and approvals | `/projects` → a project row | `pages/ProjectDetailPage.tsx` | `handlers_project.py` | `GET /api/projects/{id}`, `GET /api/activities`, `GET,POST /api/comments` |
| Spec refinement | Interactive tightening of a task spec before running | Task Runner → refine | `pages/ProjectsPage.tsx` | `handlers/taskrunner.py` | `GET,POST /api/taskrunner/refine`, `POST /api/taskrunner/refine/answer` |
| Subagents | Background agent runs spawned from a session | Chat activity viewer; rail badge | `pages/chat/ActivityViewer.tsx` | `handlers/messaging.py` | `GET,POST /api/spawn`, `POST /api/spawn/stop-all`, `GET /api/spawn/{agent_id}`, `POST /api/spawn/{agent_id}/steer` |
| Worktrees | Create a git worktree for a follow-up session | Chat follow-up card → new worktree | `pages/ChatPage.tsx` | `handlers/worktree.py` | `POST /api/worktree/create` |

## Settings

`/settings/*` is a splat route; `pages/SettingsPage.tsx` parses the trailing
segments itself (`segment[0]` = tab, `segment[1]` = sub-nav). Every row below
is `/settings/<key>`. Panels live in `pages/settings/`.

| Tab | What it is | Panel | Handler | Endpoints |
|---|---|---|---|---|
| `overview` | Health hero, stat cards, memory and usage drill-ins | `OverviewPanel.tsx` → `pages/OverviewPage.tsx` | `handlers_system.py` | `GET /api/status`, `GET /api/system` |
| `imports` | Import config and history from another tool | `ImportPanel.tsx` | `handlers/onboarding_import.py`, `handlers/portability.py` | `GET /api/onboarding/import/scan`, `POST /api/onboarding/import/apply` |
| `chat` | Chat behavior preferences, plain vs highlighted diffs, selectable model visibility, model fallback pickers — rate-limit (`agent.fallback_model`) and content-filter (`agent.refusal_fallback_model`: a content-filter refusal is retried once on the configured model — `"auto"` follows the provider's recommendation — then the primary is restored; one attempt per message: the message is replayed verbatim when the turn ran no tool, and, when it did, the same harness session is continued from the completed work instead — the message is never re-sent after a tool ran, and the continuation tells the model to stop rather than start over if that work is not in view) | `ChatPanel.tsx` | `handlers/core.py`, `handlers/files.py`, `chat_runner.py` (refusal-fallback swap/replay-or-continue/restore) | `GET,PUT,PATCH /api/config/kirocrew`, `GET,PUT /api/dashboard/config` |
| `display` | Theme, density, language, terminal (font, default shell, command completion toggle) | `DisplayPanel.tsx` | `handlers/themes.py`, `handlers/core.py` | `GET,POST /api/themes`, `GET,PUT /api/config/theme`, `GET,PATCH /api/config/kirocrew` |
| `voice` | TTS voice and dictation engine | `VoicePanel.tsx`, `SttSettings.tsx` | `chat_voice.py`, `handlers/core.py` | `GET,PUT /api/voice/config`, `GET /api/voice/voices`, `GET /api/voice/system-voices`, `GET,PUT /api/config/stt`, `GET /api/stt/status`, `POST /api/stt/polish`, `POST /api/stt/prewarm`, `POST /api/stt/ffmpeg/download` |
| `notifications` | Which events notify, and on which channel | `NotificationsPanel.tsx` | `handlers/messaging.py` | `GET /api/notifications/channels`, `PUT /api/notifications/channels/settings` |
| `shortcuts` | Keyboard shortcut reference and overrides | `ShortcutsPanel.tsx` | `handlers/files.py` | `GET,PUT /api/dashboard/config` |
| `skills` | Skill enablement and context budget | `SkillsPanel.tsx` | `handlers/prompts.py`, `handlers/skill_budget.py` | `GET /api/skills`, `GET /api/skills/-/budget` |
| `channels` | Slack, Discord, Telegram, WhatsApp, Teams, and more | `ChannelsPanel.tsx` + one panel per provider | `handlers/messaging.py` | `GET,PUT /api/slack/config`, `GET,PUT /api/discord/config`, `GET /api/slack/manifest` |
| `channels` -> file existing sessions | Move a channel's already-existing unfiled sessions into its configured session folder. Reached from Settings -> Messaging Channels -> a provider panel, via "File existing sessions", which appears once a folder is saved | `settings/ChannelFolderBackfill.tsx` (shared by every channel panel) | `handlers/messaging.py`, `channel_slots.py` | `POST /api/channel-folders/backfill` |
| `browser` | Install playwright-cli, attach token, engine choice | `BrowserPanel.tsx` | `handlers/messaging.py` | `GET,POST /api/browser/install`, `PUT /api/browser/token`, `POST /api/browser/engine` |
| `computer-use` | Enable and scope native desktop automation | `ComputerUsePanel.tsx` | `handlers/computer_use.py` | `GET,PUT /api/computer-use/config`, `POST /api/computer-use/invoke` |
| `webhooks` | Inbound webhook tokens and contexts | `WebhooksPanel.tsx` | `handlers/hooks.py` | `GET /api/webhooks`, `POST /api/webhooks/tokens`, `POST /api/webhooks/test` |
| `instances` | Remote Kiro Crew instances to connect to or rename over SSH, SSM to an EC2 instance, or SSM to an AWS Fargate task (the `fargate` method: the card shows the task's chat API URL to copy and opens no dashboard), and the provisioning lanes that create them (built-in EC2 plus edition lanes from the `remote_provisioners` CPP seam) | `RemoteCrewPanel.tsx`, `InstancesPanel.tsx`, `InstanceFormFields.tsx`, `components/remoteProvisionerRenderers.tsx` | `handlers_instances.py`, `handlers_cloud.py` | `GET,POST /api/instances`, `PATCH /api/instances/{id}`, `POST /api/instances/{id}/connect`, `GET /api/cloud/provisioners`, `GET /api/cloud/preflight` |
| `privacy` | Telemetry disclosure and opt-out | `PrivacyPanel.tsx` | `handlers/telemetry.py` | `GET /api/telemetry/collection`, `GET /api/telemetry/beacon` |
| `security` | Denied commands, sensitive paths, approval posture | `SecurityPanel.tsx`, `PostureDisclosure.tsx` | `handlers/security.py`, `handlers/tailnet.py`, `handlers/file_delivery_consent.py` | `GET /api/security/denied-commands`, `PATCH .../builtins/{id}`, `POST .../user`, `GET,POST,DELETE /api/file-delivery/consent` |
| `connections` | OAuth apps for Connections providers that refuse dynamic client registration: per-provider redirect URI to copy, Client ID (config) and Client secret (vault), env-override notes, runbook links | `ConnectionsPanel.tsx` | `handlers/connections.py` | `GET /api/connections/oauth-clients`, `PUT,DELETE /api/connections/oauth-clients/{slug}` |
| `secrets` | Managed integration credentials plus other encrypted vault entries | `SecretsPanel.tsx` | `handlers/secrets.py` | `GET,POST /api/secrets`, `DELETE /api/secrets/{name}` |
| `developer` | Developer Mode gate, Feature Previews opt-ins, local-gateway switch. Four of the five previews are per-device client flags; **Decisions (Jev)** is not — its switch writes the gateway's keystone `decisions_consent.json` through the owner-only `GET,PUT /api/decisions/consent` (`handlers/decisions.py`), because the gate that reads it runs in the gateway, and because consent to send message text off the machine must not live in the agent-writable `config.json`. That gate now ADOPTS the answer at THREE points for the share of sessions `decisions.bucket` admits — `skills.select`; `message.steer`, which routes a message sent into a running turn to the steer path or the queue path (see the Auto (Jev) send while busy row above); and `model.route`, which puts one chat turn in a difficulty tier and runs it on the model `decisions.model_route` maps that tier to — and ANNOTATES a tool call at `tool.risk` for a sampled session that approves its own calls (see the Tool risk badge row above — it changes no permission). A seventh point, `nudge.wake`, is REGISTERED here and FIRES on the monitor tick: it decides whether a cycle that found nothing is worth a wake, and the tick that asks it ships in this build. What arms it differs by loop: one whose owner wrote a `judge` brief is screened on the lane's own authority, while a GATED loop that wrote none is screened only once the point's `nudge_evidence` scope is granted, under the built-in brief, so an install with neither gates no tick. It is the one point with TWO implementations: `decisions.nudge_wake.provider` chooses, and `llm` answers with a single text-only call on the `kirocrew-lite` agent instead of reaching the endpoint at all. That lane therefore rides no scope and takes no second consent — it opens no destination, because the evidence goes to the model provider the session's own turns already go to — while `jev` needs the main switch and the point's own `nudge_evidence` scope, which this build registers; `auto` prefers the armed Jev side and falls back to the small model. Like `memory.recall` it is SUBTRACTIVE: it may only omit a wake, never add one, and a judge that cannot answer wakes anyway. No adopted point is armed by the switch alone: skill selection also needs `skills.max_triggered` above zero, mid-turn handling is armed per send by the split send button's **Auto (Jev)** mode, and model routing is armed per session by the chat model picker's **Auto (Jev)** entry (`lib/jevRoute.ts`, drawn by ChatPage and each split ChatPane only while `decisions_enabled` AND the keystone consent are both an exact `true`), which stores the sentinel `auto:jev` — resolved to plain `auto` at the top of the slot-model handler and kept only as the slot's `jev_route` flag, so `slot.model` keeps meaning "a provider model id" for every reader of it. Routing to a dearer model spends more, which is why it happens only for a session whose owner picked the entry; any concrete pick clears it. That annotation sends a WIDER category than any of those three does, the tool's name and its arguments, so it takes a SECOND consent rather than riding the first: the keystone carries `tool_args` beside the history ceiling, absent reads as not consented so an already-consented install is inert for the point rather than retroactively signed up, and that switch lives in `tool.risk`'s own panel — the point it is the OK for — writing the same `PUT /api/decisions/consent` with `tool_args`, where an OMITTED field preserves the recorded scope so an ordinary flip of the main switch can neither grant nor erase it. `compaction.keep` sends a category wider again — a whole session transcript, the conversation text plus the INPUT of every tool call in it, with each tool RESULT replaced by its own character count so no output byte leaves — so it takes a THIRD scope on the same terms (`compaction` on the keystone, `consent.consented_compaction`, absent reads as not consented, `gate._scope_consented` refusing the point without it) and a THIRD switch, in `compaction.keep`'s own detail panel, which starts off even for an owner who already granted tool arguments: the narrower yes was reviewed as the arguments of the one call about to run, not as everything the session has run. `memory.recall` sends a category of its own — the id and the first 200 characters of each memory the assistant's own memory question recalled — so it takes a FOURTH scope on the same terms (`memory_text` on the keystone, `consent.consented_memory_text`, absent reads as not consented, `gate.POINT_SCOPE_KEYS` mapping the point to it and `gate._scope_consented` refusing the point without it) and a FOURTH switch, in `memory.recall`'s own detail panel. It is SUBTRACTIVE: the answer may only drop recalled memories, never add or reorder one, and an unreachable or slow answer returns every candidate unchanged. A recalled memory is not the text the main switch describes — that is text the owner just typed or that this build shipped, while a recalled memory is text the agent wrote down turns or days ago — which is why an install that granted any other scope is inert here. `nudge.wake` sends a category of its own — the transcript tail of a DIFFERENT session from the one its owner is looking at, plus a watched pull request's typed facts and a fixed-width fingerprint of its comment bodies, never their text — so it takes a FIFTH scope on the same terms (`nudge_evidence` on the keystone, `consent.consented_nudge_evidence`, absent reads as not consented, `gate.POINT_SCOPE_KEYS` mapping the point to it and `gate._scope_consented` refusing the point without it) and a FIFTH switch, in `nudge.wake`'s own detail panel. It is NOT implied by `compaction`, its nearest neighbour: that yes was reviewed as a transcript of the session its owner was present for, while this one carries another session's tail and a comment body written by a bot or a reviewer on a forge. The four scopes beside the switch move INDEPENDENTLY, each only when its own field is present, so one panel's switch can never disturb another point's. The card is a LIST of the points this build ships plus a PANEL for the one selected, the same list-and-detail shape the ACP backend tab uses. Each row carries the point's name, one line of what it decides, and a status chip — `On`, `Needs your OK` (a scope is missing), or `Off` — read from the SERVER's own projection of its points (`decisions/gate.py` through the consent GET, as `id`/`needs_scope`/`status`), never a list written into the frontend, so a point this build adds draws a row with no dashboard change; a gateway that projects none says so rather than rendering an empty list. A row's panel holds that point's own settings: its consent scope where it has one (with a line naming that switch as the OK the chip asked for), `model.route`'s three tier pickers (simple/medium/complex, options from the advertised-model list every other picker reads, defaulting to keeping the session's own model — no model id is written into the build), `nudge.wake`'s two pickers — which judge answers (`decisions.nudge_wake.provider`) and the model the small-model judge asks with (`decisions.nudge_wake.llm_model`, inheriting the judge agent's own unless it is set), neither gated on the consent switch because the small-model lane does not depend on it — the identifier the decision log records ("logged as `skills.select`"), and, on `skills.select` alone, one literal line naming the `config.json` path the card deliberately does NOT control (`skills.max_triggered`). Above the list, collapsed, sits what the whole seam shares APART from what must stay visible: the Jev API key, the share-of-sessions SLIDER, and the prior-conversation ceiling. The share is also STATED outside that block, in both switch states, because the shipped default of 100 means "every session" is what a reader agrees to and they have to see it before flipping the switch -- the slider is the control, the sentence is the fact. The main consent switch, the egress sentence and the address in force render ABOVE that block and outside it, never behind a closed disclosure. The address is read-only there, with a pointer to `decisions.provider.endpoint` — `PATCH /api/config/kirocrew` excludes `decisions.provider.*` so a dashboard caller cannot choose where collected state is sent). The key is written to the dashboard secrets vault as `TYPESAFE_API_KEY` through `handlers/secrets.py`, which hands no value back, so the field shows set or unset and never a credential; the share of sessions is a slider (`decisions.bucket`); and the prior-conversation ceiling is a number box, held while the switch is off because the keystone stores it as 0 there — the KEYSTONE's `history_budget_chars`, written through the same consent PUT with `enabled` omitted, because what it bounds is how much conversation leaves the machine and `config.json` is agent-writable; the config value of the same name is what the seam asks with and this ceiling is what clamps it. The main switch, the egress sentence and the address are outside that collapsed block for one reason: an egress consent, and the place egress goes, behind a closed disclosure is exactly what the backend tab's own rule forbids. A gateway with no consent route (404) — including a shadow-era one exposing only the retired `decisions.preview`, which is never read as consent — renders the switch disabled with the reason beside it rather than offering a write that would be refused. The whole card is additionally governed by `capabilities.decisions` (`decisions/capability.py`): a fleet that pins the row off gets NO card (`decisions_enabled` on the dashboard config read, fail-closed on absence), the consent PUT refuses to enable with `403 decisions_capability_denied`, and the gate reads an already-written keystone as not consented, so a machine consented before the pin is inert rather than carried over. `DecisionsCard.tsx` is that card, mounted by `FeaturePreviewsSection.tsx` and owning every control above; `decisionsPreview.ts` is the pure reader behind it — validates the consent payload, projects each point row, and resolves the tier map — no feature of its own. An adopted answer also leaves a RECORD on the reply it shaped: each point's outcome — for `skills.select` what word matching would have chosen, what Jev chose and whether they agreed; for `model.route` the tier, the model used and the model that would have been used — rides the assistant row as `meta.decisions_strip`, a LIST so two points deciding one reply both reach it (`decisions/outcomes.py` hands it from prompt assembly to the message finalizer in `chat_runner.py`), and one owner-only route sits beside the consent pair — `POST /api/decisions/feedback` records one verdict about one turn, appended to the decision day-file and answering `503` rather than claiming a verdict it could not write. There is deliberately no aggregate route: the day-files already carry `agree`, `p` and `tokens_saved` beside the verdict rows, so a window is a `jq` pass rather than a second reader of the same bytes. The strip and its verdict control are the surface that renders the record and calls the route (see the Decision strip row above), and the field's absence on an ordinary turn is what keeps them drawing nothing. Prior conversation is not part of the egress unless the operator consents to a `history_budget_chars` ceiling above 0 | `DeveloperPanel.tsx`, `FeaturePreviewsSection.tsx` (mounts the card), `pages/settings/DecisionsCard.tsx` (the Decisions (Jev) card: the point list, each point's panel, and the shared block), `pages/settings/decisionsPreview.ts` (its pure reader) | `handlers/decisions.py` (consent — including the point projection and the scope-only write — and feedback), `decisions/gate.py` (the points it projects, the status of each, and `POINT_SCOPE_KEYS`, the one map both the scope enforcement and the card's rows read), `decisions/impl_llm.py` (the judge's second lane: the one text-only call, and a parser as strict as the Jev wire reader), `decisions/points/compaction_keep.py` (the shadow point), `decisions/points/memory_recall.py` with `handlers/memory_member.py` (the recalled-memory narrowing and its receipt), `decisions/points/nudge_wake.py` with `autonudge_judge.py` (the wake point's questions, bounded state and mapping, and the collectors that fill that state), `decisions/consent.py` (the keystone writer, which holds the switch, the address, the history ceiling and each scope), `decisions/capability.py` (governance probe), `handlers/files.py` (`api_dashboard_config`, which serves `decisions_enabled`), `handlers/core.py` (the shared config route, for the bucket and the tier map), `handlers/secrets.py` (the vault the API key is written to), `decisions/outcomes.py`, `chat_runner.py` (the record on the finalized row) | `GET,PUT /api/decisions/consent` (the switch, each point's scope -- `tool_args`, `compaction`, `memory_text` and `nudge_evidence` -- the history ceiling, and the point list the overview draws), `POST /api/decisions/feedback`, `GET /api/dashboard/config` (`decisions_enabled`), `GET,PATCH /api/config/kirocrew` (the bucket, the tier map and the judge's provider and model; `decisions.provider.*` is excluded, so the address and the key are not written here), `GET,POST /api/secrets` and `DELETE /api/secrets/{name}` (the API key, set and unset only) |
| `releases` | Release channel, update check, changelog | `ReleasesPanel.tsx` | `handlers/updates.py` | `GET /api/update/check`, `POST /api/update`, `GET /api/changelog`, `GET /api/releases` |
| `about` | Version, build, diagnostics bundle, and the in-app update step-up: an agent (or the panel) ARMS an update, a human approves it. On a managed-venv install the approval is the printed host command; on a packaged desktop install the gateway holds only a nonce-free request (version, who, when) and the approval is the human's own Install click in this panel, which drives the desktop updater's existing download→install IPC — there is no gateway endpoint that installs | `AboutPanel.tsx` (`InAppUpdateFlow`, `AgentUpdateRequestCard`), `ReportProblemCard.tsx` | `handlers/diagnostics.py`, `handlers/feedback.py`, `handlers/updates.py`, `platform/update_stepup.py`, `platform/app_update_request.py` | `POST /api/diagnostics/collect`, `GET /api/diagnostics/download/{filename}`, `POST,GET,DELETE /api/update/arm`, `POST /api/update/approve` |
| — (cross-cutting) | Host-side backup of the browser-held settings the tabs above write, so they survive a moved dashboard port or a relocated Electron `userData`. Restores on a profile that has never reached the host; allowlist-scoped (`DURABLE_PREF_KEYS`), not every browser key | — (no panel; `lib/uiPrefs.ts` is the client) | `handlers/ui_prefs.py` | `GET,PUT /api/ui-prefs` |

Flagged-file delivery consent (`GET,POST,DELETE /api/file-delivery/consent`,
plus `GET /api/file-delivery/consent/arm` and the host-only
`POST /api/file-delivery/consent/approve`) is listed on the `security` row
because that is the tab it belongs to, and the panel wired to it is that tab's
`Flagged-file delivery` section (#8793). Stated rather than implied: the backend
control landed first so the delivery gates could read it, and the panel followed.
The panel is a VIEW over the grant -- it reads and withdraws through the
owner-gated endpoints, and it does not record a grant directly. Recording takes a
two-step human-only step-up (issue #7770): the owner-gated POST only ARMS a
request (writing a single-use nonce), and the grant is recorded by the
loopback-only `/approve` endpoint whose authority is host-locality plus
possession of that nonce, NOT an owner session -- so an owner-authenticated but
agent-DRIVEN browser cannot self-grant. Two things fence the agent from the
nonce: it lives in its own leaf (`file-delivery-consent-pending/`) that is
bind-MASKED from the agent sandbox, so a prompt-injected agent cannot read it or
forge one there with a runtime-constructed shell path; and the
`self-protection-file-delivery` denied-command floor rule blocks the agent from
running `kirocrew file-delivery approve` at all (the same floor that makes
`kirocrew update`'s step-up agent-proof). The nonce is deliberately NOT under
`trust/`, which is sandbox-VISIBLE for SEL appends and so would leave the file
forgeable.

Recording a grant therefore has a HOST requirement, stated here because the
refusal is otherwise met as a 403 after the terminal round-trip: the owner needs
a terminal on the machine running the gateway, and that machine must be one where
Crew applies its own hidden-dir mask to the agent spawn. Two hosts do not qualify
and approval refuses on them by design -- native Windows (no Crew sandbox backend,
so the spawn is delegated) and macOS with kiro-cli's internal sandbox enabled
(a backend exists, but the spawn is handed to kiro-cli and Crew's mask never
runs). Computer use enabled, a sandbox mode whose backend carries no mask, and a
live agent session that is already running unconfined each refuse for the same
reason: the step-up's whole claim is that a human acted while the agent could not
read the nonce. An owner whose only access is a remote or tailnet dashboard with
no terminal on the gateway host cannot complete the step-up at all; withdrawing
consent stays owner-gated and needs no terminal.

Instances is deliberately not a rail row: it is set up here once and switched
from the header tab strip. Webhooks carries both a preview flag and
`hiddenFromNav`, so it surfaces as this Settings tab rather than a rail row.

The UI-preference backup row carries no tab because it has no panel and nothing
for a user to reach: it is the mechanism behind the tabs above, listed so its
handler has an owner in this map. Its allowlist is explicit, so a preference
absent from `DURABLE_PREF_KEYS` is still lost on an origin change, and a value
that gates a safety confirmation (`mc-yolo-ack`) is deliberately excluded. The
contract is in
[../system-specs/modules/config.md](../system-specs/modules/config.md).

## Developer

`/developer` (`pages/DeveloperPage.tsx`), ten `?tab=` values. Internals views;
not where a user manages their own data. The former `feature-previews` tab moved
to Settings > Developer (`pages/settings/FeaturePreviewsSection.tsx`); the old
`?tab=feature-previews` link redirects there.

| Tab | What it is | Page | Handler | Endpoints |
|---|---|---|---|---|
| `logs` | Live gateway log stream and level control | `pages/LogsPage.tsx` (`LogViewer`) | `handlers/updates.py` | `GET /api/logs`, `GET,POST /api/logs/level` |
| `system` | Host runtime, services, sessions, performance; the Services plane's **Tasks & capacity** card (task queue depth, effective concurrency per lane, degrade reason, waits and recovery); the Sessions plane's per-session resource table, which nests a task under the session that spawned it and a session under the session that opened it (creator read from each session's crew log; a footer stat says when the store holds more session logs than the scan admits) | `pages/SystemPage.tsx`, `pages/system/` (`TasksCapacityCard.tsx`, `SessionsTab.tsx`, `sessionRows.ts`) | `handlers_system.py`, `handlers/session_storage.py`, `handlers/tasks.py`, `handlers/spawn_resume.py`, `handlers/sessions.py` + `session_memory.py` (sampler; lineage from `src/kiro_crew/crew_log/session_tree.py`) | `GET /api/system`, `GET /api/system/session-storage`, `GET /api/sessions/health`, `GET /api/sessions/memory`, `GET /api/spawn/lanes`, `GET /api/spawn/adaptive`, `GET /api/spawn/{agent_id}/resume`, `GET /api/tasks`, `GET /api/tasks/summary`, `GET /api/tasks/{task_id}`, `POST /api/tasks/{task_id}` (`answer_input`, `cancel_wait`), `POST /api/tasks/{task_id}/cancel` |
| `telemetry` | Startup timings and context traces | `pages/TelemetryPanel.tsx` | `handlers/telemetry.py` | `GET /api/telemetry/startup`, `GET /api/telemetry/context-trace` |
| `storage` | Raw localStorage inspector | `pages/LocalStorageDebug.tsx` | — (client only) | — |
| `mcp-pool` | MCP connection pool state | `pages/settings/McpManagement.tsx` | `handlers/mcp.py` | `GET /api/mcp/active`, `GET /api/mcp/scopes`, `POST /api/mcp/probe` |
| `memory` | Memory graph visualizer | `pages/overview/MemoryGraphTab.tsx` | `handlers/memory.py` | `GET /api/memory/graph` |
| `config` | Raw Kiro Crew and agent config editors | `pages/overview/KiroCrewCfgTab.tsx`, `AgentCfgTab.tsx` | `handlers/core.py`, `handlers/agents.py` | `GET,PUT,PATCH /api/config/kirocrew`, `GET,PUT /api/agent/config` |
| `agent-backend` | Which agent harness backend is live, as a **list and a detail**: one row per known harness -- except one this deployment's policy denies, which is hidden rather than shown unselectable, because a denial is not the operator's to fix -- carrying a leading **in-use** dot, its name, and a trailing **readiness** glyph (ready / dependency missing / re-check needed / not offered / not checked) -- two independent marks, so a harness that is both the active backend and missing its binary reads as both -- and the **capability card** for the highlighted row only -- available / not-available capability lines, the always-visible tool-approval and security lines, and one always-visible where-it-lives line -- whose secret store this agent signs in against, the only such note left on the card because it is the only one that is also the reader's risk -- all projected from capability membership by `agent_sdk/backend_cards.py` -- plus the card's **MCP half**, narrowed to what switching COSTS the reader: the tool-off rule where it can take a whole server, its exception directly under it where the harness has one, and the agent-config settings that will not take effect here as one group under a heading that names the harness. Not the projection kind, the transports, or the where-it-lives notes that name a route Crew takes — those are `kirocrew doctor`'s and `providers/mirrors/README.md`'s the tool-off cost stated OUTSIDE that disclosure beside the security lines when it withholds the whole server, and behind the disclosure the parts of the agent config file this harness withholds or has no channel for -- all projected from the mirror declarations by `agent_sdk/backend_mcp_ability.py`. Highlighting a row never switches the backend; the single **Use \<name\>** button does. A status strip states install state and, for a missing harness, the components by name -- plus a copyable install command, with a Copy button, only when one is known for that harness -- and a **Check again** button (`POST`, owner-gated, audited) that drops this gateway's cached resolution for that one harness and re-probes, so a fresh install needs no restart; the clear runs on the event loop and only the probe is offloaded, because spawn-path readers have no `await` between their check and their read. A harness this build does not offer gets a row and no Use button. Hosts the **Kiro sign-in** card (see Standalone operator surfaces) while KAS is an offered backend | `pages/developer/AgentBackendTab.tsx`, `pages/developer/KiroSignInCard.tsx` | `handlers/acp_backend_status.py`, `handlers/kiro_prerequisite.py`, `agent_sdk/backend_cards.py`, `agent_sdk/backend_mcp_ability.py`, `agent_sdk/backend_install.py` | `GET /api/acp-backends`, `POST /api/acp-backends/recheck`, `GET /api/kiro-prerequisite` |
| `debug-tools` | Diagnostic overlays, currently the chat scroll inspector | `pages/developer/DebugToolsTab.tsx` | — (client only) | — |
| `archive` | Consolidated session archive browser | `pages/SessionArchive.tsx` | `handlers/sessions.py` | `GET /api/session/archive`, `GET /api/session/archive/{name}` |

## Standalone operator surfaces

| Feature | What it is | Reach it | Page | Handler | Endpoints |
|---|---|---|---|---|---|
| Logs | Full-page log viewer | `/logs` | `pages/LogsPage.tsx` | `handlers/updates.py` | `GET /api/logs`, `GET /api/stream` |
| Hooks | Full-page hook manager | `/hooks` | `pages/HooksPage.tsx` | `handlers/hooks.py` | `GET,POST /api/hooks`, `GET /api/kiro-hooks` |
| Webhooks | Inbound webhook tokens, contexts, run history | `/webhooks` (preview-gated) | `pages/WebhooksPage.tsx` | `handlers/hooks.py` | `GET /api/webhooks`, `POST /api/webhooks/tokens`, `POST /api/hooks/agent` |
| Cloud launch | Provision a remote Kiro Crew instance through a provisioner lane: the built-in lane creates an EC2 instance in the user's own AWS account; an edition may add lanes via the `remote_provisioners` CPP seam, and the tab shows a lane selector only when more than one drawable lane exists (`registerRemoteProvisionerRenderer`). The built-in EC2 form also names the Kiro identity the crew signs in as (Builder ID or an Identity Center portal + region), preselected from the launching computer's own sign-in and carried through the launch job, the engine's start and resume, and every wizard sign-in step; a lane drawn by a registered renderer owns its own form, and a launch that sends no `login_target` signs in as Builder ID | Settings → Remote Crew → Set up a new one | `pages/settings/RemoteCrewPanel.tsx`, `components/remoteProvisionerRenderers.tsx` | `handlers_cloud.py`, `cloud/login_target.py`, `cloud/login.py`, `platform/defaults.py` (`DefaultRemoteProvisionerProvider`) | `GET /api/cloud/provisioners`, `GET /api/cloud/preflight`, `POST /api/cloud/launch` (`provider_id`, `login_target`), `GET /api/cloud/identity`, `GET /api/cloud/iam-policy` |
| Crew sign-in recovery | The state a managed launch can end in with a working, billing crew and no Kiro session on it: the device code timed out, nobody approved it, the gateway restarted mid-wait, or the owner cancelled. The crew is badged **Needs sign-in**, Connect is held back as a disabled **Connect after sign-in**, the launch footer stops claiming the instance is ready, and the sign-in step shows a waiting key and the held connect step a warn-tinted hollow circle rather than a green check. The pending device code is offered from the crew's own row, copyable, naming the identity it approves; a fresh one can be started, which re-runs ONLY the sign-in step against the existing crew and reuses the job's stored `login_target`, so a company-SSO crew is not retried through a Builder ID prompt. Cancelling stops the remote login without signing the box out, while an unconfirmed sign-in deliberately keeps its login alive so a preserved code stays finishable | Settings → Remote Crew → Your crews (the crew's own row), and the launch progress card | `pages/settings/RemoteCrewPanel.tsx` | `handlers_cloud.py`, `cloud/launch_job.py` (`run_signin_retry`), `cloud/launch_engine.py`, `cloud/login.py` (`cancel_device_login`) | `POST /api/cloud/launch/{id}/signin` (fetch the pending prompt), `POST /api/cloud/launch/{id}/signin/restart` (owner-only; never re-provisions) |
| Mobile connect | Pair a phone to this gateway | Left rail → **Connect your phone** (governed methods + QR); Settings → Security → mobile card (sign-in link only) | `App.tsx`, `components/MobileConnectModal.tsx`, `pages/settings/MobileLoginCard.tsx` | `handlers/mobile_connect.py`, `handlers/auth_mobile.py`, `handlers/tailnet_mobile.py` | `GET /api/mobile-connect/methods`, `POST /api/auth/mobile-link`, `POST /api/tailnet/mobile/qr` |
| Kiro sign-in | Sign in to the Kiro identity agents run as under the KAS backend, without a kiro-cli login: Google/GitHub via loopback (local) or device code (remote), Builder ID and company SSO via device code; signed-in summary with sign-out; explicit "sign-in expired" state (no silent fallback to kiro-cli login) | Developer → Agent Backend → **Kiro sign-in** card, under the backend switch and only while KAS is an offered backend (Developer Mode preview, so deliberately not on Settings → Overview and not in Settings search; Overview shows only a one-line **Kiro sign-in moved to Developer > Agent Backend** signpost while KAS is the selected backend); chat error row "not signed in" → **Sign in to Kiro** deep link (`/developer?tab=agent-backend&highlight=key:kiro-sign-in`, which rings the card). The full-screen `KasLoginGate` form is still not mounted at the app root | `pages/developer/KiroSignInCard.tsx`, `pages/developer/kiroSignInLink.ts`, `pages/developer/AgentBackendTab.tsx`, `pages/OverviewPage.tsx` (`KiroSignInMovedPointer`), `components/KasLoginGate.tsx`, `pages/chat/ErrorCard.tsx` | `handlers/kas_login.py`, `auth/service.py` (`status()`), `auth/refresh.py` (`RefreshRejected`) | `GET /api/kas-login`, `POST /api/kas-login/device`, `POST /api/kas-login/loopback`, `POST /api/kas-login/poll`, `POST /api/kas-login/cancel`, `POST /api/kas-login/logout` |
| Source-provider review | PR state, checks and review threads in the Changes panel | Chat right panel → **Changes** | `components/PullRequestPanel.tsx`, `components/CommentThreads.tsx` | `handlers/source_providers.py` | `POST /api/source/pull-request`, `.../checks`, `.../status`, `.../resolve` |
| Crash report notice | Desktop-only banner on the launch after a crash: a count of new own-app crash artifacts, **Show diagnostics** (reveals the crash ledger in the OS file manager) and dismiss | Appears automatically on the next launch after a desktop crash — no navigation | `components/CrashReportNotice.tsx` (mounted app-wide in `App.tsx`) | `website/electron/crash-collector.js`, `website/electron/ipc-registrar.js`, `website/electron/preload.js` (`crashReportsAPI`) | IPC, not HTTP: `crash-reports:get`, `crash-reports:reveal` |
| Startup feature video | One short clip introducing a new feature, shown once on the launch after it ships and retired permanently by a `seen` or `dismissed` verdict; yields the whole launch to release notes, an update popup or first-run onboarding, and is skipped in incognito/temporary sessions. A clip is always played from disk -- the release folder for a downloaded one, the shipped assets for a built-in one -- and never streamed from the CDN: the browser only plays bytes the gateway downloaded and sha256-checked first, so an uncached hosted clip waits for the next launch. Every offer's files were proved on disk by the request that offered it, so the dialog opens on the `/next` answer alone | Appears by itself at startup, with no navigation, on the first launch where nothing else claimed the screen and the catalog still has an entry this install has not used | `components/StartupVideoModal.tsx`, `components/startupVideoGate.ts` (sequencing policy), mounted app-wide in `App.tsx` | `feature_videos.py`, `feature_videos_manifest.py` (signed hosted catalog), `feature_videos_cache.py` (local clip cache + serving) | `GET /api/feature-videos/next`, `POST /api/feature-videos/feedback`, `GET /feature-videos/<release>/<file>` |
| Meet CrewMates | Four-step first-run flow in the split-screen chapter chrome for a workspace with no crewmates and no custom agents: what a crewmate is (three examples), name + Built from, job + when + where it reports, then "<Name> is ready"; Create posts the crewmate (job text stored as its description) and a schedule bound to it. Fires once after the other first-run chapters, only while the Crew Members preview (`PREVIEW_CREW`) is on; the Crewmates page re-opens it when the roster holds only the built-in default row | Appears by itself after the Customize tour; `/members` roster → **Meet CrewMates** when no crewmate exists | `components/MeetCrewmatesFlow.tsx`, `hooks/useMeetCrewmatesGate.ts`, mounted in `App.tsx` inside `OnboardingShellHost` | `handlers/core.py` (`dashboard.crewmates_onboarded` via the theme endpoints), `handlers/agents.py`, `handlers/cron.py` | `GET /api/theme/boot`, `PUT /api/config/theme`, `POST /api/agents`, `POST /api/crons` |
| Feature-video cache readout | How much of the current release's clips is on disk, which one is downloading, and one control to fetch them all now. Read-only apart from that control; the control is absent when policy forbids downloads, and the whole row is absent on a gateway whose `status` predates the cache fields | `/settings/chat`, in the Messages card beside Feature Tips | `pages/settings/ChatPanel.tsx` | `feature_videos.py`, `feature_videos_cache.py` | `GET /api/feature-videos/status`, `POST /api/feature-videos/fetch-all` |
| OpenAI-compatible API | Chat-completions shim for external clients | External clients only | — | `openai_compat.py` | `POST /v1/chat/completions` |

## Popouts and embeds

Separate top-level route trees `App.tsx` selects before the dashboard chrome
renders, so nothing in them carries the sidebar.

| Route | What it is | Page |
|---|---|---|
| `/popout/chat/:slug?` | One session in its own OS window | `pages/PopoutFrame.tsx` |
| `/popout/artifact/:slug` | One artifact in its own window | `pages/ArtifactPopoutFrame.tsx` |
| `/popout/terminal` | The docked terminal, detached | `pages/TerminalPopoutFrame.tsx` |
| `/embed/chat/:slug?` | Chat embedded in a host surface | `pages/ChatPage.tsx` (`embedded`) |
| `/embed/sessions` | Session list embedded in a host surface | `pages/ChatPage.tsx` (`embedded`) |
| `/embed/settings` | Reduced settings for an embedded host | `pages/EmbedSettingsPage.tsx` |

## Redirects

Kept so old bookmarks and deep links still resolve. Adding a feature never
means adding a row here; retiring one usually does.

| From | To |
|---|---|
| `/knowledge` | `/capabilities?tab=knowledge` |
| `/agents`, `/mc-agents` | `/capabilities` |
| `/connections` | `/capabilities?tab=mcp` |
| `/overview` | `/settings/overview` |
| `/instances` | `/settings/instances` |
| `/artifacts/deploy` | `/deploy` |
| `/orchestrated/:slug?` | `/chat/:slug?` (orchestrator slot) |
| `/tasks` | Task Runner |
| anything unmatched | `/chat` |
