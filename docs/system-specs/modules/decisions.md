# Jev Decision Seam

Owners: `src/kiro_crew/decisions/`, `src/kiro_crew/context.py`, `src/kiro_crew/config/sections.py`, `src/kiro_crew/dashboard/handlers/decisions.py`, the strip hand-off, the tool-card annotation hook and the model-routing hook in `src/kiro_crew/dashboard/chat_runner.py`, the `message.steer` call site in `src/kiro_crew/dashboard/chat_handlers.py` with the two stamps in `src/kiro_crew/dashboard/chat_delivery.py`, the `Auto (Jev)` picker entry in `website/src/lib/jevRoute.ts`, and the Decisions card in `website/src/pages/settings/DecisionsCard.tsx` (mounted by `FeaturePreviewsSection.tsx`, read by `pages/settings/decisionsPreview.ts`).

## 1. Scope

The foundation sends typed questions to Jev and validates its answers. Six business integrations ship and only four of them decide anything. `skills.select`: an enabled, sampled session can use Jev to select one eligible skill, or no skill, instead of the word-overlap result. `message.steer` (section 10): a message the dashboard owner sends into a RUNNING turn is routed to the steer path or the queue path by Jev instead of by the sender. `model.route` (section 11): an enabled, sampled session whose owner picked `Auto (Jev)` in the chat model picker can use Jev to put one chat turn in a difficulty tier and run that turn on the model the tier maps to. `memory.recall` (section 13): the memories vector similarity recalled for an owner dashboard turn are narrowed to the subset Jev judges worth their place in the prompt. `tool.risk` (section 9) decides nothing -- it only ANNOTATES a tool call the permission policy has already answered. `compaction.keep` (section 12) decides nothing either, and not in the same way: it MEASURES, at every automatic compaction, which of the session's tool calls would have been worth keeping, and the compaction runs byte-identically whatever the answer is, so there is no arming choice for it anywhere -- only its own consent scope. Each integration's refusal is the shipped behaviour and never a third outcome. No deciding point is reached by consent alone -- each needs a second, owner-made choice (a non-zero `skills.max_triggered`, the send button's `Auto (Jev)` entry, the picker entry, the recalled-memory switch), so consenting to the seam arms nothing by itself. The two non-deciding points need no such choice, because there is no behaviour for one to arm. Cron delivery and skill deduplication remain unchanged. There is no shadow mode, LLM comparison implementation, calibration report, or decisions report command: the word-overlap result IS computed on every sampled turn and recorded beside Jev's, but the arm that is injected is always Jev's, so the second arm is an observation rather than a mode.

## 2. Configuration and sampling

Consent is the KEYSTONE leaf `~/.kiro/crew/decisions_consent.json` (`config.loader.decisions_consent_path()`, module `decisions.consent`), holding `{"enabled": bool, "endpoint": str}`; it defaults to absent, which reads as not consented, and only a literal boolean true consents. Consent is bound to a destination: `provider.endpoint` is in the agent-writable `config.json` too, so an enabling PUT must echo the endpoint the owner reviewed (`endpoint` in the body, the `configured_endpoint` the GET showed) and is refused with `409 decisions_consent_endpoint_changed` when the config names a different one -- the GET-to-PUT window is operator-paced while the agent runs, so binding to a fresh server-side read would consent to an address the owner never saw; on a match the PUT records it (`decisions.consent.save_enabled(enabled, endpoint=...)`, `gate.configured_endpoint()` supplying the same default `impl_jev` uses), and the gate sends only while `consent.permits(configured_endpoint)` holds: enabled is true AND the recorded endpoint equals the configured one. A keystone with the flag but no recorded endpoint permits nothing. A later edit of `provider.endpoint` is therefore a refusal, warned once per address in the application log, until the owner turns the switch off and on again; the GET returns `endpoint`, `configured_endpoint` and `permits` so the card shows the address consent is for and says when the two disagree. Disabling clears the recorded endpoint. The `dashboard/handlers/decisions.py` module imports the `decisions` package inside its handlers, not at module top, so the optional seam stays off the gateway boot path (`no-new-work-on-gateway-boot-path`; pinned by `test_decisions_consent.py::test_the_handler_module_keeps_the_seam_off_the_boot_path`). It is deliberately not a `config.json` field: that file is writable by an auto-approved agent shell and the live config watcher applies it without a restart, so a switch there could be flipped by a prompt-injected agent to start sending its own conversation off the machine. The leaf is on `security._CREW_SECRET_LEAVES` (agent file tools refuse it), `sandbox._CREW_READONLY_LEAVES` (mounted read-only in every sandbox) and the absent-ceiling pre-create list, the same placement as `computer_use.json` and `aws_service_consent.json`; its only writer is the owner-only, browser-called `PUT /api/decisions/consent` (`dashboard/handlers/decisions.py`), which refuses app tokens and non-owner users on the read as well as the write, audits every grant and revocation to SEL, and never overwrites a corrupt file. `DecisionsConfig` carries no `enabled` field and `PATCH /api/config/kirocrew` refuses `decisions.enabled`; an `enabled` key an operator or agent writes into `config.json` under any spelling is inert. `decisions.model_route` is the `{tier: model_id}` map `model.route` reads (section 11). All three tiers are present and **every one defaults to `""` -- inherit**, meaning the turn keeps the model its session is already on. No model id is named in code, and none may be: a hardcoded id fails at runtime -- silently, until the first prompt -- for every account not entitled to it, so `docs/system-specs/common/model-selection.md` keeps code defaults at `""`/`"auto"` and admits pins only in an operator-written map, and `code-review.yml` gates on it. Each value goes through `normalize_agent_model`, exactly as `coerce_role_models` does, so `"auto"` and a non-string both collapse to `""` and "inherit" has ONE spelling. A tier outside the three is dropped -- the question never offers it, so it could never be answered. The three keys stay PRESENT rather than being omitted, because "this tier is unpinned" is a state the log and the strip report (section 11), so it needs a spelling of its own instead of being inferred from a missing key. Unreadable reads as every tier unpinned, which applies nothing: the fail-closed direction for a value that decides what a turn costs. It is read through `gate.model_route_map()` so the point never imports the config loader. Each tier IS writable through `PATCH /api/config/kirocrew` -- `decisions.model_route.simple` / `.medium` / `.complex`, registered from `DECISION_MODEL_ROUTE_TIERS` itself so the gate and the answer domain cannot drift, and validated on exactly the terms the `agent.role_models.*` pins beside them are: the model-id grammar, `max_len` 64, and `_validate_role_model`, which refuses an id the active provider or the account's entitlement cannot honour. A tier value grants nothing on its own, which is what separates it from `provider.*`: the point validates it against what the provider advertises and keeps the session's model when it is absent, so the worst a write does is decide what a turn costs. `decisions.provider.endpoint` and `provider.api_key` stay OUT of the editable set on their own argument -- the endpoint would let a dashboard caller choose where collected state is sent, and `api_key` is schema-`sensitive` so the masked GET hands back a sentinel a PATCH offered beside it would clobber -- so the card SHOWS the address and names the path rather than offering a control whose every save is refused. `decisions.bucket` is an integer percentage from 0 to 100; an absent key is the default of 100, an out-of-range number is clamped, and a value that does not parse as a whole number reads as 0 (nobody sampled), never as everybody. `decisions.history_budget_chars` bounds the PRIOR CONVERSATION one request may carry beside the message, and its default is **2000**, about the last two or three turns, because a request like "do the B comparison drawer" is unanswerable without the turn that says what "B" names. The number is what the seam ASKS for and widens nothing by itself: consent is recorded against the text the owner reviewed, which names the message excerpt and the candidate descriptions and no prior turn, so an install whose owner reviewed no ceiling sends the message alone. The ceiling is the owner's choice, and the config alone cannot make it: the CEILING is `history_budget_chars` on the keystone (`consent.STATE_KEY_HISTORY_BUDGET`, `consent.consented_history_budget`), written only by the owner-only PUT, and `gate.history_budget_chars()` sends the SMALLER of the two. `config.json` is agent-writable, so a budget recorded only there could be raised by the very agent whose conversation would then be sent -- the same argument that put `endpoint` on the keystone. Only a whole non-negative number reads as a ceiling; absent, a bool, a string and a float all read as 0, so every consent recorded before the ceiling existed keeps meaning "no prior turns", and an unreadable keystone reads as 0 as well. A zero config value short-circuits before the keystone is read, so an owner who lowers the budget to 0 costs no extra IO on the turn path. It is floored at 0, so a negative number cannot read as unbounded, and an unparsable value reads as the shipped 2000 rather than 0 -- the one place this key parts from `bucket`, which reads an unparsable percentage as nobody sampled. It cannot fail open here, because the gate holds the configured number against the keystone ceiling, so an unreadable value still sends at most what the owner reviewed and an install whose owner reviewed no ceiling sends no prior turns at all. It is read through `gate.history_budget_chars()`, so a point never imports the config loader onto its own hot path, and it is not writable through `PATCH /api/config/kirocrew` (the route's editable set names `decisions.bucket` and the three `decisions.model_route` tiers, and nothing else under `decisions`). The number the CARD offers is therefore the keystone CEILING, written through `PUT /api/decisions/consent` with `history_budget_chars` and `enabled` omitted -- the seam's ask lives in the agent-writable file and the ceiling is what clamps it, so the control an owner is given has to be the one an agent cannot move. There is no per-request MENU budget: `MAX_CANDIDATES` (100) x (`MAX_KEY_CHARS` 120 + `MAX_DESCRIPTION_CHARS` 200) already caps a menu at 32,000 chars, so a budget below that could only split a request that was never too large, and one above it could never fire. Sampling uses the session key's SHA-256 digest, so a session remains in the same group rather than drawing again on each message. Zero samples none; 100 samples every otherwise eligible session.

The provider configuration contains endpoint, API-key reference, model and `timeout_ms`. A default timeout is 1000 milliseconds. The sampling percentage and provider use the existing live config snapshot; consent is one small keystone read per decision, performed off the event loop (`asyncio.to_thread` in `decide`, the caller's executor thread in `is_enabled`).

Legacy `preview`, `points`, `arm`, `shadow`, and `live` values do not enable this behavior. Enabling an observation-only configuration must never silently authorize business decisions. The keystone consent is the one explicit opt-in.

## 3. Core contract

`DECISION_POINT_NAMES` is the CLOSED tuple `("skills.select", "tool.risk", "message.steer", "model.route", "compaction.keep", "memory.recall")`: a name outside it is refused, so a point added without being listed there refuses every call rather than borrowing the section's switch. `decide(point, state, questions, session_key=...)` returns validated answers or `None`. Disabled, unknown-point, unsampled, scrubbed, timed-out, transport-error and invalid-answer paths return `None`. Cancellation propagates so a caller can abandon a pending request. `is_enabled` provides the same cheap preflight before a caller builds its candidate menu; `decide` checks the gates again.

Jev is the sole provider implementation, and `Choice` is the only question type. Requests use its documented wire format with bounded response reads, no retry and no redirect following (`allow_redirects=False`; a 3xx is refused as a provider error, since consent is bound to the endpoint asked and a followed redirect would replay the body elsewhere). A reply is accepted only when each answer's type is `choice`, its `choice` is a string, and the probability for that choice is a finite number in 0..1 (a boolean is not a number); the gate then checks identifiers and options again before anything is consumed. Provider failures do not fabricate a selection. The response mapping is tested against a loopback server; those tests do not prove a real TypeSafe account round-trip.

Before transmission, the request -- the state, the questions and the provider model id together, rendered as the wire will render them -- is scanned with the existing credential patterns, canonical credential redactor and exfiltration-URL detector. A match or scanner failure refuses the whole request. No partially scrubbed request is sent. Every agent-writable provider field that can reach the wire is bounded: `provider.model` must match `types.MODEL_ID_RE` (letters, digits, `.`, `_`, `-`, at most 64 characters) or the request is refused as `scrubbed:provider-model`, checked in the gate before the scan and again in `impl_jev._to_wire`; `provider.endpoint` is bound by consent (above); and `provider.api_key` is never itself a credential -- only the reference `secret://TYPESAFE_API_KEY` (`impl_jev.VAULT_SECRET_NAME`) resolves, from the dashboard secrets vault the agent cannot read, so a literal key written into `config.json` is refused with a warning naming the fix and a reference to any other vault entry resolves to no key and never touches the vault. Where the vault key goes is decided by consent, not by the endpoint being the default: an owner who consented to a proxy address gets the vault key sent there and nowhere else. Configuration is not an independent authorization boundary: an actor allowed to change it can enable requests only to the consented endpoint, with the operator's own model id and key.

## 4. Skill selection

The normal word-overlap result remains the fallback. The Jev candidate menu uses skills available through the existing loader and its project-confinement rules. The explicit no-skill option is `/no skill applies`: a key's first component is a directory name, so no skill can carry that name, and a skill literally called `none` or `(no skill applies)` stays selectable. Always-loaded skills are not candidates; negative triggers and `skills.max_triggered` remain effective. A zero trigger cap means no automatic selection and no Jev request. Skill identifiers are preserved, not shortened into different names.

A validated choice replaces the triggered list before the existing body/pointer split. The explicit no-skill answer produces an empty list; `None` means retain the original list. Custom-agent and minimal-context paths do not acquire automatic skill selection.

The state carries the message (truncated to 2000 chars), the candidate menu, and -- only inside the consented ceiling, which is 0 until the owner records one -- the conversation so far as `history`: earlier `user` and `assistant` rows of the SAME session, walked newest-first until the budget is spent with the last entry admitted clipped to fit. The READ is bounded independently at `MAX_HISTORY_MESSAGES` (20) rows so a long conversation cannot turn one selection into a whole-file parse, which means a budget far above a few thousand characters is not the binding constraint. Prior turns are inside `state`, which is what `gate._scan_text` renders, so a credential or exfiltration URL in an earlier turn refuses the whole request exactly as one in the message does (pinned by `test_decisions_points.py::test_a_secret_in_a_prior_turn_refuses_the_whole_request`); history carried beside the state would be egress the scrub never sees. `history` is OMITTED when there is none, so an install with no consented ceiling sends exactly the shape a message-only request has -- a wire change for every consented owner who never opts in is not worth a distinction the log already records. "No prior turns were reachable" is told apart from "this build does not send them" by `history_chars` on the call row, not by an empty list on the wire. The budget is also read BEFORE the transcript, so at 0 the source is never called and a sampled turn pays nothing for a read it would discard. Tool output is never included -- the restriction is applied at the READ (`ConversationLog.recent(roles={"user", "assistant"})`), not filtered afterwards -- and a row whose text equals the current message is skipped, because the turn's own user message may already be flushed. The source is a CALLABLE passed from `build_message` and invoked by the point only after its own gate check, so an unsampled turn pays for no transcript read; `exclude_last_n` is forwarded as every other reader of that log receives it, and a source that raises reads as no history rather than as a failed selection.

## 5. Operational log

The basic JSONL log records call metadata: timestamp, point, hashed session, latency, scrubbed boolean, bounded answer data and an error category. Raw messages, conversation history, candidate descriptions, API keys and provider exception text do not belong in rows.

A point may add its own fields through `log.build_row(extra=...)`, merged FLAT beside those seven and bounded exactly as an answer value is (`_MAX_EXTRA_KEYS`, `_MAX_EXTRA_ITEMS`, `_MAX_VALUE_CHARS`); a key naming one of the seven is dropped, so an extra can only ever add a field and never rewrite `session`, `scrubbed` or `ts`. `decide(extra=...)` writes it onto whichever row that call produces, including a timeout, scrub or provider-error row, and it is never sent -- it takes no part in `_scan_text` because it never reaches the provider. The three cheap refusals still write nothing, extra included.

`skills.select` therefore emits, per sampled turn: one CALL row for the question asked (`turn_id`, `candidates`, `history_chars`, `truncated`), written by the gate; and, only where a usable answer arrived, one OUTCOME row written by the point, carrying `baseline` (the word-overlap keys), `jev` (the keys actually injected, cap applied), `agree` (SET equality, so an order difference is not a disagreement), `p`, `tokens_saved`, the same counts and the shared `turn_id`. `tokens_saved` is an ESTIMATE: injected-body characters of the baseline minus those of the pick, divided by `CHARS_PER_TOKEN` (4), measured only over the symmetric difference because a skill both arms chose cancels -- so agreement costs no body read at all -- and counting bodies means a pointer-only skill is overstated. A refused turn writes no outcome row: `agree` against an answer that never arrived would compare one arm with nothing, and the gate's own row already carries the error category. The baseline arm is computed from the SAME tree walk that builds the menu (`candidates_from_loader(baseline_out=...)`, the same `MIN_TRIGGER_OVERLAP`, the same score order with ties in walk order, the same `skills.max_triggered` cut), so the comparison costs no second enumeration and cannot drift from the matcher's own result.

After writing the row -- and only when `append` reported that it WROTE, since a strip whose durable row was refused describes a decision no verdict could be filed against -- the point calls `publish(session_key, row)` resolved by name from `kiro_crew.decisions.outcomes` inside `try`/`except ImportError`; a build without that module, a module without that attribute, and a publisher that raises are all no-ops that cannot cost the turn its answer. The published dict IS the row that was written, so the dashboard and the log cannot become two descriptions of one turn.

`model.route` emits, per routed turn: one CALL row written by the gate (`turn_id`, `history_chars`, `truncated`); and one OUTCOME row written by the point where a tier arrived AND its model could be applied, carrying `tier`, `p`, `model_chosen`, `baseline_model` (the model the turn would otherwise have used) and the shared `turn_id`, with the elapsed time in the core `latency_ms` field rather than in `extra`. A tier that arrived and could NOT be applied writes a row with an `error` category instead and publishes nothing: `model-not-advertised` (the tier maps to an id this account cannot run), `no-switch-seam` (the provider cannot express a per-turn model at all) or `switch-failed` (the provider refused it). Each is written rather than dropped because the gate's own call row already says the decision succeeded, so without one the log would report a healthy seam while every turn kept the old model. Disabled and unsampled paths do not write logs. A candidate menu that cannot be built -- the loader walk raised, or every entry it yielded was unreadable -- writes one row with error `candidates-failed`, so a loader change that breaks enumeration is visible rather than indistinguishable from an unsampled session; an empty skill tree writes nothing. The log DIRECTORY is sealed alongside the keystone: `decisions` is in `sandbox._CREW_READONLY_LEAVES`, `_CREW_PRECREATE_READONLY_DIR_LEAVES` and `_CREW_NOFOLLOW_READONLY_DIR_LEAVES` -- the last because a resolving symlink is followed by the mount, which would seal a planted target while leaving the lexical name replaceable and the seal satisfied by a link the governed party chose, because sealing the grant while leaving the record writable is half a control -- an agent that cannot enable the seam could still append a `kind="feedback"` row in the shape `build_feedback_row` produces, and that row would be read as a verdict nobody gave. That argument needs BOTH enforcement points, so `~/.kiro/crew/decisions` is also on `security._WRITE_PROTECTED_HOME_PATHS`: the sandbox mount covers a shell and is absent on a host without the OS sandbox, while the agent's file-edit tool is present on every host -- the same input-to-an-authorization-decision placement `rotation.yaml`, the OMC index and the session store take. It is WRITE-protected rather than sensitive, because reading the rows is the point of recording them, and `platform_log_append` opens the file directly without passing that gate. Read stays open and a directory bind shows live contents, so a day-file written later is visible; every legitimate writer is the gateway, outside the sandbox. Rows are appended through `platform_log_append.append_line`, a confined, locked, full-write append (see `docs/system-specs/common/platform-compat.md`) that works on POSIX and Windows alike; a link at the directory or the leaf, or a file that is not a regular single-link file, refuses the write. Each day-file is capped at `MAX_FILE_BYTES` (8 MiB): a row that would cross the ceiling is refused inside the lock before any byte is written, and the first refusal per file is logged at WARNING. Day-files older than `RETENTION_DAYS` (14) are removed by the first append of each UTC day; only regular files carrying this module's own file-name pattern are candidates, and the directory is listed and unlinked through the same pinned, no-follow descriptor the append uses (`platform_log_append.pinned_log_dir`), so a directory link swapped in under the log directory's name cannot redirect the deletion. Log failure cannot turn a valid decision into an application exception. `append` RETURNS whether the row was written, and exactly one caller reads it: the verdict route (section 6), because that row is a person's answer rather than an observation. Every other caller ignores the value by design -- a decision must not fail because its row did. JSON serialization keeps control characters encoded as data; this feature has no terminal report renderer. Logging is best effort, not proof that a selection reached the assembled context.

## 6. The decision strip on the message

A decision is made during prompt assembly, on a worker thread, before the reply it shapes exists; the strip has to ride that reply's message record. `decisions.outcomes` is the only connection between the two: `publish(session_key, outcome)` stores at most one outcome per POINT per session and `consume(session_key)` POPS the list. Per point, because two points decide the same turn -- `skills.select` during prompt assembly and `model.route` before the prompt is sent -- and both describe the SAME reply, so one slot per session would make the second publish erase the first. A second publish for the same point still replaces, since there is one such decision per turn. The registry is in-process memory, not a file: producer and consumer are the same turn in the same process, and the durable record is the log in section 5. It is bounded four ways because a published outcome is not guaranteed a reader -- one entry per (session, point), at most `MAX_POINTS_PER_SESSION` (8) points per session evicting oldest-published-first, a 600-second TTL, and a 1000-session ceiling evicting oldest-published-first -- so a cancelled turn leaks nothing and a caller publishing under an invented point name bounds itself. The TTL ages the whole session entry from its newest publish, because the reply the list rides on is one event. `publish` runs on the executor thread and `consume` on the event loop, so both hold one lock over dict operations only; neither raises, because the producer is inside a turn and the consumer is inside the message finalizer.

`consume` is destructive by design. The strip describes ONE turn, so leaving the entry in place would attach the same decision to every later reply in the session and read as a decision made on that turn.

The read happens at `chat_runner._decisions_strip_meta(slot)`, passed as `meta=` to the `slot.append("assistant", ...)` call at both finalizers -- the normal `_flush_segment` and the recovery `_persist_partial_reply` a turn that dies mid-reply goes through. Passed AT append time, not written onto the row afterwards: `slot.append` broadcasts the live `chat_message` frame from inside the call, so a later write would persist but miss the frame the open tab renders. The field is `meta.decisions_strip`, carrying the published dicts unchanged as a LIST -- always a list, even for the one-decision turn that is the common case, because a shape that held one would have to drop the second or change shape between turns, and a reader that must branch on "object or array" is a reader two producers can disagree about. The frontend reader still accepts a bare object, so a row written by an earlier release reads unchanged. `meta` is the carrier because it is the part of a row that already travels every door -- `_build_message_entry_uncached` persists it, the restore path reads it back, `chat_message_frame` puts it on the WS push, and `append`'s own `mid` minting merges into it. A session with no decision gets no `meta` key at all: absent, never null.

The dashboard reads that field and only that field: the strip renderer and the verdict control are section 8, the model row is section 11, and the field's absence on an ordinary turn is what keeps them drawing nothing. One strip is drawn per record, keyed by the record's own `point`. `PUT /api/decisions/consent` additionally accepts an optional `history_budget_chars` (a whole non-negative number, refused rather than coerced), records it on enable and clears it on disable so a re-enable cannot inherit an unreviewed budget, audits it beside the endpoint, and the GET reports it so the card can show what was consented to rather than what the config currently asks for. An OMITTED field preserves the recorded ceiling rather than defaulting to 0: the card writes `enabled` and `endpoint` alone, so a default would let an ordinary switch flip erase a ceiling recorded through this same route, and the two facts are independent -- the switch says whether the seam may send, the ceiling says how much prior conversation it may carry, and each moves only when its own field is present. The route hands `consent.KEEP_HISTORY_BUDGET` down rather than resolving the ceiling itself, and `save_enabled` resolves that sentinel inside its own read-modify-write: a caller that read the ceiling first would hold a value read before a concurrent PUT lowered it and write that number back, raising an egress limit by losing a race. Resolving it there is necessary and not sufficient, so that read-modify-write is HELD under one process-wide lock: the handler runs `save_enabled` on a thread, two owner PUTs therefore interleave, and without the lock the enable PUT still writes back the ceiling the lowering PUT had already reduced. The lock spans the read and the write together, so the lower of two concurrent ceilings survives whichever order they took, and it covers `enabled` and `endpoint` on the same terms. A distinct object is what tells "not mentioned" from a chosen `0`, since no number can mean both. An explicit `0` still clears the ceiling, which is how prior turns are taken back without turning the seam off, and the SEL row reports the ceiling actually written. `POST /api/decisions/feedback` takes `{"turn_id": str, "verdict": "right"|"wrong"|null, "side": "jev"|"baseline"}` and appends exactly one `kind="feedback"` row through `log.append`, off the event loop. It answers `503 decisions_feedback_not_recorded` when the append did not land: the day-file ceiling is shared with the turn's own rows, so on a busy sampled day a `200` would report a verdict that was never written, and the SEL row says `denied` for it rather than `allowed`. A null verdict is a real value meaning a verdict taken back. Nothing is coerced: a missing or blank `turn_id`, an ABSENT `verdict` key, a verdict outside the pair, or a side outside the pair is `400 decisions_feedback_invalid_body` and writes nothing, because such a row makes the fold wrong rather than incomplete. Presence is tested apart from value because `null` is itself a verdict here -- the one that takes an earlier one back -- so a body that dropped the field would otherwise append a retract nobody sent. It is the line `KEEP_HISTORY_BUDGET` draws one route over: no in-band value can also mean "not asked". A verdict is never merged into the decision row it judges -- the append is constant-cost by construction, and an edit would lose both when the verdict was given and the fact that someone changed their mind.

A decision row carries no `kind`, so readers tell the two apart by `kind` being absent; that is the only test that also holds for files already written. There is NO aggregate ROUTE: the rows already carry `agree`, `p` and `tokens_saved` beside the verdict rows, so a window is a `jq` pass over `~/.kiro/crew/decisions/*.jsonl` and a served endpoint would be a second reader of the same bytes with its own failure modes -- every crash the review lanes found in this module was in that fold (a non-finite number, an unhashable verdict, a record cap taken from a shared default), and none of them could exist in a file the gateway only appends to. When a surface needs the numbers it can fold them where it renders them. The turn count is `agree + disagree`, the rows carrying a boolean `agree`, and never the row count: one sampled `skills.select` turn writes a call row plus an outcome row (section 5). The feedback route is dashboard-owner only, like the consent pair in the same module, because it writes the log an operator reads.

There is no `GET /api/decisions/strip`. The message carries the record, so a second fetch could only disagree with it.

## 7. Governance

The keystone is the OWNER's consent; `capabilities.decisions` is the FLEET's, and the two answer different questions -- may my messages be sent, versus may this machine run the seam at all. An owner on a managed laptop can consent in good faith to a paid external endpoint their fleet never approved, so the ceiling stands above the keystone rather than beside it. The row is a `SCOPE_CATALOG` capability with `capability_default=True` (data only -- no `CONTRACT_VERSION` or evaluator change, mirroring `capabilities.social_share`), so naming no row leaves the standalone user exactly where they were. `decisions/capability.py::is_decisions_denied(surface_key)` resolves it through `vet_and_audit`, and every layer's denial is honoured, because this is a per-request question rather than a process-wide startup one. The surface key is what a profile binds on, and the two kinds of caller supply it differently. The dashboard callers -- the config route and the consent PUT -- leave it at the `dashboard:ui` default, because on an HTTP request the equivalent `X-Session-Key` header is caller-controlled and honouring it would let a request carrying `slack:x` dodge a profile bound to the dashboard surface. The gate passes the turn's own session key, which is runtime state rather than caller input: it is the same value `in_bucket` hashes and the decision log records, and without it a profile bound to a non-dashboard surface would never be consulted on the one path that actually sends. A caller with no session names no surface, so the default applies and every layer above the surface still binds. It fails CLOSED: a wrong-DENY falls back to the shipped word-overlap rule that every unconsented install already runs, a wrong-PERMIT sends message excerpts to a paid third party on a fleet that forbade it. Every evaluation leaves a `governance_decision` SEL row (tool `dashboard_config_decisions`), grant and denial alike.

Two chokepoints, because either alone is a half-control. `PUT /api/decisions/consent` refuses an ENABLING write with `403 decisions_capability_denied` and writes nothing, so a denial is visible where an owner would flip the switch; a DISABLING write still succeeds, because the gate already reads the seam as off and refusing it would trap an owner with a stale `"enabled": true` keystone they cannot clear. And `gate._consented_for` -- the one keystone read every `decide` and `is_enabled` path funnels through -- reports NOT consented under a denial, so a keystone written before the pin is inert rather than carried over; without that half a fleet could pin the row and still send from every machine consented earlier. The ceiling is consulted AFTER the keystone read and inside the same off-loop hop: an install without consent costs nothing (no governance evaluation, no SEL row, no second `await`), and past that line consent is on, which is exactly when a fleet denial is a fact an auditor needs recorded. The warning is said once per process, not once per message.

`GET /api/decisions/consent` folds a denial into `permits`, so no caller reads "a decision would be sent" under a pin; the denial is not reported as a field of its own, because nothing read one and the surface a caller acts on is the 403 on an enabling write. And `GET /api/dashboard/config` reports `decisions_enabled` beside `social_share_enabled` -- read-only, dropped from the `PUT` body like the other governance-derived fields, since governance and not config owns the answer. Both are presentation: the Feature Previews card is not drawn at all unless `decisions_enabled === true` (absent fails closed), which is a card withheld rather than a disabled one, because a ceiling is not a state the user can act on. The Security panel's ceiling viewer needs no i18n key for the row: the humanising fallback renders the `decisions` leaf as "Decisions".

A policy pins it off MACHINE-WIDE with one object -- every surface, every path. A profile bound to a surface withdraws only turns on that surface, so stopping the seam everywhere means the policy, or a profile per sending surface:

```json
{"version": 1, "capabilities": {"decisions": {"enabled": false}}}
```

### 7.1 Settings and validation

The Decisions card reads and writes the keystone through `GET`/`PUT /api/decisions/consent`, not localStorage and not the config route, and reads the sampling share from the config GET. It prints the configured endpoint ("Sent to") beside the switch so consent is to an address, and a body-weight notice when consent stands for a different address than the config now names. It carries no `configKey`, because it writes no config path; `test_settingref_schema_fixture.py` pins that `decisions.enabled` is absent from the schema and the card. The backend editable-field validation accepts the bounded integer bucket only; sensitive provider credentials remain masked in GET output. The card prints the sampling share in both switch states, phrased as a fact about the on state, because the shipped default of 100 means every session and consent must see that before the switch is flipped. A backend exposing only legacy `preview` is unsupported, not an enabled gateway; that state fades the card, including the egress note, and names Settings › Releases as the fix.

Tests cover strict opt-in and legacy non-migration; sampling; the history budget, its shipped default, its fail-closed coercion, and the keystone ceiling that caps it (including an agent-raised config reaching 0); the omitted `history` key and the transcript read the default budget skips; the sealed log directory, its directory-not-file pre-creation and its no-follow membership; the outcome that is not published when its row was refused; Jev success, malformed replies and failure; credential refusal; log shape, the bounded flat `extra` merge and its core-field protection; real PATCH/GET behavior; actual skill-selection consumption and fallback from an executor thread; the prior-turn walk (order, clipping, role restriction, current-message skip, unsampled turns paying nothing) against a real `ConversationLog`; the single question at a full menu, the retired split vocabulary and the deadline; the refused verdict (503, audited as denied) and the writer's return value; both arms with `agree`, `tokens_saved` and the outcome row; the publish hook present, absent and raising; negative triggers and cap limits; frontend field support, writes, refused writes and generated key parity. These are local/loopback tests. Live provider verification requires a real key and separate consent to send test data.

## 8. The decision strip's record and feedback

The transcript's receipts for a turn's decisions travel as `decisions_strip` on the assistant row that ends it: a LIST of records, told apart by each record's own `point` (`readDecisionRecords` / `readDecisionRecord`). A bare object is read as a list of one, so a row written before the second point existed still renders. The `skills.select` record is below; the `model.route` one is section 11. The receipt for one `skills.select` decision is the shape this section pins. `meta` is the carrier on both doors -- the live websocket frame and a row reloaded from history (section 6) -- and the reader also accepts a top-level `decisions_strip`, so a producer that stamps it there is read too. Absent means the turn was not decided by this seam, and the strip draws nothing.

This block IS the contract between the gateway that stamps the record and `website/src/pages/chat/decisionRecord.ts`, which reads it. The reader's fail-safe is to draw nothing, which is byte-identical to a healthy release that stamps no record at all -- so a field renamed on one side would make the strip silently vanish rather than fail. `website/src/test/decisionStripContract.test.ts` parses the fenced record below out of this file and asserts the reader accepts it, so a change here without the matching reader change is a red test on the frontend side.

```json
{
  "turn_id": "turn-4f2a9c",
  "point": "skills.select",
  "baseline": ["brazil", "crux-code-reviews"],
  "jev": ["brazil"],
  "p": 0.81,
  "tokens_saved": 3240,
  "candidates": 42,
  "message_chars": 96,
  "history_chars": 1840,
  "latency_ms": 197,
  "dropped": [{ "key": "tst", "p": 0.12 }],
  "error": null
}
```

`baseline` is what the word-overlap rule would have loaded and `jev` is the validated choice, both as skill keys. Agreement is NOT a field: the strip derives it from the two lists, because a flag that disagreed with the names beside it could only put a check mark over a real divergence, so promising one would invite a producer to stamp a value whose only use is to be wrong. A timestamp is not a field either -- the assistant row the record rides on carries its own. `p` is the probability for the chosen option, a finite number in 0..1 or `null`; anything else prints no number. `tokens_saved`, `candidates`, `history_chars` and `latency_ms` are whole counts, and a negative or unparsable one reads as 0. `message_chars` and `history_chars` are the two halves of the question's egress, each measured on the string that was actually sent -- `message_chars` after the excerpt cap, so it is what left and not what the owner typed.

`message_chars` is the ONE count whose absence is not 0. A turn that reached the selector had text, so zero characters is not a credible measurement of it, and a record that does not state the field is a record whose producer did not measure the excerpt -- not one reporting that nothing left. The reader therefore reads an absent or unparsable `message_chars` as "not stated" and the strip DRAWS NO egress row at all, the same way it draws no latency row without a `latency_ms`. `history_chars` keeps the floor-to-zero rule in full, because 0 there is a real observation: it is how "no history was reachable" is distinguished from a field that was never sent (section 4). `latency_ms` is one of the six core log fields rather than a field of the point's own, and it rides to the strip because the record IS the log row. The strip prints it as `Jev latency` on the expanded card and inside the collapsed parenthetical beside the score. `dropped` names answers the gate refused with the score each arrived with. `error` is the failure category when the decision failed, `null` otherwise. Nothing here carries message text, candidate descriptions or a provider key -- the same bound section 5 puts on the log; `message_chars` is a length, and a length is not the text.

A count that is the same on every turn is not on the record. There is no batch count -- the menu is asked in ONE question (section 4) -- and no history-clip count: clipping is a fact about the transcript READ, which the call row carries, and a strip row for it printed 0 on every turn at the shipped history budget of 0 while saying nothing a reader could act on.

A key the reader does not know is IGNORED, not refused: a producer may stamp extra fields (a later addition, or an internal one) and the strip renders from the keys above regardless. So the contract is a floor on what must be present, not a ceiling on what may be sent. A `point` the reader does not know is the one exception and draws NOTHING rather than being read through the shape above -- rendering an unknown record through a known reader is how a row comes to print a claim nobody made.

The strip is drawn whenever the row carries this record, and the consent switch is not consulted. A stamped record is history that already happened and already sits on the machine, so drawing it sends nothing; consent governs whether a FUTURE turn may ask Jev. Gating the receipt on the current switch would mean an owner who tried the preview and turned it off loses the record of which past replies were Jev-picked.

The verdict control sends `POST /api/decisions/feedback` (section 6) as `{"turn_id": str, "side": "jev" | "baseline", "verdict": "right" | "wrong" | null}`. `side` says which of the two lists is being rated, so the word-overlap rule can be called the better pick. `verdict` is nullable because pressing a lit thumb again retracts an earlier answer, and a retraction is a recorded event rather than an absent one.

## 9. The `tool.risk` annotation on the tool card

`tool.risk` asks one question about one tool call and puts the answer on that call's card. It decides NOTHING. The permission decision is taken without consulting it and is not altered by it: a call the policy allows still runs with `risky` on it, and a call the policy refuses is still refused with `safe` on it. Two things it does NOT claim, because neither is true. It is not free of TIMING -- the caller awaits it in the `tool_call` branch, which the harness sends BEFORE it asks for permission, so a sampled call's approval is answered up to the wait budget later than it otherwise would be; the cost is bounded, sampled, and paid only where the seam is on. And a badge is not a verdict on whether the call RAN: a host gate, a `PreToolUse` hook or a batch cascade can refuse a call that already carries one, which is the same asymmetry the `policy` field's own paragraph below describes. That property is the reason the point may exist at all -- an oracle answer is an observation, and an observation that could gate a tool would be a gate nobody reviewed -- and it is pinned as a mutation rather than asserted as a claim (`test_decisions_tool_risk_card.py::TestPermissionBehaviourIsUnchanged` runs one scripted turn twice, with the point answering and with it silent, and holds the `approve_tool`/`reject_tool` calls and the SEL audit rows equal).

The hook is `chat_runner._tool_risk_meta`, called from the `tool_call` event branch -- the one funnel that appends the tool card row -- and that branch contains no approval code at all. It is deliberately NOT placed in the permission branch: placing an `await` beside `approve_tool` would make "nothing about approval changed" a claim about the ordering of two adjacent statements rather than a claim about two disjoint regions of the file. The await sits AFTER the live `tool_call` broadcast and BEFORE `slot.append`, so the pill an open tab draws is not delayed and the record reaches both doors -- the `chat_message` frame `append` broadcasts from inside the call, and the persisted transcript line -- from one write.

It runs only where the owner consented to TOOL-ARGUMENT egress, and that is a second consent rather than a wider reading of the first. `skills.select` sends a message excerpt and candidate descriptions; this point adds the tool's name and its arguments, so the keystone records the two separately: `tool_args` (`consent.STATE_KEY_TOOL_ARGS`, read through `consent.consented_tool_args`), where only a literal `true` consents and ABSENT reads as not consented. `gate._tool_args_scoped` refuses any point in `POINTS_NEEDING_TOOL_ARGS` without it, checked inside `_consented_for` -- the documented chokepoint every `decide` and `is_enabled` path funnels its one keystone read through -- so the scope costs no second read and cannot be enforced in the preflight alone. The consequence is the point of it: an install that consented before this scope existed is INERT for `tool.risk` rather than retroactively signed up for it, which is the argument `history_budget_chars` already settled one field over. The refusal is warned once per point, because an owner whose feature does nothing must be able to tell that from a broken build. `PUT /api/decisions/consent` records it on the same terms as the history ceiling: written on enable, cleared on disable so a re-enable cannot inherit a scope nobody re-reviewed, an OMITTED field PRESERVING the recorded value through `consent.KEEP_TOOL_ARGS` resolved inside the writer's own lock, and a non-boolean refused with `400` rather than coerced -- `"true"` and `1` are not deliberate yeses about a new egress category. The GET reports it so the card draws the recorded state rather than inferring it. There is no new route.

A write that omits `enabled` is a SCOPE-ONLY write, and the route accepts one so a point's own panel can move that point's scope without restating consent: restating it means sending `enabled: true` with an endpoint echo, and a scope switch is not a review of an address. Omitting the field is handed down as `consent.KEEP_ENABLED`, which leaves the recorded switch AND the recorded endpoint untouched. It grants nothing on its own. Under a governance pin it is refused with `403 decisions_capability_denied`, matching the enabling branch: a withdrawn seam must not acquire a scope it would have refused to grant. Otherwise the pre-lock branch checks NOTHING else, and the two remaining cases part company. Against a REVOKED keystone the write is deliberately allowed through and answers `200` having recorded NO scope -- the writer resolves the switch from the keystone under its own lock, so the record stays revoked, and a scope is only meaningful while the seam is on. Against a keystone ENABLED for an address `config.json` no longer names it is refused with `409 decisions_consent_endpoint_changed` carrying the address in force, because that is the case where a scope would widen egress to somewhere nobody reviewed. That refusal is re-tested INSIDE the writer's lock as well: a re-bind of `provider.endpoint` landing in the window would otherwise leave the scope written onto whatever the keystone records by then, authorizing a wider egress category for an address nobody reviewed. Under `_SAVE_LOCK`, a `KEEP_ENABLED` write whose recorded endpoint differs from the endpoint in force raises `consent.ConsentEndpointMovedError`, which the handler answers with the same `409` and writes nothing. The SEL row for a scope-only write carries the same verb rule as every other write: `granted` when the body asserted a widening value, `revoked` when every value it carried was the narrowing one. One word for all of them would collapse a grant and a revocation into a single row, and which way a scope moved is exactly what an auditor reconstructing when egress started needs.

The consent GET also projects the POINTS this build ships, as `points`: one entry per point with its `id`, the scope it needs (`needs_scope`, absent for a point that needs none) and its effective `status` (`active`, `needs_scope`, or `off`). It carries no list of the settings a point does not control: exactly one point has one, and threading a per-row registry through the payload, the reader and a props map to print a single sentence is machinery one case does not pay for -- `skills.select`'s panel prints that line as a literal, and a second point needing one is when a map earns its place. The projection is `decisions.gate`'s, because the gate is what knows which points exist and what each one's keystone answer is; it rides this payload rather than a route of its own so a point's status and the consent it depends on come from ONE keystone read and cannot answer differently. A gateway that projects nothing is a build older than the field, which the card reports rather than papering over with a list of its own.

It runs only for a session that AUTO-APPROVES its tool calls (`_session_auto_approves`, which is `_slot_is_trusted(slot) or state.is_yolo_active()` -- the same two helpers the permission branch decides by, so the annotation cannot claim a mode the approval path is not in). A session that prompts puts the call in front of the person, who is the annotation; a badge beside a control they are already operating is noise. A trusting session describes nothing to anybody, which is where a badge is the only signal there is.

The state is the tool name, its arguments and an excerpt of the message that led to the call, each bounded (`MAX_TOOL_CHARS` 120, `MAX_ARGUMENT_CHARS` 2000, `MAX_MESSAGE_CHARS` 2000) and each passed through the canonical redactors FIRST, clipped after -- clipping first can halve a secret into a fragment neither redactor matches. Redaction rather than the gate's refusal is the one place this point differs from `skills.select`, and it is a deliberate difference: the gate REFUSES a state carrying a credential, which is right for a message excerpt, where a secret is a finding; a credential in a tool argument is ORDINARY (an `aws` command, a curl header), so refusing on it would mean the seam never annotates the calls most worth annotating. The placeholder is what the gate then scans, so nothing unredacted is cleared for the wire -- pinned in both directions, with the redacted state passing `gate.scrub_reason` and the raw one refused as `scrubbed:credential`. A redactor that itself fails drops the field rather than sending it. The message is OMITTED when empty, so the wire shape says something by its absence.

One question, one `Choice`, three options: `safe`, `caution`, `risky`, in that severity order, with a one-sentence rubric per tier in the prompt (`safe` only reads or reports; `caution` changes something inside the workspace that is easy to put back; `risky` destroys data, touches credentials, sends local data out over the network, or acts outside the workspace). An answer outside those three is unusable and annotates nothing.

The tier is not the flag on its own. `risky` must also clear `RISKY_CONFIDENCE_THRESHOLD` (0.80), and `earns_badge` is the ONE function that answers the whole question -- a surface testing `tier in FLAGGED_TIERS` alone would print a badge the point refused. `p` is the probability the provider assigned to the option it CHOSE, so a `risky` at 0.5 is a coin flip about the most alarming word in the domain, and an alarm nobody believes is what costs every other badge its meaning. The value is MEASURED, not picked: over this build's own day-files (40 answered `risky` calls, median 0.795, so the tier does not arrive confident) every answer at or above 0.80 named a push, a force-push, an upload or an opened pull request, which is what the rubric's own sentence asks for, while the answers below 0.62 were a queued monitor edit, a message to another session and a comment posted on a pull request -- none of which destroy data, touch credentials or leave the workspace. Nothing was answered between 0.62 and 0.76, which is the clean part of the reading. 0.90 was measured too and REJECTED: it drops eleven of the true ones, force-pushes and a credential fix among them, and a flag that misses a force-push is worse than no flag. `n` was 40, so the number is a floor with evidence behind it rather than a tuned optimum, and `test_decisions_tool_risk.py::TestTheRiskyConfidenceThreshold` pins it inside 0.75 .. 0.85 from BOTH sides so neither failure can be reintroduced quietly. It is a CONSTANT and not a setting, for the reason `memory.recall`'s `KEEP_THRESHOLD` is one: it is the meaning of the answer rather than a knob, and a configurable bar would be a second, undocumented way to make the point a no-op (`1.0` badges nothing) without turning the seam off.

`caution` clears on its tier alone, so an unconvinced `risky` draws NOTHING while a `caution` at the same confidence draws a badge. That asymmetry is deliberate rather than an ordering slip, and it is pinned as a test so it is not quietly repaired: the two words name different claims -- `caution` is about the workspace, `risky` is about data, credentials and this machine's edge -- and only the second is expensive to be wrong about. Reading an unconvinced `risky` DOWN to `caution` was the alternative and is not what happens: it would put "easy to put back" on a call the provider was describing as a force-push, a claim nobody made.

Rows: the gate writes the CALL row (`turn_id`, `tool`, `arg_chars`, `policy`, `call_index`), and the point writes one OUTCOME row for every ANSWERED call -- `safe` included, and an unconvinced `risky` included -- carrying `turn_id`, `tool`, `tier`, `p`, `policy` and `flagged`. `flagged` is what the reader was SHOWN, so it follows `earns_badge` and not the tier: a row saying `flagged` beside a card that drew nothing would be the two-descriptions-of-one-call failure the `policy` field's own paragraph refuses. The `tier` stays on the row either way, so a suppressed `risky` is still countable -- which is how the bar was measured and the only way a later one can be. `policy` is the grant that answers the call (`yolo`, `trust` or `trust_scope`, the caller's own descriptive spelling from `_auto_approve_reason`), NOT a per-call verdict, and it cannot be one: the harness sends the `tool_call` notification BEFORE it asks for permission, so the card this record rides exists before any verdict about it does. A later refusal -- a host gate, a `PreToolUse` hook, a batch cascade -- is recorded by the SEL tool-invocation audit, which is where a per-call outcome already lives; guessing at it here would give the log two descriptions of one call and make the weaker one look authoritative.

`safe` is recorded and NOT badged, and that asymmetry is the design: the log is the observation, so a log that only kept the alarming answers could not say how often the seam is wrong, while a badge on every card of a sampled session would cost the flag its meaning. So the point returns the row it wrote only for `caution` and for a `risky` that clears the confidence bar above, and returns nothing at all for `safe`, for an unconvinced `risky`, for a refusal, for a timeout, and for a row `append` reported it did not write -- a badge whose durable row was refused names a `turn_id` no verdict could be filed against, the rule section 5 states for the strip. There is no publish hook and no `decisions.outcomes` entry: that registry holds one outcome per SESSION and `consume` pops it, which is right for a strip describing one turn and wrong for an annotation describing one of several calls inside one turn. Producer and consumer here are the same line of code, so the record is handed straight to the `slot.append` that carries it.

Two budgets. The wait is `timeout_secs` plus `WAIT_MARGIN_SECS`, clamped into `MIN_WAIT_SECS` .. `MAX_WAIT_SECS` -- the same shape and the same three values `skills.select` waits by, held equal by test so a point cannot invent its own ceiling. And at most `MAX_CALLS_PER_TURN` (20) oracle calls per turn, counted by the caller's own per-turn tool counter: the call that CROSSES the cap writes one row with error `turn-cap` carrying `calls`, so "this turn stopped being annotated" is visible instead of indistinguishable from an unsampled one, and the calls after it write nothing. That cap row is the one row here not produced by a `decide` call, so it does not inherit the gate's refusals and asks for them itself -- an unsampled session writes nothing at all, cap included.

The record on the row is `meta.decisions_tool_risk`, on the TOOL message rather than the assistant reply, because the annotation is about one call:

```json
{
  "turn_id": "tr-9d41c7",
  "point": "tool.risk",
  "tool": "bash",
  "tier": "risky",
  "p": 0.88,
  "policy": "trust",
  "flagged": true,
  "latency_ms": 420,
  "error": null
}
```

This block IS the contract between the gateway that stamps the record and `website/src/pages/chat/toolRiskRecord.ts`, which reads it, on the same terms section 8 states for the strip: the reader's fail-safe is to draw nothing, which is byte-identical to a healthy release that stamps no record, so a rename on either side would hide the badge with no exception and no red test. `website/src/test/ToolRiskBadgeContract.test.ts` parses the fixture out of this section and asserts the reader accepts it field for field.

`safe` is not a readable tier on the frontend either: the reader refuses it, so "a record exists" and "this call was flagged" are one fact, and a producer that started stamping `safe` records could not quietly badge every card. An unknown tier, a missing `turn_id` and a probability outside 0..1 each draw nothing rather than a best-effort reading -- the badge's whole claim is that a flag means something, and a `turn_id` the thumbs could not name would be a control that lies about being one. A key the reader does not know is IGNORED, so the whole row can be stamped as the log wrote it and a later field is additive.

The badge renders under the tool pill: the tier word, the score, and one thumbs pair. Its tooltip says in words that the call ran anyway and that flagging it changed nothing, because a coloured word on a tool card otherwise reads as "this was blocked" -- and for the same reason `risky` is drawn in the WARNING colour, never the danger colour this transcript uses for a call that was actually refused. The thumbs are `DecisionVerdictThumbs`, shared with the strip, posting `POST /api/decisions/feedback` (section 6) as `{"turn_id": str, "side": "jev", "verdict": "right" | "wrong" | null}`. `side` is always `jev`: there is no second arm to rate -- the shipped permission policy is not an answer to this question, it is what happened regardless of it. No new route and no new config knob: the point is governed by the same consent keystone, the same `decisions.bucket` sample and the same `capabilities.decisions` ceiling as everything else in this package.

## 10. Mid-turn handling (`message.steer`)

A message typed while a turn is RUNNING takes one of two shipped paths, and the sender picks: the composer's split send button defaults to STEER (`chat_delivery.steer_into_running_turn`, injecting into the live turn) and offers QUEUE (`chat_delivery.queue_for_next_turn`, holding it for the next one). `message.steer` asks Jev which one this message wants. Both paths are unchanged; the point only decides which is taken, so every refusal -- the seam off, the session unsampled, a timeout, an answer outside the two options -- is the steer path the composer has always defaulted to, and there is no third outcome.

The point runs only when the sender SELECTED it. The split button carries a third mode, `Auto (Jev)`, drawn only while `GET /api/dashboard/config` reports `decisions_enabled === true` AND `GET /api/decisions/consent` reports `permits` (`website/src/pages/chat/useJevAutoSend.ts`, fail-closed on `=== true` for both) AND a turn is actually RUNNING on the slot. The last conjunct is not the same as the composer being busy: `selectComposerBusy` is also true for a slot whose background sub-agents are still working, and there is no turn to decide ABOUT in that state -- the send starts a fresh turn and this point never runs -- so offering the mode there would promise a decision nothing makes. Choosing it sends `steer: "auto"` instead of `steer: true`. A manual Steer or Queue never reaches the point -- that pick IS the answer to this question -- and `chat_handlers.steer_is_auto` accepts only the exact string, so the boolean every existing client sends stays a manual steer. There is no settings page and no config field for the mode: it is the per-slot composer preference `mc-busy-send-mode` already holds, and a stored `auto` resolves back to Steer while the seam is unavailable, so a withdrawn consent cannot leave a mode on screen whose send would not be decided.

The other preconditions are the call site's, and they are the branch's own: `api_chat`'s busy branch, which runs only when a turn is on the slot (`slot.running or slot._in_stage_execution`) and only for a send that is NOT app-authenticated. The turn is also FENCED across the decision: `slot.task` is captured before the await and compared after, and a change discards the answer and the receipt so the send takes the branch's own default. A decision is a provider round-trip, so the turn it describes can end inside it -- and "interrupt what it is doing" is not an answer about a turn that finished, nor about the successor that replaced it. The row stays in the log; only the receipt is dropped, because a receipt is what would misattribute it. An app token, an integration or a cron is never decided -- each already falls through to the fail-closed queue, and none has an owner watching the reply. The decision is resolved ABOVE the steer branch rather than inside it, so that branch keeps its exact shape and the only things `auto` changes about it are one conjunct on its condition and the receipt it stamps.

The state carries the new message (truncated to 2000 chars, the same bound `skills.select` applies) and, inside the consented ceiling, the RUNNING turn: its own request (at most 1000 chars) and the newest assistant text and tool activity (at most 1500 chars, `assistant`/`chunk`/`tool` rows only, walked newest-first back to that request). Those two halves are conversation the sender did not type into this send, so they are spent out of the SAME `gate.history_budget_chars` ceiling prior turns are -- the smaller of `config.json`'s ask and what the keystone recorded the owner reviewing -- which means an install with no consented ceiling sends the new message alone and the decision is made on it. The request is served before the activity because the activity only makes sense against it. `thinking` rows are never sent. Both halves are passed through `redact_credentials` and `redact_exfiltration_urls` before the gate sees them, and that is a CLEAN rather than a refusal on purpose: this text is whatever the agent printed, so refusing on it would turn "the reply quoted an env file" into "the seam stopped deciding", while the gate's own scan still refuses a spelling the clean missed. There is no `history` key: prior turns have ended, the turn in progress is what the question is about, and one ceiling spent on both would make the context worse the further back it reached.

The point emits, per decided send: one CALL row from the gate (`turn_id`, `turn_chars`) and, only where a usable answer arrived, one OUTCOME row carrying `turn_id`, `choice`, `baseline` (always `steer`, the arm the product would have taken) and `p`. A refused decision writes no outcome row and stamps no receipt, so a receipt exists only for a decision that was actually made.

The receipt rides the USER row, not the assistant one, and `decisions.outcomes` is deliberately not used for it: that registry holds one outcome per session for whatever finalizes the next ASSISTANT message and `consume` POPS, so claiming it here would take a `skills.select` turn's strip and attach it to the wrong reply. `message_steer.record_outcome` therefore RETURNS the row it wrote and the handler passes it to whichever delivery path runs, as `decision_strip=`: `steer_into_running_turn` stamps it into the `meta` of the row it appends, and `queue_for_next_turn` stamps it onto the queue ENTRY's meta, which the drain unions onto the row it writes -- the same mechanism `sendId` and the attachment lists already ride. The field is `meta.decisions_strip`, the same one section 8 describes, read by the same reader. A send that was not decided carries no such key, which is what keeps every other row byte-identical. The key is RESERVED to the gateway: `api_chat` drops it from request-supplied `meta` (`RESERVED_ROW_META_KEYS`) at the point that value enters, because `meta` rides verbatim onto the persisted row and onto the queue entry (`_redact_meta` redacts string values; it is not an allowlist), so a caller -- an app token included -- could otherwise stamp a decision nobody made and have the strip's verdict control file feedback against its turn id.

One case has the two disagreeing: `auto` answered `steer` and the steer was UNAVAILABLE (no live client, an unsupported backend, an RPC failure), so the send falls through to the queue carrying a record that says `steer`. The record describes the DECISION and the row's own `steerState` describes how the delivery ended; withholding it there would lose the only record that a decision was made.

`website/src/pages/chat/SteerDecisionLine.tsx` renders it as ONE line on the user row -- what Jev CHOSE, its probability and the latency, with the same `jev` thumbs pair `DecisionStrip` uses (shared, not copied). The line is phrased as the CHOICE rather than the delivery, because the two can differ: a chosen steer whose live client is gone falls through to the queue, and a line reading "steered the running turn" over a queued message would report the opposite of what happened. The delivery is what the row's own steer badge and queue card describe. The probability carries its own word rather than standing as a bare number, since the hover title is unreachable on touch and silent to a screen reader, and the thumbs' visible label names the CHOICE rather than repeating Jev's name, which the line already opens with. One line rather than a disclosure card because everything the decision produced fits on it: there is no second arm to compare and no menu to expand. Side `jev` only: the record names one decision by one party, and rating the `baseline` would rate a path this send did not take. The record on the row is the only condition, the same rule section 8 states -- a stamped record is history that already sits on the machine, so drawing it sends nothing.

The reader is `readSteerRecord` in `website/src/pages/chat/decisionRecord.ts`, and this block is its contract:

```json
{
  "turn_id": "turn-91c3ab",
  "point": "message.steer",
  "choice": "queue",
  "baseline": "steer",
  "p": 0.83,
  "latency_ms": 190
}
```

`choice` IS the claim and is held against the two shipped paths: unlike a skill key or a difficulty tier, a third value would name a code path that does not exist, so the line draws nothing rather than printing it. An absent or unknown `point` is not read as this record either -- an absent one is the older producer's shape and belongs to `readDecisionStrip`. `baseline` is on the record because the record IS the log row and the day-file fold reads it, and the READER ignores it: it is `steer` on every row -- the one arm a refusal keeps -- so a line printing it would print one word forever, which this document's own rule about a count that never changes excludes. There is no `error` field, and its absence is the producer's contract: a failed decision takes the steer path and stamps nothing. `latency_ms` floors to 0, which prints no latency, and a `p` outside 0..1 prints no score.

Tests: the two arms, the fallback on every refusal, the transcript walk (its stop at the running turn's request, the excluded `thinking` rows, the shared budget's split, the bounded read, the redaction of both halves) and the row's shape (`test_decisions_message_steer.py`); which path a `steer: "auto"` send takes, that a MANUAL steer or queue is never decided, that an app send is never decided, that a refused row stamps no receipt, that a request cannot supply a receipt through either door, and that a turn ending inside the decision discards the answer (`test_decisions_message_steer_apply.py`); the reader, the line, the user row's absent-field path and the third mode's availability (`website/src/test/SteerDecisionLine.test.tsx`), with the fenced record above held against the reader in `website/src/test/decisionStripContract.test.ts`.

### 10.1 Channel messages are not decided

This point is consulted from one place: `chat_handlers.decided_message_handling`, for a `steer: "auto"` send the dashboard's own human makes into a busy slot. No channel dispatcher calls it. A message a channel receives while its bound session is mid-turn takes the steer or queue path by the static `messaging.queue_mode` and the per-message `/queue` / `/steer` overrides ([messaging](messaging.md#channel-commands-outrank-the-busy-path)), and a message for a resumed session the dashboard is driving takes the queue arm only ([messaging](messaging.md#a-busy-resumed-session)), so there is nothing for the point to choose there either.

Wiring the point for channel-native turns is not built. It needs three things, named so the change that builds them extends the point instead of re-deriving it: a way for the sender to select it (a channel has no composer, so it would be a third `messaging.queue_mode` value, and whether an operator-level selection satisfies this section's "never behind the sender's back" is the owners' call); a per-turn generation on the provider seam, because `has_active_turn()` is a boolean that cannot tell a finished turn from its successor and section 10 requires the decision to be fenced; and a receipt, because a channel-native steer has no row of its own and a channel queue entry would have to carry `decisions_strip` into `_persist_turn`, which does not take it.

## 11. Model routing (`model.route`)

An enabled, sampled session that names NO model asks Jev to put each chat turn in one of three difficulty tiers and runs that turn on the model `decisions.model_route` maps the tier to. A refusal leaves the session on the model it was already on, which is exactly what every unconsented install does.

Naming no model is two states, and they are one answer to one question -- "the owner pinned nothing here": the owner picked `Auto (Jev)` in the picker (`slot.jev_route`), or `slot.model` is `auto` / `""` while the preview is on. The second is the DEFAULT, and it is what makes the preview a feature rather than a per-session chore: an owner who consented in Settings routes every unpinned session without arming each one, and a dispatched worker slot -- whose model is `""` and whose picker nobody is sitting at -- routes at all. A slot that DOES name a model is never routed: a pin is the owner answering this very point by hand, so it wins over the tier map. `chat_runner._jev_route_armed` is the one predicate for both states, read by the turn gate and re-read inside the model locks, and `website/src/lib/jevRoute.ts:isUnpinnedModel` is its frontend mirror, so the composer chip cannot say `Auto` for a turn that routed.

The chip therefore has THREE readings, and they differ by KIND rather than by two adjectives: a slot naming a model reads as that id; a routed slot reads `Auto (Jev)` and carries NO model id; an unpinned slot with the preview off reads the served id with the inherited-default marker beside it. The routed reading names the POLICY because a routed session's model changes from turn to turn -- an id on that chip is stale by the next reply, while the model a given turn ran on is on that turn's receipt, which is per-turn and cannot go stale. It is also the label the picker highlights for the same slot (`jevRouteShownModel`), so the chip and the open menu say one word for one choice.

The preview being on IS the authorization, and the reason it can be is that the ENVELOPE is owner-only. Routing reaches a model only by way of `decisions.model_route`, and only while the keystone says `enabled: true`; neither is writable by an agent or an app, so widening WHO may route cannot widen WHAT they reach. That is the assertion `test_decisions_model_route_auto_default.py` carries in place of a provenance check.

Only the owner arms it, and the arming is not a config key. The picker draws an `Auto (Jev)` entry only while `GET /api/dashboard/config` reports `decisions_enabled === true` (the fleet's `capabilities.decisions` answer, section 7) AND `GET /api/decisions/consent` reports `permits === true`; both are read as an exact `true`, and either alone withholds the row (`website/src/lib/jevRoute.ts`, drawn by `ChatPage` and each split `ChatPane`). `permits` and not the keystone's bare `enabled`, because it is the answer the GATE will give: it holds the consented endpoint against the one config names now and folds in the governance denial, so the redirected-config state -- consent recorded for one provider address while `provider.endpoint` names another -- withholds the row instead of offering a control that routes nothing. Neither read is an enforcement point -- the gate re-checks everything, so a request carrying the sentinel against an unconsented keystone routes nothing -- so the two reads are presentation.

Selecting the entry sends `POST /api/chat/slots/{slot}/model` with the sentinel `auto:jev` (`chat_handlers.JEV_ROUTE_MODEL`). The sentinel is resolved at the TOP of that handler and nowhere else: every reader below it -- the model guard, the live `session/set_model`, the reset path, the session allocation, the composer chip -- takes the plain `auto` it produces, and the choice survives only as the slot's `jev_route` flag. It is deliberately NOT stored in `slot.model`: that field is a provider model id, it reaches `session/set_model` and the session allocation, and a value no provider advertises would have to be filtered at each of those readers -- one filter per reader, and a real breakage the first time one is missed. The prefix is `auto` so a client too old to know the entry reads it as the Auto it behaves like, and the colon keeps it outside the model namespace, since no advertised id carries one. Matching is exact on a stripped string: a near-miss spelling is not this entry and falls through to the ordinary model path, where the guard refuses it.

`jev_route` is committed on each SUCCESS path of the handler and nowhere else: once in the same-value shortcut -- which is the commonest case for this flag, since an unpinned slot picking `Auto (Jev)` resolves to the `auto` it already holds -- and once inside the transaction, where `_rollback_pick` restores `prior_jev_route` with the model. Nothing is written above the busy check, so the `409 turn_in_flight` refusal cannot change what the next turn runs on. It is written on EVERY pick, not only the routed one: picking a concrete model is the owner answering the very question this point asks, so it clears the flag. The bulk switcher clears it too, on both its switched and its unchanged paths, and broadcasts when the only change was that. It is projected on every slot as `jev_route`, so an absent key and "pinned by hand" are not the same reading, and it is inherited by a fork (in-process, gateway-authored). Losing it on a restart no longer costs the owner the feature: the restored slot's `model` is still `auto`, so the preview routes it anyway, and the flag has narrowed to "this session was armed at the picker even though it names a model".

The flag is **in-memory only and deliberately not persisted**. It records an OWNER's pick that spends money -- a routed turn can run on a dearer model -- and transcript metadata is editable by the agent's own file tools, so a value read back from there would let a prompt-injected agent write `"jev_route": true`, wait for a restart, and be granted routing the owner never selected. A strict `is True` read does not close that: a genuine JSON `true` deserializes to `True`. This is the same rule, for the same reason, as the crew log refusing to promote a restored `_created_by` to gateway-authored lineage. The cost is that a gateway restart leaves the slot on its persisted model -- which is the documented refusal, not a new failure mode -- and the owner re-picks `Auto (Jev)` to route again. `test_the_flag_never_touches_agent_writable_transcript_metadata` asserts the absence against the source of every module that handles that file, because a re-added key would reopen the hole silently.

The point runs for a turn nobody else has already answered the model question for. `chat_runner._run_chat` calls it after the model-fallback restore probe and before the prompt is sent, gated on `_jev_route_armed(slot)` AND the turn's structural actor being `user` (`_crew_log_actor`) AND the turn being neither a synthetic payload, a harness slash command, nor a recovery continuation. Cron deliveries, sub-agent turns, app injections, crew-relayed turns and autonudge wakes are excluded by the ACTOR check: each already resolves its model through its own tier (`agent.role_models`, a crew binding, a cron's own slot), and a second answer for the same turn would contradict the first.

`_directive_user_origin` is deliberately NOT a condition. A turn a conductor delivers into a worker's slot arrives through `session_send`, which queues with `user_origin=False` and names no actor, and excluding it made the feature unreachable for exactly the sessions with nobody at a picker. What routing spends against is the owner's tier map and the owner's keystone, and the provenance of the TEXT cannot widen either.

That makes the ACTOR the whole gate, so the actor has to be honest, and it was not. `_crew_log_actor` falls back to `user` for any dispatch passing no `_turn_actor`, and of the five sites that read a request's `app` claim only `chat_handlers.api_chat` named it -- so the OpenAI-compatible route, rewind, regenerate and edit-and-resend each filed an app-authored turn as a person's. Under the old gate `_directive_user_origin` hid that; without it the same turn reaches routing as `user`. All four now stamp `_turn_actor="app"`, which is a correctness fix for every reader of that field and not only for this gate.

A turn reaches the runner two ways, and the actor has to survive both. A send to a BUSY slot is never dispatched by its handler at all: it is appended to the slot queue and the DRAIN dispatches it later, so no keyword can carry the actor across the wait. It rides the entry's meta (`chat_delivery.TURN_ACTOR_META_KEY`, which `chat_runner` re-exports so the stamp and the drain that reads it share one definition), and `_actor_for_queue_items` tries a mapped `kind` first and then that stamp. Three app-derived queue appends were silent -- the busy-slot queue, the sub-agent hold (`chat_handlers`) and the plan-approval queue (`chat_orchestrator`, whose `plan_approval` kind maps to no actor) -- so each drained as the person's turn. `queue_for_next_turn` takes a `turn_actor` keyword for the first; the other two stamp the meta dict they assemble.

That stamp is PROCESS-ONLY, and the half that makes it so is the important one. A queue entry persists to an ordinary writable file in the crew home, and `slot_queue_repository` already refuses to carry `_directive_user_origin` or `_directive_channel_origin` across a restart for exactly that reason -- its own docstring calls anything restored from that line attacker-supplied. The actor stamp is dropped on the same terms (`sanitize_restored_queue`), or whoever can edit the file decides which turns reach this point.

Dropping it is only half a fail-closed rule, because an ABSENT actor resolves to `user`, which is the arm this point admits. So the restore also marks the entry (`RESTORED_QUEUE_KEY`, process-local by construction -- the durable writer emits only id, content and meta, so a hand-edited line can neither set it nor clear it), the drain turns that into `_turn_provenance_restored`, and the gate refuses on THAT rather than on an actor it cannot vouch for. The cost is the one `slot.jev_route` already documents for a restart: the turn runs on the model the session is already on.

`test_decisions_model_route_app_actor.py` is an AST sweep over the dashboard package rather than a test per handler: the sites reach the runner through several shapes, so a test each would pin the ones that exist and say nothing about the next. It sweeps BOTH shapes, pins each population it can see, names the one site it cannot (`api_chat` builds its kwargs as a dict), and is revert-verified against every stamp. The queue rule's evidence is deliberately narrow -- the call's own `turn_actor` keyword, or an assignment into the exact dict it passes as `meta` -- because a first version searched the whole enclosing function and was satisfied by `api_chat`'s unrelated direct-dispatch stamp, i.e. it passed on the very defect it exists to catch.

The two halves of the gate are split by COST. `_jev_route_armed` is in-memory, so the turn gate can read it on the event loop; whether the preview is on is a keystone read, so `_route_model_for_turn` asks it once through `asyncio.to_thread(_jev_preview_on, ...)` -- `gate.is_enabled`, i.e. the same refusals `decide` re-runs -- and only for the IMPLICIT arm. That ordering is what keeps the shipped default free: consent off with every slot on `auto` is the overwhelmingly common install, and it pays one small read per turn rather than the history budget, the tier map and a provider round trip. It grants nothing, because `decide` checks all of it again.

The state is the message excerpt (`MAX_MESSAGE_CHARS` 2000, the same bound `skills.select` applies, because it is the same text answering a question about the same turn) plus `history` under the same shared budget -- `gate.history_budget_chars()`, the smaller of `decisions.history_budget_chars` and the keystone ceiling, over the same `MAX_HISTORY_MESSAGES` (20) read bound and the same `user`/`assistant` roles. Both points read prior turns through one implementation (`decisions/points/__init__.py`: `history_budget`, `prior_turns`, `build_history`), because a second copy of that walk would be a second place for the consented budget to be spent differently. `history` is omitted when there is none, so an install with no consented ceiling carries the message alone. The budget is read off the event loop and BEFORE the transcript, so at 0 the transcript source is never called.

The question is ONE `Choice` over `("simple", "medium", "complex")`, each carried in the prompt with a one-sentence description of the WORK. No description names a model or a price: the tier-to-model mapping is the owner's, and naming a model would ask the oracle to price the turn rather than to judge it. The tier domain is closed, so a provider inventing a fourth tier is refused by the gate as an invalid result; the point checks the value against the domain again, because it is about to index a map the owner writes where a near-miss spelling would read as an absent key.

An UNPINNED tier is answered, recorded and shown, and applies no tier model. That is the shipped state for all three tiers, and it is deliberately not a refusal: the answer is what tells an owner which tier their turns land in, which is how they decide what to pin. The outcome row and the strip carry `model_chosen: ""` (`model_route.UNPINNED`), the strip reads `complex -> (unpinned)`, and the row carries neither `applied` nor `model_used`, because no tier model was asked for. What it does NOT do is leave the session wherever an earlier turn's tier put it: a map is PARTIALLY pinned on the way to a full one, so "applies nothing" meant the first answered pinned tier captured the session for the rest of its life -- a one-line rename answered `simple` moved it to the cheap pin, the redesign answered `complex` applied nothing and kept it there, and the owner's own model was never reached again on that session. So an unpinned tier switches BACK to the pre-route baseline below, through the same seam and under the same two locks a pin uses. A restore that cannot be expressed attempts nothing: an unlatched baseline, a session already on it, a `""` baseline the backend never named (no id a `set_model` could ask for), or no switch seam at all each keep the model, which is the outcome every refusal has, and the unpinned outcome row is still written. A failed restore is recorded the same way rather than as an `error` category, because no tier model was dropped. `model_route.is_unpinned(tier, mapping)` is what tells that apart from a pin the account cannot run: both keep the model, only the second is a finding, and conflating them would hide the second.

`resolve_model(tier, mapping, advertised)` answers the model id or `""` (keep the current model). `""` covers an unpinned tier, a tier absent from the map, and a pin the provider does not advertise. The advertised list is the one the picker is built from (`llm_helpers.provider_advertised_ids`), so "the owner could have picked this by hand" and "the tier may route to it" are one test. An EMPTY advertised list is "not known", not "nothing is runnable", and reads as permitted -- the list comes from a live provider read that can be cold, and refusing on a cold read would make the feature silently inert on the first turn after a restart. Comparison goes through the canonical registry first and falls back to the frontend's own lossless fold (trim, lowercase, `.`->`-`) for an id the registry does not list; the registry's dotted and dashed entries stay DISTINCT (`claude-opus-4.8` is not `claude-opus-4-8`: two entries, two context windows), so routing never silently lands on a smaller window. Those two are named as the registry PAIR under test, which is the one thing a placeholder cannot stand in for -- a fake id has no registry entry and would exercise the string fold instead.

The switch itself goes through `llm_helpers.resolve_substitute_set_model(client)`, under the SAME two locks the fallback swap and the restore probe hold (the session-scoped switch lock, then the slot's pick lock, in that order), because it is the same kind of model transaction: without them a pick arriving through another alias of this session could land inside the `set_model` await and then be silently overwritten. The `decide` call is deliberately OUTSIDE those locks -- it is a network round trip, and holding a session-scoped lock across it would stall every sibling alias for the provider budget. `served_model` is re-read after the switch so the composer chip names the model the turn runs on.

`baseline_model` is the model the session runs with routing OFF. Three rules bind it. It is latched on the SESSION -- one attribute on the host the explicit-pick epoch lives on (`llm_helpers.pick_epoch_host`), never per slot, because several slot aliases drive one wire session and each alias's `served_model` is only that alias's cache of it; for the same reason the cache is re-read from the live session on every routed turn. The record is `(session id, model, pick epoch)` and ONE equality invalidates it: a moved epoch (an explicit pick through any alias, read with the restore probe's own default of `0` so a session's first pick counts), a different session id (a pooled runtime serving a new session), or no record at all. While a model SUBSTITUTION serves the session -- a throttle fallback or a refusal fallback, each undone by its own restore -- the latch takes what was substituted for and no restore is attempted; when that model is unnameable the latch is not taken at all. Nothing here is persisted, for the reason `jev_route` is not, and `""` is a real value: the backend named no model, so there is nothing to go back to.

The outcome row is written and published only once the switch has landed, so the strip and the log describe the model the turn actually ran on; the append is pushed to a thread, since the caller is the event loop. The strip's record is:

```json
{
  "turn_id": "turn-7b1c40",
  "point": "model.route",
  "tier": "complex",
  "model_chosen": "model-c",
  "baseline_model": "model-b",
  "p": 0.91,
  "latency_ms": 180,
  "history_chars": 0,
  "truncated": 0,
  "applied": true,
  "model_used": "model-c",
  "error": null
}
```

`tier` is REQUIRED and non-empty: it IS the answer, and a record that cannot name it has no claim left to print, so the reader draws nothing. It is deliberately not held against a fixed list of three -- the tiers are the backend's own question domain, and a reader that refused a fourth would hide a shipped tier instead of showing it. `model_chosen` MAY be `""`, which is the shipped state rather than a broken producer: the answered tier is unpinned and the turn kept its session's model, which the row prints as `(unpinned)`. The id above is illustrative; the one worked example of an owner's real pins is in `src/kiro_crew/docs/decisions.md`. `baseline_model` may be `""`, which is a real value and the common one: it is what the gateway records for a session running the backend's own default, and the row prints a named default rather than an empty gap. `p` is a finite number in 0..1 or `null`. `latency_ms`, `history_chars` and `truncated` are whole counts, and a negative or unparsable one reads as 0. `applied` and `model_used` appear on a row that ATTEMPTED a switch -- the tier's pin, or the restore an unpinned tier makes -- and say whether it took, always about the model THAT row's turn asked for: a returning `set_model` is not proof, because a backend that judges the model value can exhaust its candidate ladder and stay on the backend default without raising. So the model the session reports afterwards is read and recorded as `model_used`, and `applied` is `false` when the turn neither reached the chosen id nor moved off `baseline_model` at all -- a ladder's fallback spelling is a move, and a session already on the chosen model counts as applied. The reader treats only an exact `false` as a failed switch, so an unpinned row (which attempts nothing and carries neither field) and a row written before the fields existed both read as applied. The row is what an owner reads when deciding what to pin, and it is never rewritten, so it states what happened rather than what was asked for. Nothing here carries message text or a provider key -- the same bound section 5 puts on the log.

The row renders as one collapsed line: the point, the tier, the model it routed to, the score and the elapsed time. `baseline_model` is deliberately NOT on that line. It is the model the turn would OTHERWISE have used, and on a routed session -- which is every session with an unpinned slot once the preview is on -- that is the PREVIOUS turn's tier rather than a pin, so a caption reading `default: …` named a "default" that changed every turn and invited a reader to disagree with a comparison nobody made. It stays on the record and in the expanded panel, labelled `Model without routing`, which is what it actually is. Expanding adds the tier, both models, the conversation the question carried and what was clipped, plus the failure category when there is one. There is ONE thumbs pair, side `jev`, through the existing `POST /api/decisions/feedback` route (section 6) -- no new route and no new side: the alternative to a routed model is not another judgement but the absence of one, so a `baseline` pair would ask the reader to rate "whatever the session was on", which is not a decision anybody made about this turn.

Routing a turn to a dearer model spends more. What bounds that spend is the ENVELOPE rather than the entry: every model a turn may land on is one the owner listed in `decisions.model_route`, and the seam sends nothing until the owner's keystone permits it -- neither writable by an agent or an app. Inside that envelope the owner's own pin still wins: a slot naming a model is never routed, so choosing one by hand is how a turn is taken back out, and the picker entry remains the way to route a slot that DOES name a model.

## 12. Compaction scoring (`compaction.keep`)

`compaction.keep` asks, at every AUTOMATIC compaction, which of that session's tool calls would have been worth keeping. It is a SHADOW point: there is no apply path, `score_compaction` returns a record for tests and for the card and nothing in the product branches on it, and the compaction runs byte-identically whatever the answer is. That is the reason the point may exist at all -- the alternative is a live keep-list, and whether that trade is worth making is a question about data this point produces.

Why shadow and not live. Nothing in Kiro Crew can replace a harness's message list: `LLMProvider` (`providers/base.py`) offers `start / shutdown / stream / stream_command / compact / wait_for_compaction / cancel / steer` and no set-messages call, and `/compact` summarizes inside the harness process. The one place a keep-list COULD land is the transcript Crew re-injects after a recycle (`context.build_session_replay`), and that replay admits `user` / `assistant` / `inject` rows only (`context.RECALL_ROLES`) -- so every tool call and every tool result is already dropped today, and a live keep-list could only DISPLACE text inside the same 80 000-character budget.

The hook is `session_compaction.CompactionCoordinator._shadow_score_compaction`, called at the top of `_compact_session`. That method is the ONE funnel both AUTOMATIC entry points reach -- the per-turn threshold trigger (`check_context_usage` -> `_trigger_compaction`, a background task) and the awaited between-turn one (`compact_if_needed`) -- and it is reached BEFORE the Claude arm, before `_recycle_unmanaged` and before `_compact_in_place`, so every arm Crew itself takes is covered and the transcript observed is the one that stood when the compaction was decided.

What that population IS, and is not, stated exactly -- because "every backend" would be false. The hook sits inside the funnel, so it covers exactly the compactions Crew DISPATCHES; a harness that compacts on its own initiative is declined by `_compaction_gate_decision` before the funnel and is therefore never scored:

| Compaction shape | Scored? | Why |
|---|---|---|
| Threshold auto-compaction Crew dispatches — the in-place `/compact` on kiro-cli, codex, opencode | **yes** | `check_context_usage` → `_trigger_compaction` → `_compact_session` |
| The awaited between-turn check | **yes** | `compact_if_needed` → `_compact_session`, the same funnel |
| A recycle, because the in-place `/compact` failed or the backend has none (deepseek) | **yes** | reached from inside `_compact_session` |
| **Claude-Code-managed sessions** | no | `_compaction_gate_decision` returns `cc_managed` and declines before `_compact_session`; Claude compacts natively in-prompt, and Crew never dispatches it |
| **A harness that summarizes on its own initiative** (KAS, `ACP_BACKENDS_HARNESS_MANAGED_COMPACTION`) | no | the same gate returns `compact_unsupported`; KAS compacts and reports it, so there is no Crew-side dispatch to hang the measurement on |
| A backend in no compaction set at all | no | declined on the same rung, with the warning that names the missing classification |
| Manual `/compact` | no | dispatched as an ordinary turn through `provider.stream_command`; the person typed the command |
| Any other gate decline — below threshold, unconfirmed reading, already compacting, cooldown | no | `_compact_session` is never scheduled |

The two declines in bold are not gaps to close from here: there is no Crew-side dispatch to hang a measurement on, because Crew is not the one compacting. KAS reports its own summarization through `summarization_completed` and Claude compacts in-prompt, so measuring either would mean a different hook on a different event -- and the live design (#12516) targets the recycle path, which is exactly the population this hook already covers.

MANUAL `/compact` is excluded by CONSTRUCTION, not by a flag: the dashboard dispatches that command as an ordinary turn through `provider.stream_command("/compact")` and never calls `_compact_session`, which `test_decisions_compaction_hook.py::TestManualCompactIsExcluded` pins by asserting no caller of that method exists outside the session layer.

It cannot delay or alter the compaction, and that is structural rather than asserted. The task is created and NEVER awaited, tracked on `owner._background_tasks` so it cannot be collected mid-await; the compaction proceeds on its own coroutine; the only thing the deadline decides is whether the task gives up. `test_decisions_compaction_hook.py` runs one scripted compaction with a scoring that hangs for an hour and holds the compaction's own outcome and arm equal to a run with the hook stubbed out.

It runs only where the owner consented to WHOLE-TRANSCRIPT egress, and that is a THIRD consent rather than a wider reading of either of the first two. `tool_args` was reviewed as the arguments of the one call about to run; this sends the conversation and every tool input the session has accumulated, in a request one to two orders of magnitude larger. So the keystone records `compaction` (`consent.STATE_KEY_COMPACTION`, read through `consent.consented_compaction`), where only a literal `true` consents and ABSENT reads as not consented. `gate._scope_consented` refuses any point in `POINTS_NEEDING_COMPACTION` without it, from inside `_consented_for` -- the same chokepoint, so the scope costs no second keystone read. `_scope_consented` replaces the earlier `_tool_args_scoped` and drives EVERY scope off one table (`gate._POINT_SCOPES`), because a predicate per scope is a second place to forget one. `PUT /api/decisions/consent` records it on exactly the terms `tool_args` is recorded on: written on enable, cleared on disable, an OMITTED field PRESERVING the recorded value through `consent.KEEP_COMPACTION` resolved inside the writer's own lock, and a non-boolean refused with `400`. The GET reports it. There is no new route.

A write that moves only a SCOPE omits `enabled` entirely, and that is a property of the route rather than a convention its callers follow. A body carrying the switch can only carry what its sender last READ, so a view read before a revoke hands `true` back and re-grants egress the owner just withdrew -- and no client-side guard can make such a body safe, because the staleness is in the value. So an omitted `enabled` on a body that names at least one scope is a scope-only write: the route hands `consent.KEEP_ENABLED` down, and `save_enabled` resolves the switch AND the endpoint from the keystone inside the same lock the other three sentinels resolve in, writing both back unchanged. Such a write can neither grant nor revoke consent, and cannot rebind the endpoint -- the recorded binding is the one the owner reviewed. Against a REVOKED keystone it leaves the record revoked and stores no scope, which is the same rule that clears the scopes on a disabling write: a scope is only meaningful while the seam is on. A body naming neither the switch nor a scope is a `400`, so an empty or misspelled body is refused rather than silently rewriting the record as itself, and a non-boolean `enabled` is still a `400` -- accepting the omission did not widen what counts as consent. The fleet ceiling still gates only the ENABLING direction, because a scope-only write cannot turn the seam on and so acquires no authority a withdrawn ceiling would have to refuse. Both scope switches on the card write this shape; `client.ts` takes `enabled` as optional and sends the key only when the switch is what moved.

The state is read from `ConversationLog.read_messages(session_key)` -- the same JSONL the dashboard reads, and the only corpus carrying `meta.input` and `meta.output`; `recent` and the replay builder both project a row down to `role` and `content`. The SINGLE file rather than a chained or archive-inclusive read, and that is the subject rather than an oversight: the measurement is about THIS compaction, so its universe is the session as the compaction finds it. `read_messages_chained` would pull in a forked parent's rows and `read_messages_chained_full` the size-rotated archive -- rows this compaction is not discarding, which would sit in the denominator and shrink a share that is about what it throws away. Bounded three ways, and the third is a SIZE rather than a count: the newest `MAX_ROWS` (4000), the newest `MAX_CALLS` (1000), and `MAX_FIELD_CHARS` (4096) on each retained string. Counts alone left one pathological row -- a file read, a base64 blob -- held whole and re-redacted once per rung of the ladder, so the bytes a single transcript costs were unbounded; rows times this cap is what makes them finite. 4 KB is a SMALL multiple of what any rung sends rather than a large one: every rung clips a tool input to 1000 characters or less, so nothing is lost there, and a message longer than this reaches the wire as its first 4 KB -- which a measurement of which TOOL CALLS to keep does not turn on (conversation text is 6% of a measured transcript's characters). A bound generous enough to keep every message whole is a bound that does not close the crash it exists for. The admission bound is itself REDACT-then-clip, through the same `redacted` every other clip here goes through: a slice would cut a credential spanning the bound into a fragment neither redactor matches, and the fragment is what would travel, because a stage's own clip only ever sees what admission kept. Every LENGTH is taken from the RAW value, before both the redaction and the clip, so no tally and neither character arm moves; a tool RESULT is never sent at any stage, so only its length is retained at all. `user` / `assistant` text travels verbatim, each tool input is redacted then clipped, and EVERY tool result is replaced by `ok, N chars (omitted)` -- no result byte is on the wire at any stage, which is the widest single omission in the seam and keeps the largest half of a transcript local (results are 53% of a measured transcript's characters, inputs 33%, conversation text 6%). The first row and the newest `PINNED_TAIL_ROWS` (6) are PINNED: kept whatever an answer would say, marked `pinned: true` in the state, and never asked about.

Redaction is `platform.context.redact_via_context`, the canonical egress shim, and NOT the bare `security.redact_credentials` baseline. The baseline knows only the patterns this repository ships; a host that loaded a companion has its own credential and cookie spellings, and a baseline-only pass over a WHOLE TRANSCRIPT would send those verbatim to a third party. The shim deliberately re-raises `PlatformCompositionError` so a host that could not compose its companion cannot silently downgrade, and the point catches that and DROPS the field -- the same direction every other arm of `redacted` fails in, because a scan that did not complete cannot clear text for the wire. The shipped exfiltration-URL pass runs on top, since the shim covers credentials. Registered in `security_posture._REDACTION_SINKS`.

Tool ROWS are folded by `tool_call_id` before anything is counted. One logical call reaches the transcript as up to THREE `role="tool"` rows sharing one id -- the dispatch row `chat_runner` appends, plus the approval path's own approved/rejected row -- so a per-row count is not a count of calls: it inflated `total_calls`, sent the same input twice in the state, asked two questions about one call, and counted its characters twice in the denominator. Each field of the fold takes the first NON-EMPTY value (the dispatch row carries the input; the result handler writes the output onto whichever row it matched) and `pinned` is the OR across the rows, because a call whose approval landed inside the newest rows is one the agent is still working through. A row carrying NO `tool_call_id` -- a hook-blocked row -- gets a key of its own, because two rows that name no call cannot be shown to be the same call and merging them would undercount. The fold finishes before the cap is applied, so the cap measures CALLS rather than rows.

A walk that reaches `MAX_CALLS` keeps the NEWEST `MAX_CALLS` calls, in chronological order, and COUNTS the ones it dropped as `calls_truncated`; the count travels to the row, to the reader and onto the line. The newest end is chosen rather than fallen into: a forward walk that simply stops admitting keeps the OLDEST calls, which are the least likely to still matter, are not what the pinned tail is chosen for, and are not what this document promises. Past the cap the totals describe a PREFIX of the session, so a bare "N of M" would be a wrong sentence -- M is the cap, not the session's call count -- and the line says `(+K not scored)` instead, only when `K > 0`. A count rather than a flag, and published rather than withheld, because both other answers are worse: a wrong number, or hiding a measurement that is accurate about everything it covers. The overflow rows' characters are left out of BOTH arms, which keeps the two on one universe; the count is what says that universe is smaller than the session.

`chars_today_eligible <= chars_jev_eligible <= chars_all` is an INVARIANT, not a coincidence, and it is what makes the card's percentage a percentage. The arms count different ROLES -- the replay admits `inject` rows, which carry no tool call and are not part of the state -- so `chars_all` counts every row EITHER arm counts, not only `STATE_ROLES`. Without that, an ordinary `inject` row (a cron notification, a subagent-completion event) put bytes in the numerator that the denominator skipped, and the card rendered a share above 100%. The reader holds the same invariant from its own side: a share above 1 prints NO percentage rather than being clamped, because clamping would print a plausible number over a broken measurement.

Fitting is a five-rung ladder (`STAGES`), and the FIRST rung whose state is under `STATE_TOKEN_CEILING` (25 000 estimated tokens at `CHARS_PER_TOKEN` 4) is the one sent: inputs at 1000, then 200, then 60 characters, then the conversation text cut to head+tail halves, then to a quarter with each call's whitespace collapsed onto one line. The order is part of the contract -- inputs are clipped before text is touched at all, because clipping the 6% first would buy almost nothing and lose the part the question is answered against. Measured over 206 real transcripts the rungs admit 25% / 67% / 80% / 88% / 92% of sessions; the remaining ~8% fit nowhere and write one `state-too-large` row and send nothing, so the population that cannot be measured is visible rather than silently absent.

One `Choice` per NON-PINNED call, id `call_<index>`, options `drop` / `call` / `both` -- the reference design's three outcomes: drop it entirely, keep the call without its result, keep both. Questions are split into requests of `QUESTIONS_PER_REQUEST` (25) carrying the SAME state, at most `MAX_CONCURRENT_REQUESTS` (4) in flight, each an ordinary `decide` so each is scrubbed, bounded by `timeout_secs` and logged on its own. A batch is ALL-OR-NOTHING (`read_decisions`): a mapping missing one of the calls it was asked about cannot be folded into a fraction over all of them. The whole run is bounded by `wait_budget()` -- `timeout_secs` plus `WAIT_MARGIN_SECS`, clamped into `MIN_WAIT_SECS` .. `MAX_WAIT_SECS`, the same three values every point in this package waits by -- and a run that outlives it writes one `run-timeout` row.

Rows: the gate writes one CALL row per request (`turn_id`, `request`, `requests`, `questions`, `fitting_stage`), and the point writes one OUTCOME row per compaction carrying `total_calls`, `pinned_calls`, `kept_both`, `kept_call`, `dropped`, `calls_truncated`, `chars_all`, `chars_today_eligible`, `chars_jev_eligible`, `requests` and `fitting_stage`. The gate's own row records at most `log._MAX_ANSWERS` (8) answers whatever the batch size, which is why the tallies live on the outcome row rather than being recounted from the request rows.

Both character arms are named ELIGIBLE, and the names are the contract rather than a caveat. `chars_today_eligible` counts the rows today's recycle replay ADMITS BY ROLE, using `context.RECALL_ROLES` read from the replay's own definition rather than restated. That is the role filter only: `build_session_replay` additionally applies per-role row quotas (`_REPLAY_CONVERSATION_MAX_ROWS`, `_REPLAY_INJECT_MAX_ROWS`) and an 80 000-character budget scaled by the model window, so a long session is replayed with LESS than this arm reports. The true figure is deliberately not derived here: the quota walk takes a `ConversationLog` rather than a row list and the budget is applied inline while formatting, so the only thing available to measure is a rendered string whose length carries `Role:` prefixes and separators — formatting overhead inside a content measurement, and large enough to break the invariant below. Re-deriving the selection in this module would be a second implementation of it, free to disagree with the one that runs. So the arm is named for what it is and the card's copy says `up to`. `chars_jev_eligible` inherits the bound, because it seeds from that arm and then adds the kept calls' inputs and results, measured on the TRANSCRIPT's own lengths rather than the clipped state's (a live keep-list would re-inject the transcript's bytes). `chars_all` is everything the compaction is discarding and is the denominator the card's percentage is taken over.

A PARTIAL answer writes the row with error `partial-answers` and publishes NOTHING, because "23 of 61" over a denominator including calls nobody was asked about is a wrong number rather than an incomplete one.

Each compaction mints an ATTEMPT token (`begin_attempt`), synchronously, before the scoring task exists. `publish_record` refuses a run whose token is not the session's newest -- and an ABSENT counter is a mismatch, never a match, because the cap evicts the oldest counters and reading an absent one as equal would let the token AUTHORIZE a publish rather than only refuse one. Minting retires whatever the previous attempt left pending -- the rule `outcomes.discard` applies at the start of a turn, for the same reason. Without it, a scoring that straggled past its own compaction's notice had its record popped by the NEXT compaction on that key and shown as that one's measurement. The rows are written either way: the observation happened, and the log is where it lives.

The record reaches the compaction card through a store of the point's own (`publish_record` / `take_record`, one entry per session, `RECORD_TTL_SECONDS` 60, `MAX_PENDING_RECORDS` 200 bounding both the records and the tokens, the read destructive). The token itself comes from ONE process-wide `itertools.count`, so no value is ever handed out twice: numbering kept per session restarts at 1 once the cap evicts that session's entry, and a re-minted 1 equals the 1 a straggler minted before the eviction still carries -- the one way an equality comparison that may otherwise only REFUSE could ADMIT a record belonging to a notice already drawn. Losing a session's entry to the cap reads as a MISMATCH, which is the safe direction. `decisions.outcomes` is deliberately NOT used: that registry holds one outcome per session for the next ASSISTANT reply and `consume` pops, so claiming it here would take a `skills.select` turn's strip and attach it to a compaction notice. `dashboard/state.py::_compaction_keep_record` takes it and stamps `meta.decisions_strip` on the notice row `_on_compacted` already appends. A compaction that finishes before the scoring does takes no record, which is the documented "no card line" and is the same outcome as the seam being off.

The reader is `readCompactionKeepRecord` in `website/src/pages/chat/decisionRecord.ts`, and this block is its contract:

```json
{
  "turn_id": "cmp-7ab419",
  "point": "compaction.keep",
  "total_calls": 61,
  "pinned_calls": 7,
  "kept_both": 14,
  "kept_call": 9,
  "dropped": 38,
  "chars_all": 1482300,
  "chars_today_eligible": 74110,
  "chars_jev_eligible": 607743,
  "requests": 3,
  "fitting_stage": "inputs_200",
  "calls_truncated": 0,
  "latency_ms": 2140,
  "error": null
}
```

`keptCalls` is DERIVED by the reader from `kept_both + kept_call` rather than read as a field, the rule section 8 applies to `agree`: the line prints "N of M", and a count disagreeing with the tallies beside it would be a number nobody could reconcile with the log. A record whose three tallies do not SUM to `total_calls` draws nothing, and so does one with `total_calls: 0` -- "0 of 0" is furniture, not a measurement. The character share is `chars_jev / chars_all` and is `null` rather than 0 when either side is missing, the `messageChars` rule: folding "the record did not say" into "the record said zero" would print "0% of the characters" over a keep-set carrying plenty. A `point` that is not `compaction.keep` -- including an ABSENT one, which is the oldest producer's shape -- draws nothing rather than being guessed at. `calls_truncated` is read through `asCount`, so an absent field -- every record stamped before it existed -- reads as `0` and adds nothing to the line.

`website/src/pages/chat/CompactionKeepLine.tsx` renders it as ONE line UNDER whatever the compaction card drew, with the same `jev` thumbs pair `DecisionStrip` uses. Under, not inside one branch: the scoring starts before the compaction picks an arm, so a record outlives the success notice, the recycle notice AND the `⚠`-led failure that renders through `ErrorNotice`, and a line placed in one branch would drop it on the other two. The copy says "would keep", and that wording is load-bearing rather than cautious -- nothing was kept, and a reader who took the line for a description of what the compaction did would believe their tool history survived it. The record on the row is the only condition, the rule section 8 states. No new route and no new config knob: same consent keystone, same `decisions.bucket` sample, same `capabilities.decisions` ceiling.

Tests: the transcript walk, the pinning split, the fitting ladder and its monotonicity, the redaction-before-clip order AND its route through the companion shim (stubbed by behaviour, so a later edit reaching past it fails), the composition-failure drop, the absence of any result byte at every stage, the batching, the all-or-nothing batch read, the tallies and the two character arms, the `chars_today_eligible <= chars_jev_eligible <= chars_all` invariant under every answer and with a large `inject` row present, the eligible arm's equality with the role filter itself, the fold by `tool_call_id` (a dispatch row plus its approval row is one call, counted once in the denominator, asked about once, and the cap counts calls not rows), the overflow count and its newest-`MAX_CALLS` selection and the line that states it, the attempt token's supersession, its refusal on an evicted counter and its never reissuing a value across that eviction, the per-field retention bound (clipped in memory, whole in every tally, holding at every rung, generous for a tool input, small enough that rows times the cap is a size a gateway survives, and redacting a secret that straddles the bound rather than leaving a fragment of it), and the record store's bounds, plus the gate's refusal of the point without its scope (`test_decisions_compaction_keep.py`); both automatic entry points reaching the scoring, a recycle-only backend reaching it too, a hanging scoring not delaying the compaction, a broken point not costing it, the outcome being identical with and without the hook, the absence of a manual caller, and the stamp onto the notice row (`test_decisions_compaction_hook.py`); the keystone scope's reader, writer and route, including that `tool_args` does not grant it, that a scope-only PUT against a revoked record leaves it revoked and stores no scope, that such a PUT moves neither the switch, the endpoint, nor the scope it does not name, and that a body naming neither the switch nor a scope is a 400 (`test_decisions_compaction_scope.py`); the reader and the line on every card shape (`website/src/test/CompactionKeepLine.test.tsx`), with the fenced record above held against the reader in `website/src/test/decisionStripContract.test.ts`; and the third switch on the card (`website/src/pages/settings/decisionsCard.test.tsx`).

## 13. Recalled memories (`memory.recall`)

The agent recalls memory ON DEMAND, through the `memory_recall` tool: the tool calls `GET /api/memory/recall`, the route calls `VectorMemoryStore.recall`, and that searches episodes by vector similarity, drops what falls under the length-aware cosine relevance gate, and bounds the survivors to a char budget with its own `fit` walk. Similarity is the only judgement made, so a memory that is ABOUT the same words as the question is returned whether or not it answers it -- and every returned episode is paid for twice, once out of the tool's response budget and again out of the model's window. `memory.recall` asks the second question, per candidate: keep this one, or drop it. Jev's kept set is what the tool returns.

The seam is `VectorMemoryStore.recall(keep=...)`, armed by `handlers/memory_member._memory_recall_keep`. It is deliberately NOT on `get_episodic_context`: prompt assembly asks `MemoryStore.get_context` for preferences with `include_activity=False`, and that same flag gates episodic retrieval, so that block is not built on any live path. A `keep=` parameter there would be a second door nothing opens, which the next reader would take for a live path -- so it is absent, and `test_decisions_memory_recall_reachable.py::TestTheHookIsNotOnThePromptPath` pins its absence along with `MemoryStore.get_context`'s carried hook and `ContextBuilder`'s hook builder.

The point is a SUBSET operation and nothing else. It cannot reorder, because a keep/drop answer says nothing about rank and reordering on it would discard the ranker's judgement for one that was never made. And it cannot widen, which takes TWO enforcement points: `_kept_episodes` refuses an answer naming a row the search did not rank, and the hook is offered the rows `fit` ALREADY SELECTED rather than the raw search result. Without that ordering, dropping a high-ranked episode frees budget that a lower-ranked one -- one `fit` had already excluded -- then fits into, which is the hook ADDING a memory the response would never have carried, reached without ever naming an unranked row. Pinned by `test_decisions_memory_recall_apply.py::TestTheBudgetIsAppliedBeforeTheHook`.

A row the point's own SCREEN dropped -- capped out at `MAX_CANDIDATES`, or carrying an unusable id -- is returned UNCONDITIONALLY (`surviving_rows`). Only an offered row may be removed, and then only by an answer naming it: a plain filter cannot tell "past the cap" from "Jev said no", and treating them alike would delete memories nobody was asked about.

It runs only for a session with a live DASHBOARD SURFACE, and that condition is the CALL SITE's rather than the seam's (`handlers/memory_member._memory_recall_keep`). It is read TWICE, for two different jobs. In `_memory_recall_keep`, on the event loop before the search is dispatched, it is a cheap refusal that keeps a request the seam will not serve from paying for the gate's import graph. The read that GATES the egress is a predicate (`still_watched`) carried to `keep_decision` and read on the line before `core.decide`, because that call IS the egress. The earlier reads -- the route's, before the search is dispatched, and `kept_memories`', before the menu is built -- are cost savers: each happens while the answer can still change, since a tab closed DURING the search takes the slot out of the published set while a request already assembled would still go out. A guard for an egress belongs at the egress, and removing either earlier read leaves the route-level test passing on the strength of the one at the call. An egress gate has to be read at the moment of egress, and this is the one gate where the difference is sending versus not sending. Two reasons, both about who is served: the egress is snippets of the member's own recalled memories, and the receipt for the decision rides a reply someone is looking at, through `decisions.outcomes`. A cron turn, a sub-agent, an integration and any session whose tab is closed have no such reader.

The test is MEMBERSHIP of `session_surface.dashboard_surfaced_keys()`, the set `chat_utils._sync_dashboard_slots` republishes from `state._slots` -- `effective_session_key` for every open slot -- whenever the slot table changes. So a CHANNEL-born session with its dashboard tab open DOES qualify, because a channel-born slot contributes its channel key, which is the intended reading: the owner is reading that conversation in the dashboard, so the strip reaches them.

Deliberately NOT `has_dashboard_surface`, whose first test is a `dashboard:` PREFIX answered before observed state is consulted at all (`rfc-session-address-model.md` section 2.2 documents that fast-path). That branch is right for a gate asking "could this session ever be shown" -- a dashboard-born session qualifies before the dashboard has published anything -- and wrong for this one: a retained or archived slot keeps its key and has no open tab, so the prefix would send recalled memory about a conversation nobody is watching and stamp a receipt on a reply nobody reads. Open-tab membership is exactly the liveness an egress gate wants, and an empty set before the first publish fails CLOSED. Pinned by `test_decisions_memory_recall_reachable.py::TestTheGatesStillHold::test_a_retained_dashboard_slot_is_not_decided_for`, which asserts the prefix reading says yes for the key it refuses.

It also runs only where the owner consented to RECALLED-MEMORY egress, and that is a consent of its own rather than a wider reading of any beside it. `skills.select` sends a message excerpt and candidate descriptions; a recalled memory is neither -- it is text the AGENT wrote down turns or days ago, about work the owner was not reviewing when they flipped the switch. So the keystone records them separately: `memory_text` (`consent.STATE_KEY_MEMORY_TEXT`, read through `consent.consented_memory_text`), where only a literal `true` consents and ABSENT reads as not consented. `gate.POINTS_NEEDING_MEMORY_TEXT` names the point and `gate._POINT_SCOPES` binds it to the reader that answers for it -- `("consented_memory_text", "the text of recalled memories")` -- and `gate._scope_consented` refuses the point without it, inside `_consented_for`, the chokepoint every `decide` and `is_enabled` path funnels its one keystone read through. The consequence is the point of it: an install that consented before this scope existed is INERT for `memory.recall` rather than retroactively signed up. The refusal arrives as `is_enabled` answering False, so it costs no redaction and writes NO row -- the gate's three cheap refusals touch no disk by design, which is what makes "no consent leaves the log directory empty" checkable, and the diagnostic is the once-per-point warning naming the point and the category. `PUT /api/decisions/consent` records it on the same terms as `tool_args` and the history ceiling (section 9), and the Decisions card draws a switch of its own for it, under the whole-transcript one. The card's own list of what Jev decides carries a `memory.recall` row, gated on this scope exactly as the `compaction.keep` row is gated on its own: without the scope the point is inert, so a row for it would name a decision that cannot happen. It is listed rather than left out because it is the one point on that list which changes what a TOOL RETURNS -- the others annotate or measure -- and a list answering "what does Jev decide" that omits it understates the feature at the moment the reader is deciding whether to grant it.

A SCOPE-ONLY write omits `enabled` entirely, and the writer preserves the recorded flag AND the recorded endpoint for an absent one (`consent.KEEP_ENABLED`, resolved inside `save_enabled`'s own read-modify-write under the same lock). That is what keeps a scope click from asserting a consent state: sending even the value the card had just read is a write against a switch the owner did not touch, and a read taken before a concurrent revoking PUT would re-commit a consent that had been withdrawn. The governance ceiling is held against whether a write GRANTS -- consent itself, a scope, or a NON-ZERO `history_budget_chars` -- rather than against `enabled`, so a grant under a pin is a `403` while a revoke stays available for the reason a disabling PUT does. The ceiling counts because the number bounds how much prior conversation may leave the machine, so recording a bigger one under a pin books an egress allowance for the moment the pin lifts, and a pin is exactly when it is cheapest to book. It is judged by VALUE and not against the recorded number: reading the old ceiling in the handler would be the stale read this route refuses to take, and no read is needed, because `0` is the only budget write that cannot widen anything -- which is also why `0` stays available under a pin, being how prior turns are taken back. A DISABLING write carrying a budget is excluded, since `save_enabled` forces the ceiling to 0 on one, so it records no allowance to refuse. The recorded ENDPOINT needs no such rule: it is only ever `gate.configured_endpoint()`, a server-side read, and a write that preserves consent preserves the recorded address from the writer's own read, so the body's `endpoint` is never anything but the echo an enabling write is checked against. The SEL verb is taken on the same terms, from what the body ASSERTED rather than from the flag the write left behind: `granted` when any field it carried widens egress, `revoked` when every field it carried narrows one, and `allowed` for a body that asserted nothing and therefore changed no permission. The ceiling is judged by the SAME expression the governance check uses rather than by a second test of it, so a raise audits `granted`, an explicit `0` audits `revoked`, and a pin cannot refuse a write the log then calls something else; a body that mentioned no ceiling stays `allowed`, which is why the absent case is kept apart from the false one. Read off the recorded `enabled` flag instead, a PUT turning a scope OFF beside still-enabled consent audits as `granted` -- the opposite of what it did, in the one row an auditor reconstructs "when did egress start" from. A mixed write naming one grant and one revocation reads as `granted`, the widest thing it did, and `resources` carries every recorded value either way.

The wiring is a `keep=` callable, mirroring `skills.get_triggered_skills(select=...)`: the recall route builds the hook and `VectorMemoryStore.recall` carries it to `_recall_once`, which owns every fallback (`_kept_episodes`). The route is the right place because it is the one that has all three things the point needs -- the session key (from `X-Session-Key`, which the MCP tool sends), the event loop (it is a coroutine), and the `run_in_embed_pool` call that puts `recall` on the worker thread the point must be called from. A hook that raises, returns `None`, returns a non-list, names a row the search did not rank, or reorders is discarded and the full result is formatted. Membership is judged on `id()`, not equality, because two distinct episodes can hold equal dicts and a value test would let one answer admit the other -- which is why the point hands back the SAME row objects it was given rather than copies. That coupling fails silently in the safe direction (every decision discarded, the baseline block injected), so it is pinned from both sides in `test_decisions_memory_recall.py::TestTheHookAndTheStoreAgreeOnIdentity`.

`Choice` is the only question type `impl_jev` speaks, so the request carries ONE question per candidate -- `keep` or `drop` -- rather than a new question type or a single question whose answer would have to encode a subset as a string the gate could not check against a domain. Question ids are ORDINALS (`m0`, `m1`, ...), never memory ids: they are dictionary keys in the request body, and the caller already holds the list, so the position is what maps an answer back. The provider reports the probability of the option it CHOSE, so a `drop` answer's probability is read as its complement; without that, a confident drop clears the threshold on the raw number and every candidate is kept. A memory reaches the prompt at a keep probability at or above `KEEP_THRESHOLD` (0.5), which is a CONSTANT rather than a setting: it is the meaning of the answer, and a configurable threshold would be a second, undocumented way to make the point a no-op without turning the seam off. EVERY offered candidate must carry a readable answer -- a missing one is indistinguishable from "drop it", so a partial reading is a refusal rather than a partial application.

The state carries the message (truncated to 2000 chars, the same bound the other points apply) and at most `MAX_CANDIDATES` (20) candidates, each `{key, snippet}`. There is NO `history` key: the candidates ARE prior conversation, already ranked by relevance to this message, so spending the consented `history_budget_chars` ceiling on a second unranked copy would make the question worse and the egress larger. Each snippet passes `redact_credentials` then `redact_exfiltration_urls` over the WHOLE text and is clipped to 200 characters AFTER that, never before -- a cut placed first can halve a secret into a fragment neither pattern matches, the order `tool_risk.scrubbed` and `message_steer.redacted` both state. This module SCRUBS rather than refusing, because a recalled episode is text the agent wrote down turns or days ago and refusing on one that quotes an env file would stop the point firing on that store at all; a redactor that raises yields the empty string, and the gate's own scan still refuses the whole request for a spelling this pass missed. The clip is a HEAD rather than a tail, unlike the running-turn excerpt: a memory's opening is what says what it is about. The path is registered in `security_posture._REDACTION_SINKS`. The point sends no tool-call arguments, so it is deliberately NOT in `gate.POINTS_NEEDING_TOOL_ARGS`: adding it would make an already-consented install inert for an egress category it does not perform.

The wait is BOUNDED, and the ceiling is the part that matters. The provider call is capped by `gate.timeout_secs()` -- `provider.timeout_ms` from the config, default 1000 ms, always finite and positive -- and the caller's own wait is that plus `WAIT_MARGIN_SECS` (0.5 s of scheduling slack, because the coroutine is submitted to a loop that may be mid-task), clamped into `MIN_WAIT_SECS` .. `MAX_WAIT_SECS` = 0.25 s .. 7 s. The clamp is why a hand-edited `timeout_ms` of an hour cannot hold a recall on this path: 7 s is the most a sampled call can ever spend here, whatever the config says, and expiry cancels the future and returns the similarity top-k.

The ceiling is DERIVED rather than declared, and that is the correction a constant could not make. `_wait_budget` holds the provider's budget under `_remaining_budget()` -- the time actually left on the enclosing request -- so the wait shrinks by whatever the recall has already spent. The search runs BEFORE the judge does, inside the same bounded request, so a cold store, a rebuilt index or a slow embedding spends budget a fixed ceiling would assume was still there; at 7 s against a 9 s route that left 2 s of headroom and a full wait then ended PAST the deadline, answering `504` instead of the memories the search had already found. The deadline is readable at the point because `run_in_embed_pool` submits the search with `copy_context().run`, so the worker thread the hook runs on carries the loop's `EmbeddingWork`. `WAIT_MARGIN_SECS` is withheld from the remainder rather than spent, because the judge answering is not the end of the request -- the store still has to finish, and the route still has to bound and serialize -- so a wait that ran to the deadline itself would turn a judge that answered in time into a `504` anyway. One quantity serves both ends of that slack, so there is no second number to keep in step. A remainder at or below zero is a REFUSAL rather than a shortened wait: nothing is sent, no receipt is held, and the similarity result goes back unnarrowed, which is what every other failure on this path does. `MAX_WAIT_SECS` stays as the ceiling where no deadline is in force -- `kept_memories` is reachable from outside the bounded route -- and as a belt on a hand-edited provider timeout. The ceiling is 7 s and not `skills.select`'s 10 s because THIS point sits inside a request that has a deadline of its own. `GET /api/memory/recall` is wrapped in `memory_recall_deadline`, which bounds the whole recall at `executors.RECALL_TIMEOUT_SECS` (9 s, itself under the MCP client's 10 s) and answers `504 memory_recall_timeout` when it expires. A 10 s wait therefore could outlast the request it is inside, and break this seam's one promise in the only direction that costs the caller something: instead of the memories the search had already found, the tool would answer with an ERROR -- a slow judge taking the recall down with it. 7 s leaves 2 s for the search the decision is attached to. `skills.select` sits on the prompt path, which has no enclosing deadline, so its own ceiling is not wrong there. The relationship, rather than either number, is what is pinned: `test_decisions_memory_recall.py::TestTheWaitFitsInsideTheRoute` asserts `MAX_WAIT_SECS` is under `RECALL_TIMEOUT_SECS` with the headroom named.

A row whose episode id is missing, over `MAX_KEY_CHARS` or a duplicate is dropped from the MENU rather than repaired -- the id is what maps an answer back. It stays in the response and in the BASELINE arm, because the baseline is what the recall would have returned rather than what was asked about; `candidates` on the record is the menu size, so the two numbers answer different questions and neither stands in for the other. A row with no readable id at all is left out of both KEY LISTS rather than named as `""`, because the strip's reader refuses a list carrying an empty name and one such row would cost the whole receipt; it is still in both arms, and `chars_saved` measures rows rather than keys, so the saving stays exact.

Four different things shorten a recall, and every number on the receipt names exactly one of them. `candidates` is OFFERED -- the rows put in front of the oracle, already selected by the store's `fit` walk, capped at `MAX_CANDIDATES` and screened for a usable id. `baseline_keys` is what the recall returns without this point. `jev_keys` is KEPT BY JEV: the decision's own output, in the search's order, and it is never quietly narrowed into the set that shipped. `bounded_omitted` is DROPPED BY BOUNDING: how many of `jev_keys` the response budget then removed, counted BESIDE the kept list rather than subtracted from it, so `len(jev_keys) - bounded_omitted` is what arrived and each removal stays attributable to whoever made it. Folding the second into the first is what made the header read "Jev kept 1" beside a row saying "2 that Jev kept did not fit" -- arithmetic a reader cannot close. `chars_saved` is the characters of memory text JEV'S NARROWING removed and nothing else's: both arms are measured by `injected_chars` on the SAME rows at the SAME moment, before redaction and before bounding, so the scrubber's substitutions and the budget's clipping are absent from the difference by construction rather than subtracted by hand. REMOVED BY REDACTION is therefore a quantity the receipt deliberately does not carry -- it is the same scrubbing applied to whatever ships, it is no part of what Jev decided, and a figure folding it in would credit the scrubber's work to the judge. `baseline_chars` and `jev_chars` are both on the row so a reader can check the subtraction. 

The point emits, per sampled recall: one CALL row from the gate (`turn_id`, `candidates`, `message_chars`) and, only where a usable answer arrived, one OUTCOME row carrying `turn_id`, `baseline_keys`, `jev_keys`, `agree` (SET equality), `p` and `chars_saved`. `p` is the MEAN keep probability over the offered candidates and is named as a summary: the request asked one question per memory, and the strip has one number to print. `chars_saved` is the baseline response's episode characters minus the kept set's, each clipped by `vector_memory.EPISODIC_BLOCK_TEXT_CHARS` -- the same constant the store's `fit` walk clips to, named because a literal in one place and a different literal in the other would make the saving a number about a block nobody assembled. It floors at 0, since the point only removes.

The outcome row is written in TWO steps, and the ordering is a correctness property rather than tidiness. The point HOLDS its outcome (`kept_memories(pending=...)`) and the route commits it (`commit_receipt`) only after the recall has come back and it is answering with it. `commit_receipt` is the ONLY thing that writes or publishes a receipt, and it takes the completed payload as an argument -- which is the structural half of the rule rather than a convention: a caller cannot reach the writer without the finished response in hand, and the writer, not the caller, checks that anything was held, that the payload carries a delivered `retrieval`, and that the request has not already been abandoned. `delivered_outcome` and `publish_outcome` are not reachable names: a second way in would be a second place for those three checks to be forgotten, which is the whole reason they sit in the funnel. There is nothing left to get right at the call site. `_recall_once` validates the embedding generation AFTER applying the hook, and `recall` answers a moved generation by running the whole recall again keyword-only -- so a row written the moment the answer arrived would describe a subset the store then discarded, and a retried recall would leave two rows for one tool call. Only the LAST held entry is committed, because that is the attempt whose result was returned; the earlier ones are dropped rather than written.

The commit happens after the FINAL bounding, not before it, and that ordering is the second half of the same property. `bound_recall_payload` runs after the decision and holds the response to a transport budget: it clips the largest text and, once no text is left to clip, drops whole rows off the tail. Multibyte content reaches that budget on an ordinary query rather than an exotic one, because the same text is counted in the evidence, again in the model-facing context and again through JSON escaping. So a receipt built from the decision's own output names memories and a saving that never left the process. The route therefore bounds the payload itself (`_RECALL_CONTEXT_CAP`, named because the bounding runs twice on this path and two literals could bound the receipt to a different budget than the response), hands the RESULT to both the commit and the response, and re-reads the receipt from the delivered rows through `delivered_outcome`: `jev_keys` becomes the ids actually present, `chars_saved` is measured against the same `baseline_chars` so it is the reduction the caller really got, `agree` follows the arm that arrived, and `bounded_omitted` states how many of the decision's own rows bounding then removed. That last field is what keeps the smaller kept count and the larger saving from reading as Jev's work, and it is ABSENT rather than 0 when bounding removed nothing, so an ordinary receipt carries no field about a thing that did not happen. `baseline_keys` is untouched: it is a counterfactual nothing bounded. Pinned by `test_decisions_memory_recall_reachable.py::TestOnlyACommittedRecallLeavesAReceipt::test_a_bounded_payload_records_the_bounded_numbers`, which lowers the cap until bounding drops one of the kept rows and then holds the row against the response.

So a failure of ANY kind leaves NO receipt: a provider answer that arrives after the caller's wait expired, a generation the store superseded, a request that raised after the decision was made, a route that refused. The gate's own CALL row still records a timeout or a transport error, because that is a fact an operator acts on; what does not exist is an outcome row for a subset nobody received. Pinned by `test_decisions_memory_recall_reachable.py::TestOnlyACommittedRecallLeavesAReceipt`, whose retry case makes the two attempts answer differently so that "commit the last" is distinguishable from "commit the first".

The receipt rides the ASSISTANT row through `decisions.outcomes`, the same hand-off `skills.select` uses and the same `meta.decisions_strip` field. That registry holds ONE entry per (session, point), so a turn whose agent called the tool twice publishes twice and the second replaces the first: the strip describes the turn's MOST RECENT recall. Kept rather than changed, and the copy carries the scope instead -- accumulating would need a per-point list in a registry four points share plus a transcript component that draws N strips under one reply, which is a larger change than the thing it fixes. The strip's expanded body states the scope in words and the collapsed label carries it as a title, so a reader is never shown one receipt that looks like it covers every recall the turn made. The LOG is unaffected: each call appends its own outcome row, so the measurement the strip samples is complete in the durable record even where only the latest is drawn. `website/src/pages/chat/MemoryRecallStrip.tsx` renders it and `readMemoryRecallRecord` in `decisionRecord.ts` is its reader; this block is that reader's contract:

```json
{
  "turn_id": "turn-7d1e04",
  "point": "memory.recall",
  "baseline_keys": ["mem-a", "mem-b", "mem-c"],
  "jev_keys": ["mem-a", "mem-c"],
  "p": 0.81,
  "chars_saved": 2100,
  "candidates": 3,
  "message_chars": 96,
  "latency_ms": 210,
  "error": null
}
```

The two lists are `baseline_keys` and `jev_keys`, never `baseline` and `jev`, and that is load-bearing rather than a style choice: those two names are what `readDecisionStrip` requires, so a memory record spelling them would be ACCEPTED by the skill reader and drawn as a skill selection holding memory ids. Naming each list for what it holds makes the three records on this one field decline each other on SHAPE as well as on `point`, and `point` is REQUIRED here -- an absent one is the oldest producer's shape and belongs to `readDecisionStrip`. `agree` is not a field, for the reason section 8 gives: the strip derives it from the two lists, so a promised flag could only ever contradict them. `chars_saved`, `candidates` and `latency_ms` are whole counts flooring to 0; `message_chars` is the one count whose absence reads as "not stated" rather than 0, and the strip then draws no egress row. There is no `history_chars`, because this question sends none. `bounded_omitted` is the one field the fence above deliberately omits: it is absent on every recall the response budget did not shorten, which is the ordinary case, and the reader floors it to 0. `baseline_chars` and `jev_chars` ride the row for a reader checking the saving and are not read by the strip. A key the reader does not know is ignored, so the contract is a floor on what must be present.

The strip is COUNTS collapsed and ids expanded: `memory · recalled: 6 · Jev kept: 3 (confidence 0.81, 210 ms) · saved 2.1K prompt chars`. On a record carrying an `error` the second count and the panel's list title name the FALLBACK instead (`kept after fallback: 6`, `Kept (fallback)`): the decision did not land and the shipped recall was returned whole, so crediting Jev beside "Decision failed" would name an actor that chose nothing. The list itself is still drawn -- it is the attribution that was wrong, not the content. A memory id is a store handle a reader cannot read anything off, so six of them on one line would be six opaque words where the only actionable fact is that three were dropped; the ids are in the expanded body, where a reader who wants to look one up can. Both counts are printed even when they are equal -- "similarity 6 · Jev kept 6" already says the two sides agreed, so there is no agreement branch and no second way to say one thing. The body adds the two id lists, the candidate count, the excerpt length, the latency, a `Dropped to fit` row drawn only where `bounded_omitted` is non-zero, the scope sentence naming the turn's latest recall, and a second `DecisionVerdictThumbs` pair on side `baseline`, so a reader can say the similarity ranker was the better judge instead of only rating Jev. Each pair names the ACT it rates and WHERE in the sequence it sits -- `Rate Jev's narrowing` and `Rate the search (before Jev)` -- because two labels sharing one noun read as one vote on one thing, and two names for adjacent steps still read as one step. The `Dropped to fit` row carries BOTH numbers in one wording (`{{count}} of the {{total}} Jev kept did not fit the response`), so the subtraction closes where a reader sees it rather than across two lines that disagree. On a record carrying an `error` the Jev pair is not drawn at all (the judge chose nothing, so there is no pick to rate) and a sentence says the SEARCH still ran, since the remaining pair then sits under `Decision failed` and would otherwise read as an invitation to rate something that did not happen. The strip's root carries `data-bounded` beside `data-agree` for the screenshot harness, which selects a row by the state it photographs. Both pairs post to the existing `POST /api/decisions/feedback`; there is no new route and no new config beyond the point name in `gate.DECISION_POINT_NAMES`.

Tests: the decision and its application, every refusal, the complement reading of a `drop` probability, the threshold's inclusive bound, the ordinal question ids, the menu's caps and key screen, the redaction order, the absent `history` key, both arms on the row, the publish hook present/absent/raising, the store's own fallbacks, the identity coupling from both sides and a revert-verify of the apply (`test_decisions_memory_recall.py`); the kept set reaching a real recall response, the switch-off result being unchanged, and the budget being applied before the hook (`test_decisions_memory_recall_apply.py`); the whole chain from the route inward, with a row logged, the oracle asked and the kept subset returned, plus the three absent prompt-assembly seams (`test_decisions_memory_recall_reachable.py`); the reader, the two strip states, the three-way mutual refusal and the assistant row's absent-field path (`website/src/test/MemoryRecallStrip.test.tsx`), with the fenced record above held against the reader in `website/src/test/decisionStripContract.test.ts`.

## 14. The wake judge's second lane (`nudge.wake` provider)

PR #11490 shipped this seam with ONE provider and said so: the LLM lane was cut because nothing consumed it. `nudge.wake` is that consumer, so the lane is back, and it is the whole reason the judge can ship to a machine whose owner has no Jev key. The point itself -- its collectors, its questions and its verdict mapping -- is specified with the autonudge module; this section is only about which oracle answers it and on whose authority.

`decisions.nudge_wake.provider` selects over `auto | jev | llm`, and `decisions.nudge_wake.llm_model` names the model the `llm` lane runs on. Both are ordinary config values written through the config route's `_EDITABLE_CONFIG` allowlist, beside the `decisions.model_route.<tier>` tier pins, and both are hot-applied: the gate reads the live snapshot per call. An unknown provider name reads as `auto` rather than as an error, because a typo must not become a third lane and must not stop the gateway booting. An empty `llm_model` INHERITS -- the runner passes no model and the judge agent's own resolves, which is the `kirocrew-lite` background template's model rather than the model of whichever session the tick belongs to -- and that is what the picker's inherit default means end to end.

`auto` resolves to Jev when the Jev side is ARMED for this point, and to the small model otherwise, so an owner with no key needs no setting at all. An explicitly pinned `jev` without that arming refuses, and the tick then fires exactly as the ungated timer does: the owner named a lane, and quietly answering from a different model would be a substitution rather than a decision.

**The two lanes are authorized by different things, because they send to different places.** The Jev lane sends conversation state to a paid third party, so it needs the keystone in full: consent for the configured endpoint AND this point's own `nudge_evidence` scope. That is what a scope MEANS here, a category of egress to the Jev endpoint, and the judge reads it fail-closed (`gate._point_scope_granted`), where a build with no registration for the point is a refusal rather than the permissive answer `_scope_consented` gives a point that sends nothing extra.

The `llm` lane needs no consent row: `provider = llm` plus a `judge` spec on the loop is its whole authorization. It adds no DESTINATION, because the state goes to the model provider the owner's sessions already send to every turn. It adds no DATA CLASS, because the evidence is the owner's own children's transcripts, review comments and ledger events, which that provider already received when those sessions ran. And the judge chooses only QUIET against firing and is fail-open, so the worst case of every failure -- a refusal, a timeout, prose, an invalid answer -- is one delayed wake, bounded by the quiet-streak floor the autonudge loop enforces around its own ask site, which lands with the point's registration rather than here, while the lane removes more model turns than it spends. What still binds it is the FLEET ceiling: a managed install that pinned `capabilities.decisions` off withdrew the seam, not one provider's endpoint, so the LLM lane pays that governed probe itself rather than inheriting it from the keystone read.

The lane's budget is the seam's own `provider.timeout_ms` CLAMPED UP into 8..60 seconds. That number is sized for Jev (1 s by default, ~100 ms in practice) and a text model cannot answer inside it, so a lane that honoured it alone would time out on every tick and the feature would read as "does nothing" rather than as "misconfigured". Clamped rather than replaced, so an operator who deliberately raised it still gets what they asked for, up to a ceiling that keeps one tick from holding its loop indefinitely.

The oracle is `decisions/impl_llm.LlmOracle`, one tool-less run of the shipped `kirocrew-lite` agent: no tools, no MCP servers, no skills. It runs on that agent rather than one of its own, because a judge template would differ from lite in exactly two fields and neither survives inspection -- the model, which the runner passes per call, and a standing system prompt, every sentence of which the per-call prompt already states more completely. The prompt carries the state fenced as data plus each question with its id, type and options, and the answer must be ONE JSON object whose keys are EXACTLY the question ids. Parsing is as strict as `impl_jev._from_wire`: a choice must be in that question's own option list, every number must be finite in 0..1 and not a bool, a `probabilities` map may name only that question's options and must include the chosen one, and a complete distribution must sum to 1. A two-option question may answer with a bare number, read as the probability of the first option. A response over 64 KiB is refused unread, counted per chunk as it streams so the ceiling bounds what is held rather than what is parsed. Anything else raises, and the gate turns a raise into the same `None` a timeout produces, which is the caller's FALLBACK. Every raise carries a CONSTANT string, so no model prose reaches an exception message or a log row -- the same bound section 5 puts on the log.

The `nudge.wake` row ships on the Decisions card: its label and one-line description in the card's own name maps, and a provider picker over `auto | jev | llm` plus a model picker in the point's detail panel, written through the same config mutation as the `decisions.model_route.<tier>` pins. Both pickers stay reachable while the card's consent switch is off, because that switch governs the Jev endpoint and the `llm` lane does not use it.

The row's status is the GATEWAY's, and it names the LANE that would answer rather than the state of the switch. `_points` reports a pinned `llm` as running on its own, a pinned `jev` only when the Jev side is armed for this point, and `auto` when either lane can answer; the sampled share binds all three, since a share of zero asks no tick whatever the provider says, and the fleet denial refuses all three. The card renders the lane word the payload carries instead of deriving one, so a row that reads `Judged by the small model` means that lane and no other.
